<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://karankessy.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://karankessy.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-09-10T15:03:05+00:00</updated><id>https://karankessy.github.io/feed.xml</id><title type="html">Karan Kc</title><subtitle>Here I try exploring the philosophy behind how systems work; from system internals and application security to the patterns behind internet communication. </subtitle><entry><title type="html">A From-Scratch Tour of OpenFlow, From the Wire Up</title><link href="https://karankessy.github.io/blog/2026/openflow-from-the-wire-up/" rel="alternate" type="text/html" title="A From-Scratch Tour of OpenFlow, From the Wire Up"/><published>2026-09-10T00:00:00+00:00</published><updated>2026-09-10T00:00:00+00:00</updated><id>https://karankessy.github.io/blog/2026/openflow-from-the-wire-up</id><content type="html" xml:base="https://karankessy.github.io/blog/2026/openflow-from-the-wire-up/"><![CDATA[<blockquote> <p><em>“What I cannot create, I do not understand.”</em> — Feynman, allegedly on a blackboard, definitely on every networking engineer’s conscience.</p> </blockquote> <p>I have read the OpenFlow spec maybe four times over the years and each time I came away with the same uneasy feeling: I could recite the message types, I could draw the controller-above-switch picture, and I still could not have told you what <em>physically happens</em> when a <code class="language-plaintext highlighter-rouge">FLOW_MOD</code> lands on a switch. Which is a bad sign. Knowing that <code class="language-plaintext highlighter-rouge">OFPT_FLOW_MOD = 14</code> is trivia. Knowing why a 100 Gbps ASIC can evaluate ten thousand wildcard rules in a handful of nanoseconds, and why your controller absolutely must not be in that path, is the actual content.</p> <p>So I did the thing that always works: I wrote a tiny OpenFlow switch and a tiny controller in Python, ~600 lines total, no dependencies beyond the stdlib, and then went and read what the hardware people actually built. This post is the writeup. We’ll go bottom-up — photons, PHY, parser, TCAM, pipeline, actions, counters — and only <em>then</em> talk about the protocol, because by that point the protocol is almost obvious.</p> <p>Fair warning on the artifact: everything I built runs in software on a laptop, which means it’s a <em>model</em> of a switch, not a switch. Every hardware number in here comes from published papers or from arithmetic, and I’ve flagged which is which. The software numbers are mine and they’re rough.</p> <hr/> <h2 id="the-one-number-that-explains-everything">The one number that explains everything</h2> <p>Start here, because the entire architecture falls out of it.</p> <p>100 Gigabit Ethernet. Minimum-size frame: 64 bytes of Ethernet frame, plus 8 bytes of preamble/SFD, plus a 12-byte interframe gap. So 84 bytes on the wire, 672 bits.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>100e9 bits/s ÷ 672 bits/packet = 148,809,523 packets/s
</code></pre></div></div> <p>~148.8 Mpps. That’s the canonical number and you can derive it on a napkin. Invert it:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1 / 148.8e6 = 6.72 nanoseconds per packet
</code></pre></div></div> <p>Now put a 3 GHz core next to it. 6.72 ns × 3e9 cycles/s = <strong>about 20 clock cycles per packet</strong>, per 100G port, if a single core had to do the whole job. Twenty cycles. A single L3 cache miss is ~40 cycles. A DRAM access is a few hundred. You cannot do a hash lookup that misses cache. You cannot take an interrupt. You cannot, hilariously, even afford a mispredicted branch.</p> <p>And a mid-range top-of-rack switch today has 32 or 64 of these ports.</p> <p>So: whatever a switch is, it is <em>not</em> a computer running a <code class="language-plaintext highlighter-rouge">while</code> loop over packets. That’s the whole thing. Everything else in this post — TCAM, pipelining, the control/data plane split, the very existence of OpenFlow — is a consequence of those 20 cycles.</p> <p>I like to think of it as the memory hierarchy argument transplanted into networking. In a CPU you have registers, L1, L2, DRAM, disk, each ~10x slower and ~10x bigger than the last, and good code is code that respects the hierarchy. A switch has the same shape: a fixed-function ASIC pipeline (nanoseconds, tiny state), the switch CPU (microseconds, megabytes), the controller (milliseconds, unbounded). Good network design is design that respects <em>that</em> hierarchy. Sending every packet to the controller is the networking equivalent of doing your inner loop over swap.</p> <hr/> <h2 id="two-machines-in-one-box">Two machines in one box</h2> <p>Open a switch and you find two computers glued together.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>                 CONTROL PLANE                     ~ms, GBs of RAM
              ┌────────────────────┐
              │ CPU: Linux/NOS     │
              │ BGP, OSPF, STP     │
              │ OpenFlow agent     │
              │ gNMI, SNMP, CLI    │
              └─────────┬──────────┘
                        │  PCIe / MDIO / vendor SDK
                        │  "program these tables"
                        ▼
              ┌────────────────────┐
   packet ───►│ ASIC: DATA PLANE   │───► packet    ~ns, MBs of SRAM/TCAM
              └────────────────────┘
</code></pre></div></div> <p>The control plane is a normal-ish computer. Often a modest x86 or ARM core running Linux. It thinks in seconds and milliseconds. It runs routing protocols, holds the RIB, talks to humans.</p> <p>The data plane is an ASIC. It thinks in nanoseconds and it does exactly one thing: turn incoming bits into outgoing bits according to tables that somebody else filled in.</p> <p>The connection between them is <em>table writes</em>. That’s it. That’s the interface. The control plane doesn’t “handle packets”; it populates state that the data plane consults. Once you internalize that the CPU→ASIC interface is a table-programming API, OpenFlow stops looking like a networking protocol and starts looking like what it is: <strong>a standardized, remote version of that same table-programming API</strong>.</p> <p>That reframing is, I think, the single most useful sentence in this post. OpenFlow is a remote table-write interface with a vendor-neutral schema.</p> <p>This lineage is not something I’m inventing post-hoc, either — it’s explicit in the intellectual history. Feamster, Rexford &amp; Zegura’s <em>The Road to SDN</em> (ACM CCR 2014) traces the line from active networks through the IETF’s <strong>ForCES</strong> (RFC 5810, literally “Forwarding and Control Element Separation”), the <strong>RCP</strong> routing control platform, the <strong>4D</strong> architecture (Greenberg et al., CCR 2005), and <strong>Ethane</strong> (Casado et al., SIGCOMM 2007) — which is essentially OpenFlow’s direct parent — straight into McKeown et al.’s <em>OpenFlow: Enabling Innovation in Campus Networks</em> (CCR, April 2008). OpenFlow’s actual innovation over ForCES was not the idea. It was the pragmatism: <em>don’t ask vendors to expose their ASIC; ask them to expose the flow table they already have for ACLs.</em></p> <hr/> <h2 id="lets-follow-one-packet">Let’s follow one packet</h2> <p>Client <code class="language-plaintext highlighter-rouge">10.10.1.25:49152</code> → server <code class="language-plaintext highlighter-rouge">10.10.2.50:443</code>. TCP SYN, 74 bytes.</p> <h3 id="1-photons--bits-phy">1. Photons → bits (PHY)</h3> <p>The frame arrives as modulated light in a fiber, hits a transceiver, becomes an electrical signal, and lands in the PHY. The PHY’s job is decidedly unglamorous and completely essential: clock recovery, line-code decoding (64b/66b for 10G+, PAM4 for the modern high-lane-rate stuff), lane deskew across the multiple physical lanes that make up one logical 100G link, FEC. Out the other side comes a clean bitstream with frame boundaries.</p> <p>There is nothing “networking” about this layer. It’s signal processing. But it’s where your link flaps and your mysterious CRC errors come from, so.</p> <h3 id="2-bits--frame-mac">2. Bits → frame (MAC)</h3> <p>The MAC finds the preamble, strips it, validates the FCS (CRC-32) over the frame, and hands up:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>┌──────────┬──────────┬───────────┬─────────────────────┬─────┐
│ dst MAC  │ src MAC  │ EtherType │      payload        │ FCS │
│  6 B     │  6 B     │   2 B     │      46-1500 B      │ 4 B │
└──────────┴──────────┴───────────┴─────────────────────┴─────┘
</code></pre></div></div> <p>EtherType <code class="language-plaintext highlighter-rouge">0x0800</code> → IPv4. Now we have to look deeper, and this is where it gets genuinely interesting.</p> <h3 id="3-frame--fields-the-parser">3. Frame → fields (the parser)</h3> <p>Here’s the naive mental model, and it’s wrong: <em>“IP header starts at byte 14.”</em></p> <p>It doesn’t, reliably. Consider what can actually show up:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Eth │ IPv4 │ TCP                                    → IP at byte 14
Eth │ VLAN │ IPv4 │ TCP                             → IP at byte 18
Eth │ VLAN │ VLAN │ IPv4 │ TCP                      → IP at byte 22   (QinQ)
Eth │ MPLS │ MPLS │ MPLS │ IPv4 │ TCP               → IP at byte 26
Eth │ IPv6 │ HopByHop │ Routing │ Frag │ TCP        → good luck
Eth │ IPv4 │ UDP │ VXLAN │ Eth │ IPv4 │ TCP         → two of everything
</code></pre></div></div> <p>The parser is a <strong>state machine over the header graph</strong>. You start in the Ethernet state, read a field, and that field tells you which state to jump to next:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>        ┌──────────┐
        │ Ethernet │
        └────┬─────┘
   0x8100│   │0x0800        │0x86DD
   ┌─────┘   ▼              ▼
   ▼      ┌──────┐      ┌──────┐
┌──────┐  │ IPv4 │      │ IPv6 │
│ VLAN │  └──┬───┘      └──┬───┘
└──┬───┘  6  │ 17          │ 6 / 17 / ext-hdr chain
   └────►    ▼             ▼
        ┌───────┐     ┌───────┐
        │  TCP  │     │  UDP  │
        └───────┘     └───────┘
</code></pre></div></div> <p>In hardware this is implemented as a pipeline of parser stages, each of which extracts a fixed-width chunk, matches it against a small TCAM of “next header” transitions, and advances a cursor. It’s a compiled DFA in silicon. The RMT paper (Bosshart et al., <em>Forwarding Metamorphosis</em>, SIGCOMM 2013) describes exactly this and it’s the single best hardware-architecture paper in the area — if you read one citation from this post, read that one.</p> <p>The parser is also, incidentally, where a <em>lot</em> of security bugs live, because “the parser and the endpoint disagree about where the headers are” is the entire ambiguity-based-IDS-evasion literature (Ptacek &amp; Newsham 1998 is still depressingly relevant). Every additional encapsulation you support is another chance for the switch and the host to parse a packet differently.</p> <p>Out of the parser comes a bag of fields — the <strong>packet header vector</strong> in RMT terminology:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span>
  <span class="sh">'</span><span class="s">in_port</span><span class="sh">'</span><span class="p">:</span>   <span class="mi">3</span><span class="p">,</span>
  <span class="sh">'</span><span class="s">eth_src</span><span class="sh">'</span><span class="p">:</span>   <span class="sh">'</span><span class="s">aa:bb:cc:dd:ee:ff</span><span class="sh">'</span><span class="p">,</span>
  <span class="sh">'</span><span class="s">eth_dst</span><span class="sh">'</span><span class="p">:</span>   <span class="sh">'</span><span class="s">11:22:33:44:55:66</span><span class="sh">'</span><span class="p">,</span>
  <span class="sh">'</span><span class="s">eth_type</span><span class="sh">'</span><span class="p">:</span>  <span class="mh">0x0800</span><span class="p">,</span>
  <span class="sh">'</span><span class="s">vlan_vid</span><span class="sh">'</span><span class="p">:</span>  <span class="bp">None</span><span class="p">,</span>
  <span class="sh">'</span><span class="s">ipv4_src</span><span class="sh">'</span><span class="p">:</span>  <span class="sh">'</span><span class="s">10.10.1.25</span><span class="sh">'</span><span class="p">,</span>
  <span class="sh">'</span><span class="s">ipv4_dst</span><span class="sh">'</span><span class="p">:</span>  <span class="sh">'</span><span class="s">10.10.2.50</span><span class="sh">'</span><span class="p">,</span>
  <span class="sh">'</span><span class="s">ip_proto</span><span class="sh">'</span><span class="p">:</span>  <span class="mi">6</span><span class="p">,</span>
  <span class="sh">'</span><span class="s">tcp_src</span><span class="sh">'</span><span class="p">:</span>   <span class="mi">49152</span><span class="p">,</span>
  <span class="sh">'</span><span class="s">tcp_dst</span><span class="sh">'</span><span class="p">:</span>   <span class="mi">443</span><span class="p">,</span>
<span class="p">}</span>
</code></pre></div></div> <p>That dict is the whole point. Everything downstream operates on this ~100-500 bit vector, not on the packet. The packet body goes and sits in a buffer and doesn’t participate. This is a <em>massive</em> engineering simplification and it’s why the pipeline can be fixed-latency: the pipeline processes a small fixed-size struct, not a variable-length blob.</p> <p>Here’s the parser from my toy switch. It’s the honest version of the diagram above:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">struct</span>

<span class="k">def</span> <span class="nf">parse</span><span class="p">(</span><span class="n">pkt</span><span class="p">:</span> <span class="nb">bytes</span><span class="p">,</span> <span class="n">in_port</span><span class="p">:</span> <span class="nb">int</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">dict</span><span class="p">:</span>
    <span class="n">m</span> <span class="o">=</span> <span class="p">{</span><span class="sh">'</span><span class="s">in_port</span><span class="sh">'</span><span class="p">:</span> <span class="n">in_port</span><span class="p">}</span>
    <span class="n">m</span><span class="p">[</span><span class="sh">'</span><span class="s">eth_dst</span><span class="sh">'</span><span class="p">],</span> <span class="n">m</span><span class="p">[</span><span class="sh">'</span><span class="s">eth_src</span><span class="sh">'</span><span class="p">],</span> <span class="n">et</span> <span class="o">=</span> <span class="n">struct</span><span class="p">.</span><span class="nf">unpack_from</span><span class="p">(</span><span class="sh">'</span><span class="s">!6s6sH</span><span class="sh">'</span><span class="p">,</span> <span class="n">pkt</span><span class="p">,</span> <span class="mi">0</span><span class="p">)</span>
    <span class="n">off</span> <span class="o">=</span> <span class="mi">14</span>

    <span class="c1"># VLAN tags: chase the stack
</span>    <span class="n">m</span><span class="p">[</span><span class="sh">'</span><span class="s">vlan_vid</span><span class="sh">'</span><span class="p">]</span> <span class="o">=</span> <span class="bp">None</span>
    <span class="k">while</span> <span class="n">et</span> <span class="ow">in</span> <span class="p">(</span><span class="mh">0x8100</span><span class="p">,</span> <span class="mh">0x88a8</span><span class="p">):</span>
        <span class="n">tci</span><span class="p">,</span> <span class="n">et</span> <span class="o">=</span> <span class="n">struct</span><span class="p">.</span><span class="nf">unpack_from</span><span class="p">(</span><span class="sh">'</span><span class="s">!HH</span><span class="sh">'</span><span class="p">,</span> <span class="n">pkt</span><span class="p">,</span> <span class="n">off</span><span class="p">)</span>
        <span class="k">if</span> <span class="n">m</span><span class="p">[</span><span class="sh">'</span><span class="s">vlan_vid</span><span class="sh">'</span><span class="p">]</span> <span class="ow">is</span> <span class="bp">None</span><span class="p">:</span>
            <span class="n">m</span><span class="p">[</span><span class="sh">'</span><span class="s">vlan_vid</span><span class="sh">'</span><span class="p">]</span> <span class="o">=</span> <span class="n">tci</span> <span class="o">&amp;</span> <span class="mh">0x0fff</span>
        <span class="n">off</span> <span class="o">+=</span> <span class="mi">4</span>

    <span class="n">m</span><span class="p">[</span><span class="sh">'</span><span class="s">eth_type</span><span class="sh">'</span><span class="p">]</span> <span class="o">=</span> <span class="n">et</span>
    <span class="k">if</span> <span class="n">et</span> <span class="o">!=</span> <span class="mh">0x0800</span><span class="p">:</span>
        <span class="k">return</span> <span class="n">m</span>                              <span class="c1"># not IPv4, we're done
</span>
    <span class="n">ihl</span> <span class="o">=</span> <span class="p">(</span><span class="n">pkt</span><span class="p">[</span><span class="n">off</span><span class="p">]</span> <span class="o">&amp;</span> <span class="mh">0x0f</span><span class="p">)</span> <span class="o">*</span> <span class="mi">4</span>               <span class="c1"># &lt;- variable-length header
</span>    <span class="n">proto</span> <span class="o">=</span> <span class="n">pkt</span><span class="p">[</span><span class="n">off</span> <span class="o">+</span> <span class="mi">9</span><span class="p">]</span>
    <span class="n">m</span><span class="p">[</span><span class="sh">'</span><span class="s">ip_proto</span><span class="sh">'</span><span class="p">]</span> <span class="o">=</span> <span class="n">proto</span>
    <span class="n">m</span><span class="p">[</span><span class="sh">'</span><span class="s">ipv4_src</span><span class="sh">'</span><span class="p">]</span> <span class="o">=</span> <span class="n">pkt</span><span class="p">[</span><span class="n">off</span><span class="o">+</span><span class="mi">12</span><span class="p">:</span><span class="n">off</span><span class="o">+</span><span class="mi">16</span><span class="p">]</span>
    <span class="n">m</span><span class="p">[</span><span class="sh">'</span><span class="s">ipv4_dst</span><span class="sh">'</span><span class="p">]</span> <span class="o">=</span> <span class="n">pkt</span><span class="p">[</span><span class="n">off</span><span class="o">+</span><span class="mi">16</span><span class="p">:</span><span class="n">off</span><span class="o">+</span><span class="mi">20</span><span class="p">]</span>
    <span class="n">off</span> <span class="o">+=</span> <span class="n">ihl</span>

    <span class="k">if</span> <span class="n">proto</span> <span class="ow">in</span> <span class="p">(</span><span class="mi">6</span><span class="p">,</span> <span class="mi">17</span><span class="p">):</span>                      <span class="c1"># TCP or UDP
</span>        <span class="n">sport</span><span class="p">,</span> <span class="n">dport</span> <span class="o">=</span> <span class="n">struct</span><span class="p">.</span><span class="nf">unpack_from</span><span class="p">(</span><span class="sh">'</span><span class="s">!HH</span><span class="sh">'</span><span class="p">,</span> <span class="n">pkt</span><span class="p">,</span> <span class="n">off</span><span class="p">)</span>
        <span class="n">m</span><span class="p">[</span><span class="sh">'</span><span class="s">tcp_src</span><span class="sh">'</span><span class="p">],</span> <span class="n">m</span><span class="p">[</span><span class="sh">'</span><span class="s">tcp_dst</span><span class="sh">'</span><span class="p">]</span> <span class="o">=</span> <span class="n">sport</span><span class="p">,</span> <span class="n">dport</span>
    <span class="k">return</span> <span class="n">m</span>
</code></pre></div></div> <p>Note <code class="language-plaintext highlighter-rouge">ihl</code> — the IPv4 header is variable length, so even “skip the IP header” is a data-dependent computation. In hardware that’s a shifter fed by a field you just extracted. Little things like this are why parsers are hard.</p> <h3 id="4-fields--decision-the-match">4. Fields → decision (the match)</h3> <p>Now the flow table. The naive implementation, which is what I wrote first:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">lookup</span><span class="p">(</span><span class="n">match</span><span class="p">,</span> <span class="n">table</span><span class="p">):</span>
    <span class="n">best</span> <span class="o">=</span> <span class="bp">None</span>
    <span class="k">for</span> <span class="n">entry</span> <span class="ow">in</span> <span class="n">table</span><span class="p">:</span>                       <span class="c1"># O(n). RIP.
</span>        <span class="k">if</span> <span class="n">entry</span><span class="p">.</span><span class="nf">matches</span><span class="p">(</span><span class="n">match</span><span class="p">):</span>
            <span class="k">if</span> <span class="n">best</span> <span class="ow">is</span> <span class="bp">None</span> <span class="ow">or</span> <span class="n">entry</span><span class="p">.</span><span class="n">priority</span> <span class="o">&gt;</span> <span class="n">best</span><span class="p">.</span><span class="n">priority</span><span class="p">:</span>
                <span class="n">best</span> <span class="o">=</span> <span class="n">entry</span>
    <span class="k">return</span> <span class="n">best</span>
</code></pre></div></div> <p>On my laptop this does something like a few hundred thousand packets/sec with a 1000-entry table — the exact number depends enormously on your Python version and how many fields each rule constrains, so don’t quote mine, run your own. The point is the shape, not the constant: it’s O(n) in table size and it’s three to four orders of magnitude away from line rate on a single 100G port. That gap is not closeable by optimizing Python. It’s not closeable by rewriting in C either — remember, the budget is 20 cycles.</p> <p>It’s closeable by changing the memory.</p> <hr/> <h2 id="tcam-or-why-networking-hardware-is-weird">TCAM, or: why networking hardware is weird</h2> <p>Regular RAM: you give it an address, it gives you contents. CAM inverts this: you give it contents, it gives you the address where that content lives — in one shot, by comparing against <em>every</em> entry in parallel.</p> <p>TCAM (“ternary”) adds a third state per bit: <strong>X, don’t care</strong>. Which happens to be exactly what a subnet mask is.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>rule:    192.168.1.0/24
bits:    11000000 10101000 00000001 XXXXXXXX
                                    ^^^^^^^^ don't care

packet:  192.168.1.77
bits:    11000000 10101000 00000001 01001101
                                             → MATCH
</code></pre></div></div> <p>Every TCAM entry compares itself against the search key simultaneously. A priority encoder picks the lowest-index match. The result is an index, which indexes into a parallel SRAM holding the <em>action</em>. Lookup time: <strong>one clock, independent of table size.</strong> That’s the trick. That’s the whole trick.</p> <p>The cost is brutal and worth stating precisely:</p> <ul> <li>An SRAM cell is 6 transistors. A TCAM cell is on the order of <strong>16</strong> — it stores two bits (value + mask) <em>and</em> carries its own comparator.</li> <li>Every entry is powered and switching on every lookup, because they all compare in parallel. Power scales with <em>capacity</em>, not with utilization. An empty TCAM burns nearly as much as a full one. Agrawal &amp; Sherwood’s TCAM power model (ISPASS 2008) is the reference here; the folk summary “TCAM is roughly an order of magnitude worse than SRAM in both area and power per bit” is close enough for reasoning, and I’d hedge anything more precise than that.</li> </ul> <p>Which is why merchant silicon ships with TCAM budgets measured in the low thousands to low tens of thousands of entries, while the same chip has room for hundreds of thousands of exact-match L2/L3 entries in SRAM. DevoFlow (Curtis et al., SIGCOMM 2011) called this out directly and it remains the correct framing: <strong>flow table space is a scarce, contended, physically expensive resource</strong>, and any SDN design that pretends otherwise will fall over.</p> <p>So real ASICs stratify:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>              ┌─────────────────────────────────────┐
              │            LOOKUP MEMORY            │
              ├──────────────┬──────────────────────┤
   wildcard   │    TCAM      │  ACLs, OF wildcards  │  1000s
   longest-px │    TCAM/algo │  IP routing (LPM)    │  10Ks-100Ks
   exact      │  hash + SRAM │  MAC table, 5-tuple  │  100Ks+
   indexed    │    SRAM      │  port/VLAN configs   │  cheap
              └──────────────┴──────────────────────┘
</code></pre></div></div> <p>The rule of thumb that falls out: <strong>if your match is exact, never spend TCAM on it.</strong> Hash it. TCAM is for wildcards, ranges, and priority, and for nothing else.</p> <p>Ranges deserve a footnote of shame. TCAM does prefixes natively but not ranges — <code class="language-plaintext highlighter-rouge">tcp_dst ∈ [1024, 65535]</code> isn’t a prefix. Classic prefix expansion turns one range into up to <code class="language-plaintext highlighter-rouge">2W-2</code> prefixes for a W-bit field, so a single arbitrary 16-bit port range can burn up to 30 TCAM entries, and a rule with ranges on <em>two</em> fields multiplies. This is why “just add a firewall rule” sometimes silently costs you 900 hardware entries. Vendors mitigate with range encoders and DIRPE-style tricks, but the tax is real.</p> <hr/> <h2 id="the-pipeline">The pipeline</h2> <p>We can now do one lookup in one clock. But a real forwarding decision isn’t one lookup — it’s L2, then ACL, then L3, then QoS, then rewrite, then egress ACL. Six-ish dependent lookups. Do them sequentially and you’re back to being slow.</p> <p>So: pipeline it, exactly like a CPU pipeline.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>   time ──────────────────────────────────────────────►
   pkt 1  [PRS][L2 ][ACL][L3 ][MOD][EGR]
   pkt 2       [PRS][L2 ][ACL][L3 ][MOD][EGR]
   pkt 3            [PRS][L2 ][ACL][L3 ][MOD][EGR]
   pkt 4                 [PRS][L2 ][ACL][L3 ][MOD][EGR]
                              ▲
                    every stage busy, one packet retires per clock
</code></pre></div></div> <p>Latency per packet: six stages. Throughput: <strong>one packet per clock</strong>, once the pipe is full. At ~1 GHz that’s a billion packets/sec of pipeline capacity, which you then statically fan out across ports. This is why a switch can advertise 6.4 Tbps and sub-microsecond port-to-port latency at the same time: those are throughput and latency, and pipelining decouples them.</p> <p>The constraint this imposes is the one people underestimate: <strong>the pipeline only goes forward.</strong> No loops, no backtracking, no “go re-check table 2.” Each stage sees the packet header vector once, for one clock, and passes it on. If your policy needs to consult a table twice, you either need two physical stages holding copies of it, or you need recirculation (send the packet back to the top), which costs you bandwidth out of your own budget.</p> <p>RMT’s contribution was showing you could make this pipeline <em>reconfigurable</em> — 32 match-action stages with flexibly allocated TCAM/SRAM blocks and a programmable parser — at 64×10 GbE, and they estimated the area cost at under ~15% versus a fixed-function chip. That number is what made programmable switching commercially plausible rather than a research toy, and it’s the direct ancestor of Tofino.</p> <hr/> <h2 id="now-finally-openflow">Now, finally, OpenFlow</h2> <p>With the hardware in view, the protocol is nearly derivable.</p> <p>OpenFlow says: expose the pipeline as a sequence of <strong>flow tables</strong>, where each table holds entries of the form</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>┌──────────┬────────────────┬──────────────┬──────────┬─────────────┐
│ priority │ match fields   │ instructions │ counters │  timeouts   │
└──────────┴────────────────┴──────────────┴──────────┴─────────────┘
</code></pre></div></div> <p>and let a remote controller write those entries over a TCP (ideally TLS) channel.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>      TABLE 0            TABLE 1            TABLE 2
   ┌───────────┐      ┌───────────┐      ┌───────────┐
   │ classify  │─────►│  security │─────►│ forward   │───► action set
   └───────────┘ goto └───────────┘ goto └───────────┘        │
        │                  │                                  ▼
     table-miss         DROP                               execute
        │
        ▼
    PACKET_IN to controller
</code></pre></div></div> <p>A packet enters table 0, matches at most one entry (highest priority wins), and that entry’s <em>instructions</em> can modify an action set, write metadata, and <code class="language-plaintext highlighter-rouge">goto-table N</code> for N &gt; current. Strictly increasing — because, as we just established, the pipeline only goes forward. The spec’s table-chaining rule is not an arbitrary design choice; it is silicon leaking through the abstraction, and once you see it that way the spec reads much better.</p> <p>The version history is basically a story of the abstraction chasing the hardware:</p> <table> <thead> <tr> <th>Version</th> <th>Year</th> <th>What it added</th> <th>What it reveals</th> </tr> </thead> <tbody> <tr> <td>1.0</td> <td>2009</td> <td>Single table, fixed 12-tuple match</td> <td>“assume every switch has one ACL TCAM”</td> </tr> <tr> <td>1.1</td> <td>2011</td> <td>Multiple tables, groups, MPLS</td> <td>“oh, switches are pipelines”</td> </tr> <tr> <td>1.3</td> <td>2012</td> <td>OXM extensible match (~40 fields), meters, multiple controllers</td> <td>“oh, and the field set keeps growing”</td> </tr> <tr> <td>1.5.1</td> <td>2015</td> <td>Egress tables, packet-type-aware pipeline</td> <td>“…and there’s an egress pipeline too”</td> </tr> </tbody> </table> <p>OF 1.0’s twelve-tuple was chosen almost exactly because that’s what commodity ACL TCAMs of 2009 could already match on. Beautiful pragmatism. Also the seed of every later problem, because the moment you standardize a <em>fixed</em> field list, you’ve frozen the abstraction and the hardware keeps moving.</p> <p>Here’s the flow table from my toy, with priority ordering and OXM-ish wildcards:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">FlowEntry</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">priority</span><span class="p">,</span> <span class="n">match</span><span class="p">,</span> <span class="n">instructions</span><span class="p">,</span>
                 <span class="n">idle_timeout</span><span class="o">=</span><span class="mi">0</span><span class="p">,</span> <span class="n">hard_timeout</span><span class="o">=</span><span class="mi">0</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">priority</span> <span class="o">=</span> <span class="n">priority</span>
        <span class="n">self</span><span class="p">.</span><span class="n">match</span> <span class="o">=</span> <span class="k">match</span>                    <span class="c1"># dict of field -&gt; value; absent = wildcard
</span>        <span class="n">self</span><span class="p">.</span><span class="n">instructions</span> <span class="o">=</span> <span class="n">instructions</span>
        <span class="n">self</span><span class="p">.</span><span class="n">idle_timeout</span> <span class="o">=</span> <span class="n">idle_timeout</span>
        <span class="n">self</span><span class="p">.</span><span class="n">hard_timeout</span> <span class="o">=</span> <span class="n">hard_timeout</span>
        <span class="n">self</span><span class="p">.</span><span class="n">packet_count</span> <span class="o">=</span> <span class="mi">0</span>
        <span class="n">self</span><span class="p">.</span><span class="n">byte_count</span> <span class="o">=</span> <span class="mi">0</span>
        <span class="n">self</span><span class="p">.</span><span class="n">created</span> <span class="o">=</span> <span class="n">time</span><span class="p">.</span><span class="nf">monotonic</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">last_used</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">created</span>

    <span class="k">def</span> <span class="nf">matches</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">m</span><span class="p">):</span>
        <span class="k">for</span> <span class="n">field</span><span class="p">,</span> <span class="n">want</span> <span class="ow">in</span> <span class="n">self</span><span class="p">.</span><span class="k">match</span><span class="p">.</span><span class="nf">items</span><span class="p">():</span>
            <span class="k">if</span> <span class="n">field</span><span class="p">.</span><span class="nf">endswith</span><span class="p">(</span><span class="sh">'</span><span class="s">_prefix</span><span class="sh">'</span><span class="p">):</span>     <span class="c1"># e.g. ipv4_dst_prefix
</span>                <span class="n">base</span><span class="p">,</span> <span class="n">plen</span> <span class="o">=</span> <span class="n">want</span>
                <span class="k">if</span> <span class="ow">not</span> <span class="nf">in_prefix</span><span class="p">(</span><span class="n">m</span><span class="p">[</span><span class="n">field</span><span class="p">[:</span><span class="o">-</span><span class="mi">7</span><span class="p">]],</span> <span class="n">base</span><span class="p">,</span> <span class="n">plen</span><span class="p">):</span>
                    <span class="k">return</span> <span class="bp">False</span>
            <span class="k">elif</span> <span class="n">m</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="n">field</span><span class="p">)</span> <span class="o">!=</span> <span class="n">want</span><span class="p">:</span>
                <span class="k">return</span> <span class="bp">False</span>
        <span class="k">return</span> <span class="bp">True</span>                           <span class="c1"># absent field == don't care == X
</span>

<span class="k">class</span> <span class="nc">FlowTable</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">table_id</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">table_id</span> <span class="o">=</span> <span class="n">table_id</span>
        <span class="n">self</span><span class="p">.</span><span class="n">entries</span> <span class="o">=</span> <span class="p">[]</span>                     <span class="c1"># kept sorted by -priority
</span>
    <span class="k">def</span> <span class="nf">add</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">e</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">entries</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">e</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">entries</span><span class="p">.</span><span class="nf">sort</span><span class="p">(</span><span class="n">key</span><span class="o">=</span><span class="k">lambda</span> <span class="n">x</span><span class="p">:</span> <span class="o">-</span><span class="n">x</span><span class="p">.</span><span class="n">priority</span><span class="p">)</span>   <span class="c1"># software's fake priority encoder
</span>
    <span class="k">def</span> <span class="nf">lookup</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">m</span><span class="p">,</span> <span class="n">nbytes</span><span class="p">):</span>
        <span class="k">for</span> <span class="n">e</span> <span class="ow">in</span> <span class="n">self</span><span class="p">.</span><span class="n">entries</span><span class="p">:</span>                <span class="c1"># sorted =&gt; first match is highest priority
</span>            <span class="k">if</span> <span class="n">e</span><span class="p">.</span><span class="nf">matches</span><span class="p">(</span><span class="n">m</span><span class="p">):</span>
                <span class="n">e</span><span class="p">.</span><span class="n">packet_count</span> <span class="o">+=</span> <span class="mi">1</span>
                <span class="n">e</span><span class="p">.</span><span class="n">byte_count</span> <span class="o">+=</span> <span class="n">nbytes</span>
                <span class="n">e</span><span class="p">.</span><span class="n">last_used</span> <span class="o">=</span> <span class="n">time</span><span class="p">.</span><span class="nf">monotonic</span><span class="p">()</span>
                <span class="k">return</span> <span class="n">e</span>
        <span class="k">return</span> <span class="bp">None</span>                           <span class="c1"># table miss
</span></code></pre></div></div> <p>That <code class="language-plaintext highlighter-rouge">self.entries.sort(key=lambda x: -x.priority)</code> line is doing in software, badly, what a TCAM priority encoder does in one clock for free. And that early-return loop is the O(n) that hardware turns into O(1). Two lines, two orders of the entire hardware architecture. I find that pleasing.</p> <p>Also note: the counters live <em>on the entry</em>, updated on match. That’s not an add-on, it’s part of the abstraction, and it’s what makes the controller feedback loop possible at all. In hardware they’re SRAM counter banks incremented by the action unit, periodically drained by the CPU.</p> <hr/> <h2 id="the-table-miss-and-the-mistake-everyone-makes">The table miss, and the mistake everyone makes</h2> <p>No match. Now what?</p> <p>The switch consults the <strong>table-miss flow entry</strong> (priority 0, matches everything), which typically says either <code class="language-plaintext highlighter-rouge">drop</code> or <code class="language-plaintext highlighter-rouge">output:CONTROLLER</code>. The latter generates a <code class="language-plaintext highlighter-rouge">PACKET_IN</code>, and this is the single most consequential design point in all of SDN.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code> packet ─► parse ─► lookup ─► MISS ─► PACKET_IN ─┐
                                                  │  (switch CPU, then TCP, then controller)
                                                  ▼
                                            ┌──────────┐
                                            │controller│  compute path
                                            └────┬─────┘
                                                 │ FLOW_MOD × N switches
                                                 ▼
 packet ─► parse ─► lookup ─► HIT ─► output   (every subsequent packet)
</code></pre></div></div> <p>Here’s the honest reactive controller from my build. It’s the classic Ethane-style learning switch plus a policy hook:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">on_packet_in</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">dpid</span><span class="p">,</span> <span class="n">in_port</span><span class="p">,</span> <span class="n">pkt</span><span class="p">,</span> <span class="n">match</span><span class="p">):</span>
    <span class="n">self</span><span class="p">.</span><span class="n">mac_to_port</span><span class="p">[</span><span class="n">dpid</span><span class="p">][</span><span class="k">match</span><span class="p">[</span><span class="sh">'</span><span class="s">eth_src</span><span class="sh">'</span><span class="p">]]</span> <span class="o">=</span> <span class="n">in_port</span>     <span class="c1"># learn
</span>
    <span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">policy</span><span class="p">.</span><span class="nf">is_blocked</span><span class="p">(</span><span class="n">match</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="nf">send_flow_mod</span><span class="p">(</span><span class="n">dpid</span><span class="p">,</span> <span class="n">priority</span><span class="o">=</span><span class="mi">1000</span><span class="p">,</span> <span class="n">match</span><span class="o">=</span><span class="p">{</span>
            <span class="sh">'</span><span class="s">eth_type</span><span class="sh">'</span><span class="p">:</span> <span class="mh">0x0800</span><span class="p">,</span> <span class="sh">'</span><span class="s">ipv4_src</span><span class="sh">'</span><span class="p">:</span> <span class="k">match</span><span class="p">[</span><span class="sh">'</span><span class="s">ipv4_src</span><span class="sh">'</span><span class="p">]},</span>
            <span class="n">instructions</span><span class="o">=</span><span class="p">[],</span> <span class="n">hard_timeout</span><span class="o">=</span><span class="mi">600</span><span class="p">)</span>             <span class="c1"># empty = drop
</span>        <span class="k">return</span>                                             <span class="c1"># and drop this one
</span>
    <span class="n">out</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">mac_to_port</span><span class="p">[</span><span class="n">dpid</span><span class="p">].</span><span class="nf">get</span><span class="p">(</span><span class="k">match</span><span class="p">[</span><span class="sh">'</span><span class="s">eth_dst</span><span class="sh">'</span><span class="p">])</span>
    <span class="k">if</span> <span class="n">out</span> <span class="ow">is</span> <span class="bp">None</span><span class="p">:</span>
        <span class="n">self</span><span class="p">.</span><span class="nf">packet_out</span><span class="p">(</span><span class="n">dpid</span><span class="p">,</span> <span class="n">pkt</span><span class="p">,</span> <span class="n">port</span><span class="o">=</span><span class="n">OFPP_FLOOD</span><span class="p">)</span>        <span class="c1"># don't know yet: flood
</span>        <span class="k">return</span>

    <span class="n">self</span><span class="p">.</span><span class="nf">send_flow_mod</span><span class="p">(</span><span class="n">dpid</span><span class="p">,</span> <span class="n">priority</span><span class="o">=</span><span class="mi">100</span><span class="p">,</span>
        <span class="n">match</span><span class="o">=</span><span class="p">{</span><span class="sh">'</span><span class="s">eth_src</span><span class="sh">'</span><span class="p">:</span> <span class="k">match</span><span class="p">[</span><span class="sh">'</span><span class="s">eth_src</span><span class="sh">'</span><span class="p">],</span> <span class="sh">'</span><span class="s">eth_dst</span><span class="sh">'</span><span class="p">:</span> <span class="k">match</span><span class="p">[</span><span class="sh">'</span><span class="s">eth_dst</span><span class="sh">'</span><span class="p">]},</span>
        <span class="n">instructions</span><span class="o">=</span><span class="p">[(</span><span class="sh">'</span><span class="s">output</span><span class="sh">'</span><span class="p">,</span> <span class="n">out</span><span class="p">)],</span>
        <span class="n">idle_timeout</span><span class="o">=</span><span class="mi">30</span><span class="p">,</span> <span class="n">hard_timeout</span><span class="o">=</span><span class="mi">300</span><span class="p">)</span>
    <span class="n">self</span><span class="p">.</span><span class="nf">packet_out</span><span class="p">(</span><span class="n">dpid</span><span class="p">,</span> <span class="n">pkt</span><span class="p">,</span> <span class="n">port</span><span class="o">=</span><span class="n">out</span><span class="p">)</span>                   <span class="c1"># don't drop the trigger packet
</span></code></pre></div></div> <p>Two things in there that took me embarrassingly long to get right, and which I’ll flag because I suspect they bite everyone:</p> <p><strong>One.</strong> You must <code class="language-plaintext highlighter-rouge">PACKET_OUT</code> the triggering packet after installing the rule. The rule only helps <em>future</em> packets; the one that caused the miss is sitting in your controller’s memory and if you don’t send it back, you’ve silently dropped a SYN and the connection eats a 1-second TCP retransmit timer. Path installed correctly, application still feels broken. Wonderful debugging afternoon.</p> <p><strong>Two.</strong> There is a race, and it’s unavoidable in reactive mode. Between <code class="language-plaintext highlighter-rouge">FLOW_MOD</code> and the ASIC actually committing the entry, more packets of the same flow arrive and generate <em>more</em> <code class="language-plaintext highlighter-rouge">PACKET_IN</code>s for a flow you already handled. Your controller must be idempotent about this. Also <code class="language-plaintext highlighter-rouge">FLOW_MOD</code> has no reply by default — you get no acknowledgment that it landed. If you want ordering you send an <code class="language-plaintext highlighter-rouge">OFPT_BARRIER_REQUEST</code> and wait for the reply, which serializes you and costs an RTT. The spec gives you correctness or speed, and makes you choose explicitly. I respect that more than I did before I hit the race.</p> <h3 id="why-reactive-is-a-trap-at-scale">Why reactive is a trap at scale</h3> <p>Now do the arithmetic that DevoFlow did. Published measurements of early controllers landed in the ballpark of tens of thousands of flow installs per second with flow-setup latencies in the millisecond range (NOX-era numbers; the ONIX/ONOS generation improved this substantially, and I’d treat any specific figure as version-dependent). Meanwhile, a single busy datacenter rack can generate flow arrivals in that same order of magnitude <em>by itself</em>.</p> <p>The failure mode isn’t graceful degradation. It’s this:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code> attacker sends 1M packets, each with a random src IP
              │
              ▼
      1M table misses
              │
              ▼
      1M PACKET_INs  ──► switch CPU saturates (it's a small CPU!)
              │
              ▼
      controller channel floods  ──► legitimate PACKET_INs queue behind attack traffic
              │
              ▼
      1M FLOW_MODs (if the controller is naive)  ──► TCAM exhausted at entry ~4000
</code></pre></div></div> <p>You have built, at line rate, a machine for converting cheap data-plane packets into expensive control-plane work. The amplification factor is enormous: one 64-byte spoofed packet costs the attacker ~6.7 ns of link time and costs you a controller round trip, a TCAM entry, and CPU on a switch management processor that was sized for running BGP, not for absorbing a flood.</p> <p>This is a well-studied class — AVANT-GUARD (Shin et al., CCS 2013) is the canonical treatment, and it proposes <em>connection migration</em> (have the data plane complete the TCP handshake itself and only escalate to the controller once a connection proves real) plus actuating triggers. FloodGuard, FRESCO, and the topology-poisoning work (TopoGuard, NDSS 2015 — LLDP-based topology discovery is unauthenticated by default, so a host that can inject LLDP can fabricate links in the controller’s graph) round out the picture.</p> <p>Given a security background, I’d summarize the SDN threat model as: you took the distributed control planes of N devices, centralized them into one process, and then exposed a <em>write</em> interface to the forwarding behavior of the entire network. The upside is programmability. The downside is that you have created (a) an extremely high-value target, (b) a control channel that is now a DoS amplification vector, and (c) a resource-exhaustion surface with a hardware floor of a few thousand entries. All three are consequences of the same architectural choice.</p> <h3 id="the-fix-is-proactive-mostly">The fix is proactive, mostly</h3> <p>So real deployments install rules ahead of time. Google’s <strong>B4</strong> (Jain et al., SIGCOMM 2013) — the WAN between their datacenters, one of the flagship production SDN deployments — is centrally computed and proactively programmed with traffic-engineered paths; they reported running those links at near-100% utilization, versus the 30-40% typical of over-provisioned WANs. That’s the real SDN win, and note it has approximately nothing to do with <code class="language-plaintext highlighter-rouge">PACKET_IN</code>. It’s about global visibility enabling better <em>optimization</em>, not about reactive per-flow control.</p> <p>The clean way to say it:</p> <blockquote> <p>Use the controller to compute policy over a global view. Use the data plane to execute it. Any design where the controller is in the per-flow critical path is a design that will meet the 20-cycle budget and lose.</p> </blockquote> <hr/> <h2 id="actions-groups-and-keeping-the-controller-out-of-the-loop">Actions, groups, and keeping the controller out of the loop</h2> <p>Matching answers “which packets.” Actions answer “then what.” The interesting ones:</p> <ul> <li><code class="language-plaintext highlighter-rouge">output:port</code> — the obvious one</li> <li><code class="language-plaintext highlighter-rouge">set_field</code> — rewrite any OXM field (dst MAC for routing, DSCP for QoS, …). Triggers checksum recomputation, which is a dedicated ALU in the pipeline, not free</li> <li><code class="language-plaintext highlighter-rouge">push_vlan</code> / <code class="language-plaintext highlighter-rouge">pop_mpls</code> — encap/decap, i.e. changing the <em>shape</em> of the header vector mid-pipeline</li> <li><code class="language-plaintext highlighter-rouge">meter</code> — rate limit, with mark-or-drop over threshold</li> <li><code class="language-plaintext highlighter-rouge">group</code> — indirection</li> </ul> <p>Groups are where it gets clever. A group is a list of <em>buckets</em>, each with its own actions, plus a type that says how to pick:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  ALL        → clone to every bucket        (multicast, port mirroring to an IDS)
  SELECT     → hash-pick one bucket         (ECMP / load balance)
  INDIRECT   → exactly one bucket           (a named indirection point — change once,
                                             thousands of flows follow)
  FAST_FAILOVER → first bucket whose watch_port is live
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">FAST_FAILOVER</code> is the one that matters architecturally. Without it, a link failure means: switch detects loss → notifies controller → controller recomputes → controller writes new rules to N switches. That’s tens of milliseconds minimum, and you’re black-holing the whole time. With it, the backup path is <em>pre-installed</em> and the ASIC switches over locally in microseconds when the watch port goes down.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    primary: port 2 (watch_port 2)   ── link up? ──► use it
                                          │ no
    backup:  port 3 (watch_port 3)   ◄────┘
</code></pre></div></div> <p>Same principle, again: precompute in the control plane, execute in the data plane. The controller’s job is to have already thought about the failure, not to think about it when it happens.</p> <p><code class="language-plaintext highlighter-rouge">INDIRECT</code> groups are the underrated one for anyone who’s fought TCAM limits — instead of 50,000 flow entries each naming an output port, you point 50,000 entries at one group, and next-hop changes become a single <code class="language-plaintext highlighter-rouge">GROUP_MOD</code>. It’s a pointer. Networking rediscovers indirection, film at 11.</p> <hr/> <h2 id="the-feedback-loop">The feedback loop</h2> <p>Every flow entry counts packets and bytes. The controller polls with <code class="language-plaintext highlighter-rouge">MULTIPART_REQUEST(FLOW_STATS)</code> and gets back per-entry counters. Add <code class="language-plaintext highlighter-rouge">PORT_STATUS</code> (link up/down), <code class="language-plaintext highlighter-rouge">FLOW_REMOVED</code> (a rule aged out), and you have a telemetry stream.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>        ┌──────────────────────────────────────────────┐
        │                                              │
        ▼                                              │
   ┌──────────┐   FLOW_MOD   ┌────────┐   packets  ┌───┴────┐
   │controller├─────────────►│ switch ├───────────►│ network│
   └──────────┘              └────────┘            └────┬───┘
        ▲                         │  counters, events   │
        └─────────────────────────┴─────────────────────┘
                    observe → decide → program → observe
</code></pre></div></div> <p>This is a control loop in the actual control-theory sense, and it has all the pathologies of one. Poll too fast and you melt the switch CPU (counter reads are CPU work — the ASIC increments them, but somebody has to walk the tables and serialize them into a reply; polling thousands of entries at high frequency is a documented way to make a switch management CPU very unhappy). Poll too slow and you’re steering traffic based on a stale picture, which in the pathological case means you oscillate: you see path A congested, move flows to B, by the time you observe again B is congested and A is empty, and you move them back. Classic.</p> <p>DevoFlow’s answer was to push the measurement down: sampling, triggers, and approximate counters in the data plane, so the switch only tells you about things you’d actually act on. OpenSketch (Yu et al., NSDI 2013) took it further with sketch-based measurement primitives in hardware. Both are instances of the same recurring lesson: <strong>the data plane should summarize, not report.</strong></p> <p>Timeouts close the loop on the resource side. <code class="language-plaintext highlighter-rouge">idle_timeout</code> reclaims entries whose flow went quiet; <code class="language-plaintext highlighter-rouge">hard_timeout</code> reclaims unconditionally. Without them TCAM fills and stays full. With them you get thrash if they’re too short. Picking these is empirical and I don’t think there’s a principled answer — for a learning switch, 30s idle / 300s hard felt fine in my toy and I wouldn’t defend it further than that. <code class="language-plaintext highlighter-rouge">¯\_(ツ)_/¯</code></p> <p>And there’s a genuinely hard correctness problem hiding here that took the community years: <strong>updating rules across many switches is not atomic.</strong> During an update, packets can see the old rules on switch A and the new rules on switch B, which can produce loops or drops in configurations neither the old nor the new policy would ever allow. Reitblatt et al. (<em>Abstractions for Network Update</em>, SIGCOMM 2012) solved this with per-packet consistent updates via version tagging: stamp packets with a version at ingress, have every switch carry rules for both versions, flip the ingress stamp atomically, then garbage-collect. It costs you 2× table space during the transition — which, given everything above about TCAM, is a genuinely painful price. The theory is elegant; the resource bill is the reason people sometimes don’t pay it.</p> <hr/> <h2 id="what-openflow-got-wrong-and-what-came-next">What OpenFlow got wrong, and what came next</h2> <p>OpenFlow’s central bet was: <em>there exists one match-action abstraction that fits all switches.</em> That bet did not fully pay off, and the reason is structural.</p> <p>The field list grew 12 → ~40 and never stopped wanting to grow, because new encapsulations keep appearing (VXLAN, NVGRE, Geneve, SRv6, and now whatever this year’s overlay is). Every one requires a spec revision, an ASIC revision, and a firmware revision. Meanwhile each vendor’s real pipeline had different stage counts, different table widths, different action capabilities — so the switch agent had to <em>compile</em> the requested logical tables onto a physical pipeline that might not fit, and when it didn’t fit you got the worst possible outcome: it “worked,” slowly, in software, and nobody told you. The Table Type Patterns effort was the ONF trying to patch this by letting switches describe their real pipeline, which is a tacit admission that the uniform abstraction had failed.</p> <p>So the field inverted the question. Instead of <em>“here is a fixed pipeline, install rules into it,”</em> <strong>P4</strong> (Bosshart et al., CCR 2014 — note the same author as RMT, this is not a coincidence, it’s a research program) says: <em>“here is the pipeline I want; compile it to the target.”</em></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>   OpenFlow                        P4
   ────────                        ──
   fixed field list                you declare your headers
   fixed pipeline shape            you declare your parser (as a state machine!)
   controller installs entries     you declare your tables and actions
                                   ─────────────────────────────────
                                   compile → ASIC / FPGA / DPDK / eBPF
                                   then P4Runtime installs entries
</code></pre></div></div> <p>The key separation: <strong>pipeline definition at compile time, table population at runtime.</strong> OpenFlow conflated them. Once you split them, the runtime API (P4Runtime) can be generated <em>from</em> your program, so there’s no fixed field list to argue about — the schema is whatever you declared. You want to parse a header nobody has invented yet? Write it in your P4 program. This is the thing that made in-band network telemetry (INT) possible, where switches append their own hop-by-hop latency/queue data into passing packets — an idea that is simply inexpressible in OpenFlow’s vocabulary.</p> <p>The software side went the same direction, from the other end. <strong>Open vSwitch</strong> (Pfaff et al., NSDI 2015) is worth reading closely even if you never touch OVS, because it’s an honest account of what happens when you implement OpenFlow properly in software and discover the abstraction is too slow: they ended up with a two-level cache — a microflow cache keyed on the exact 5-tuple, backed by a “megaflow” cache holding wildcard entries derived from which bits the full lookup <em>actually</em> consulted — sitting in front of a userspace classifier using tuple space search. It’s software rediscovering the TCAM/hash-table split from first principles, by measurement. Then <strong>eBPF/XDP</strong> (Høiland-Jørgensen et al., CoNEXT 2018) went further: run verified bytecode in the kernel at the driver hook, before <code class="language-plaintext highlighter-rouge">sk_buff</code> allocation, and get multi-Mpps-per-core software forwarding. Cilium is that idea pointed at Kubernetes.</p> <p>The arc is consistent, and it’s the same arc as everywhere else in systems:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>fixed function  →  configurable  →  programmable  →  compiled from a high-level language

 fixed ASIC        OpenFlow          P4 / eBPF          P4 + INT + verification
 (2000s)           (2008-2015)       (2014- )           (now)
</code></pre></div></div> <p>Which is more or less what happened to GPUs, to DSPs, to network cards, and to basically every accelerator that survived long enough. Fixed function is fast and inflexible; you make it configurable, then the configuration language becomes complex enough that it wants to be a real language, then you build a compiler, and then the interesting work moves into the compiler. I don’t think networking is special here. I think networking just got there about fifteen years after CPUs did, because the 20-cycle budget made the fixed-function phase last much longer.</p> <hr/> <h2 id="reflections">Reflections</h2> <p>Things that surprised me building this:</p> <p><strong>The abstraction is downstream of the memory technology.</strong> I expected OpenFlow’s design to be driven by networking semantics. It’s mostly driven by TCAM economics. Priorities exist because priority encoders are free. Tables chain forward-only because pipelines don’t loop. The 12-tuple exists because that’s what 2009’s ACL TCAMs held. Once you know the hardware, you can practically re-derive the spec.</p> <p><strong>The hard parts weren’t the parts the spec talks about.</strong> The spec spends pages on message encodings and almost none on the two things that actually broke my implementation: the flow-install race, and the fact that <code class="language-plaintext highlighter-rouge">FLOW_MOD</code> is fire-and-forget. Distributed systems problems, dressed as networking.</p> <p><strong>“Centralized control” oversells it.</strong> Even in a “centralized” SDN, fast failover is local, learning is local, counters are local, and the controller is a replicated distributed system with its own consistency model (ONIX, OSDI 2010, is explicit about offering you a choice between a strongly-consistent transactional store and an eventually-consistent DHT, and telling you to pick per-application). The centralization is <em>logical</em>. Physically you’ve traded one distributed system you understood — routing protocols, thirty years of hardening — for a different one you wrote yourself last quarter. That trade is sometimes right! B4 says it can be very right. But it should be made with open eyes.</p> <p><strong>The security story is genuinely inverted from the pitch.</strong> SDN is marketed as a security win: dynamic policy, IDS-driven quarantine, microsegmentation. All real. But the same interface that lets your IDS install a drop rule in 5 ms is an interface that installs <em>whatever the controller says</em> in 5 ms, and the controller’s inputs include LLDP frames from untrusted hosts and <code class="language-plaintext highlighter-rouge">PACKET_IN</code>s from whoever is sending you traffic. You’ve made the network programmable by the network’s own traffic. Handle accordingly.</p> <hr/> <h2 id="looking-forward">Looking forward</h2> <p>The 20-cycle budget doesn’t improve. Per-port line rates went 10G → 25G → 100G → 400G → 800G while clock frequencies went basically nowhere, so the per-packet cycle budget has been shrinking for two decades and will keep shrinking. Every architectural shift in this post — ASIC offload, pipelining, TCAM, then programmable pipelines, then SmartNICs and DPUs — is a response to that same tightening constraint. I’d bet the next decade’s answers are more of the same shape: push more logic into the data plane, and make the compiler smarter about placing it.</p> <p>The part I find most interesting to speculate about is verification. Once the pipeline is a program, “is my network correct?” becomes a program-analysis question, and there’s already a real body of work treating it that way: Header Space Analysis (Kazemian et al., NSDI 2012) treats packets as points in a 2^L space and forwarding as transforms on it; VeriFlow (Khurshid et al., NSDI 2013) checks invariants in real time as rules are installed; NetKAT (Anderson et al., POPL 2014) gives the whole thing a sound and complete equational theory. That’s a genuinely startling sentence — <em>networks have a Kleene algebra now</em> — and I suspect it’s where the field’s long-term leverage actually is. Not “can I program the network” (yes, solved) but “can I prove the program does what I meant” (mostly not yet, in production).</p> <p>If you want to actually understand this rather than have read about it, build the thing. Take the parser above, add the flow table, wire two of them together over a socket, write a controller that does shortest-path over the topology, then run it under Mininet against real traffic. The bugs you hit — the SYN you dropped, the race you didn’t handle, the table you filled — are the entire curriculum, and they arrive in roughly the order the field encountered them historically, which is a nice bonus.</p> <p>Then go read RMT and try to explain why your <code class="language-plaintext highlighter-rouge">sort(key=-priority)</code> is a priority encoder. Once that clicks, the rest is details.</p> <hr/> <h2 id="references">References</h2> <p><strong>Foundational</strong></p> <ul> <li>N. McKeown et al., <em>OpenFlow: Enabling Innovation in Campus Networks</em>, ACM SIGCOMM CCR 38(2), 2008. — https://dl.acm.org/doi/10.1145/1355734.1355746</li> <li>M. Casado et al., <em>Ethane: Taking Control of the Enterprise</em>, SIGCOMM 2007. — OpenFlow’s direct predecessor.</li> <li>N. Feamster, J. Rexford, E. Zegura, <em>The Road to SDN: An Intellectual History of Programmable Networks</em>, ACM CCR 44(2), 2014. — best single orientation piece.</li> <li>A. Greenberg et al., <em>A Clean Slate 4D Approach to Network Control and Management</em>, CCR 2005.</li> <li>IETF RFC 5810, <em>Forwarding and Control Element Separation (ForCES) Protocol Specification</em>, 2010.</li> </ul> <p><strong>Hardware / data plane</strong></p> <ul> <li>P. Bosshart et al., <em>Forwarding Metamorphosis: Fast Programmable Match-Action Processing in Hardware for SDN</em>, SIGCOMM 2013. — the RMT architecture. Read this one.</li> <li>P. Bosshart et al., <em>P4: Programming Protocol-Independent Packet Processors</em>, ACM CCR 44(3), 2014. — https://p4.org/</li> <li>B. Agrawal, T. Sherwood, <em>Modeling TCAM Power for Next Generation Network Devices</em>, ISPASS 2006/2008.</li> <li>B. Pfaff et al., <em>The Design and Implementation of Open vSwitch</em>, NSDI 2015. — https://www.usenix.org/conference/nsdi15/technical-sessions/presentation/pfaff</li> <li>T. Høiland-Jørgensen et al., <em>The eXpress Data Path: Fast Programmable Packet Processing in the Operating System Kernel</em>, CoNEXT 2018.</li> </ul> <p><strong>Scale, control, production</strong></p> <ul> <li>T. Koponen et al., <em>Onix: A Distributed Control Platform for Large-scale Production Networks</em>, OSDI 2010.</li> <li>S. Jain et al., <em>B4: Experience with a Globally-Deployed Software Defined WAN</em>, SIGCOMM 2013.</li> <li>A.R. Curtis et al., <em>DevoFlow: Scaling Flow Management for High-Performance Networks</em>, SIGCOMM 2011. — the “controller is not free” paper.</li> <li>M. Yu, L. Jose, R. Miao, <em>Software Defined Traffic Measurement with OpenSketch</em>, NSDI 2013.</li> </ul> <p><strong>Correctness &amp; verification</strong></p> <ul> <li>M. Reitblatt et al., <em>Abstractions for Network Update</em>, SIGCOMM 2012.</li> <li>P. Kazemian, G. Varghese, N. McKeown, <em>Header Space Analysis: Static Checking for Networks</em>, NSDI 2012.</li> <li>A. Khurshid et al., <em>VeriFlow: Verifying Network-Wide Invariants in Real Time</em>, NSDI 2013.</li> <li>C.J. Anderson et al., <em>NetKAT: Semantic Foundations for Networks</em>, POPL 2014.</li> </ul> <p><strong>Security</strong></p> <ul> <li>S. Shin et al., <em>AVANT-GUARD: Scalable and Vigilant Switch Flow Management in Software-Defined Networks</em>, CCS 2013.</li> <li>S. Hong et al., <em>Poisoning Network Visibility in Software-Defined Networks: New Attacks and Countermeasures</em> (TopoGuard), NDSS 2015.</li> <li>T. Ptacek, T. Newsham, <em>Insertion, Evasion, and Denial of Service: Eluding Network Intrusion Detection</em>, 1998. — for the parser-ambiguity section.</li> <li>S. Scott-Hayward, S. Natarajan, S. Sezer, <em>A Survey of Security in Software Defined Networks</em>, IEEE COMST, 2016.</li> </ul> <p><strong>Specs &amp; diagrams</strong></p> <ul> <li>ONF, <em>OpenFlow Switch Specification v1.3.5 and v1.5.1</em> — the pipeline and flow-entry figures (Fig. 2–3 in most versions) are the canonical diagrams. https://opennetworking.org/software-defined-standards/specifications/</li> <li>ONF, <em>SDN Architecture</em> (TR-521) — the application/control/infrastructure layer diagram everyone redraws.</li> <li>Open vSwitch architecture diagram — https://docs.openvswitch.org/en/latest/topics/design/</li> <li>P4 PISA / v1model architecture figures — https://p4.org/specs/ and the p4lang/tutorials repo.</li> <li>Cisco/Broadcom public switch-architecture diagrams for the PHY→MAC→pipeline→traffic-manager block layout (vendor docs; useful as a reality check against the idealized spec diagrams).</li> </ul> <p><em>One honesty note on the references: I wrote these from memory of the literature rather than re-fetching each one, so titles/venues/years are right to the best of my knowledge but worth a quick verify before you cite them in anything load-bearing. The three I’d stake the most on are RMT, DevoFlow, and the OVS paper — those are the ones where the specific technical claims in this post came from.</em></p>]]></content><author><name></name></author><category term="networking"/><category term="networking"/><category term="sdn"/><summary type="html"><![CDATA[Building a toy OpenFlow switch and controller in ~600 lines of Python, then working bottom-up through PHY, parser, TCAM, pipeline, and actions to show why the protocol's design is downstream of memory technology and a 20-cycle-per-packet budget.]]></summary></entry><entry><title type="html">IPv8 and the Internet’s Eternal Transition Problem</title><link href="https://karankessy.github.io/blog/2026/ipv8-new-internet-fuss/" rel="alternate" type="text/html" title="IPv8 and the Internet’s Eternal Transition Problem"/><published>2026-04-28T00:00:00+00:00</published><updated>2026-04-28T00:00:00+00:00</updated><id>https://karankessy.github.io/blog/2026/ipv8-new-internet-fuss</id><content type="html" xml:base="https://karankessy.github.io/blog/2026/ipv8-new-internet-fuss/"><![CDATA[<p>i found IPv8 landing in my feed last week. an internet-draft, april 2026, proposing a full replacement of the IP stack.</p> <p>my first reaction was confusion. not the technical kind but more of a philosophical kind. <em>why are we here again?</em></p> <p>we already did this. IPv4 ran out of addresses. IPv6 was the answer. twenty-five years of standards work, deployment effort, vendor pressure, and IPv6 still carries a minority of global traffic. the same old IPv4 protocol survived on borrowed time, held together by NAT, by CGNAT, by workarounds stacked on workarounds.</p> <p>like renovating a collapsing house by continuously adding extension rooms instead of rebuilding the foundation.</p> <p>and now someone is proposing IPv8.</p> <p>so i happened to read it.</p> <p>and this is what i’m understanding.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/ipv8-480.webp 480w,/assets/img/ipv8-800.webp 800w,/assets/img/ipv8-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/ipv8.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" title="IPv8 Architecture Overview" loading="eager" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <h2 id="compatibility-argument"><strong>compatibility argument</strong></h2> <p>the first thing the draft tells us is that IPv4 is a proper subset of IPv8.</p> <p>an IPv8 address with its routing prefix set to zero <em>is</em> an IPv4 address.</p> <p>no device needs to change.<br/> no application.<br/> no network.</p> <p>100% backward compatible.</p> <p>that’s a strong claim.</p> <p>and at the addressing layer, it’s actually clever.</p> <p>the “zero trick” seems to work structurally.</p> <p>an IPv4 packet fits inside IPv8 without any bit-level conflict.</p> <p>something roughly like this:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>          IPv8 Address Layout
| routing prefix | IPv4 Address            |
|----------------|-------------------------|
| 00000000       | 192.168.1.10            |

=&gt; if prefix = 0
=&gt; treat remaining bits as normal IPv4
</code></pre></div></div> <p>or in more literal bit terms:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>IPv4
11000000 10101000 00000001 00001010
192      168      1        10

IPv8-compatible representation
00000000 | 11000000 10101000 00000001 00001010
zero-prefix           original IPv4 bits
</code></pre></div></div> <p>so structurally:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>IPv4 ⊂ (proper subset) to IPv8
</code></pre></div></div> <p>at least that’s seems to be the core idea.</p> <p>and we can imagine of it like designing a new shipping container that can still perfectly carry the old boxes without reshaping them.</p> <p>but compatibility at the address format layer isn’t the same as compatibility at the forwarding layer.</p> <p>a router that doesn’t understand:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>IP Version = 8
</code></pre></div></div> <p>inside the packet header will simply drop the packet before even caring about the address structure.</p> <p>and that is the important distinction or lets say the bigger problem.</p> <p>the draft has a tunnelling mechanism for this — 8to4, which wraps IPv8 across IPv4-only transit.</p> <p>visually:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[ IPv8 Packet ]
        ↓ encapsulated into
[ IPv4 Packet ]
        ↓ travels through legacy internet
[ unpacked back into IPv8 ]
</code></pre></div></div> <p>but that’s not backward compatibility.</p> <p>that’s transition engineering.</p> <p>and honestly, we’ve seen this movie before:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>IPv6 → 6to4
IPv8 → 8to4
</code></pre></div></div> <p>same idea.<br/> different number.</p> <p>it’s a little like saying:</p> <p>“the new train system works perfectly…<br/> as long as we keep carrying the trains on top of the old roads first.”</p> <p>we’re not escaping the transition problem.</p> <p>we’re just renaming it.</p> <p>short version:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>address compatibility ≠ infrastructure compatibility
</code></pre></div></div> <h2 id="and-the-zone-server-which-seems-to-be-the-real-bet"><strong>and the zone server, which seems to be the real bet</strong></h2> <p>this is where the draft gets genuinely interesting.</p> <p>and also genuinely risky.</p> <p>IPv8 doesn’t seem to be just making change on addressing.</p> <p>it somehow collapses your entire network management stack into a single platform called the zone server.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Current Internet Reality

DHCP  → separate server
DNS   → separate server
NTP   → separate server
Auth  → separate server
Policy → separate server
Monitoring → separate server
</code></pre></div></div> <p>and the holy-moly IPv8 tries to compress this into:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>                +----------------+
device joins →  |   Zone Server  |
                +----------------+
                  |  IP address
                  |  DNS config
                  |  time sync
                  |  auth token
                  |  route policy
                  |  firmware info
</code></pre></div></div> <p>all from one discovery exchange.</p> <p>plug device in.</p> <p>device asks:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>"who am i?"
</code></pre></div></div> <p>zone server replies:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>"here's your address,
identity,
routes,
permissions,
time sync,
and policy."
</code></pre></div></div> <p>done.</p> <p>the problem this is solving is real.</p> <p>right now those services are separate products, separately licensed, separately maintained, with no shared awareness of network state.</p> <p>a device connecting to a network might need a dozen independent configurations before it can do anything useful.</p> <p>that fragmentation is genuinely painful.</p> <p>especially at smaller scale where operational complexity becomes the real enemy.</p> <p>but collapsing everything into one platform doesn’t eliminate the blast radius.</p> <p>it concentrates it.</p> <p>because now failure looks like this:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Zone Server Failure
        ↓
DNS failure
Auth failure
Time sync failure
Routing validation failure
Access control failure
</code></pre></div></div> <p>single dependency and multi-system collapse.</p> <p>and to tackle this, the draft happens to mention about active/active redundancy, which is obvious.</p> <p>but redundancy here is deployment architecture. not protocol magic.</p> <p>and the actual deeper architectural shift is this:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>traditional internet stack

Layer 3 → packet delivery
Layer 7 → identity/authentication
</code></pre></div></div> <p>and IPv8 intentionally blurs those layers.</p> <p>it somehow amazingly performs identity-aware checks at forwarding time itself, wooho! this is magic now.</p> <p>something like:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>packet arrives
     ↓
validate DNS linkage
     ↓
validate WHOIS8 route ownership
     ↓
allow forwarding
</code></pre></div></div> <p>routers stop behaving like dumb packet movers.</p> <p>they become identity-aware infrastructure.</p> <p>that’s not a tiny adjustment.</p> <p>that’s a mere philosophical redesign of networking itself.</p> <h2 id="routing-changes--actually-the-most-defensible-part"><strong>routing changes — actually the most defensible part</strong></h2> <p>the cost factor metric is probably the strongest idea in this draft.</p> <p>CF is a 32-bit routing quality score derived from seven components:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>CF =
latency +
packet loss +
congestion +
session stability +
link capacity +
economic policy +
geographic physics floor
</code></pre></div></div> <p>that last part i.e. geographic physics floor is the fascinating one.</p> <p>the physics floor.</p> <p>no route can appear faster than physics itself permits. pure science, not sure how literally will this be applied.</p> <p>roughly:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>minimum latency ≥ speed-of-light distance
</code></pre></div></div> <p>example:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Kathmandu → London

distance: ~7,400 km

light itself needs a minimum amount of time
to physically travel that distance.

if telemetry suddenly reports:
"1 ms latency"

something is wrong.
</code></pre></div></div> <p>either:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>bad measurement
spoofed metrics
fake routing advertisements
broken telemetry
</code></pre></div></div> <p>the route becomes suspicious automatically.</p> <p>that’s elegant engineering.</p> <p>because physics (geographic physics floor, one of the seven CF metric) becomes the validator.</p> <p>and then there’s ASN aggregation.</p> <p>today BGP routing tables exploded because networks continuously advertise more and more prefixes.</p> <p>simplified:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Current BGP

ASN 64500
 ├── 203.0.113.0/24
 ├── 203.0.114.0/24
 ├── 203.0.115.0/24
 ├── ...
</code></pre></div></div> <p>IPv8 tries to structurally limit this.</p> <p>something closer to:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>One ASN
→ One major routing identity
→ One bounded table entry
</code></pre></div></div> <p>which means:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>smaller routing tables
less memory pressure
less churn
more predictable scaling
</code></pre></div></div> <p>at least theoretically.</p> <p>and honestly, one of the surprising things while reading the draft is how much of networking today feels like accumulated exception handling.</p> <h2 id="this-is-whats-missing"><strong>this is what’s missing</strong></h2> <p>the OAuth2 JWT requirement appears everywhere in the draft.</p> <p>tokens for every device.<br/> every packet.<br/> every connection. and <strong>local validation caches</strong>, which is actually sensible for availability and lowest possible latency.</p> <p>because otherwise:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>packet arrives
     ↓
contact external identity provider
     ↓
wait for validation
     ↓
forward packet
</code></pre></div></div> <p>would destroy latency.</p> <p>so IPv8 caches trust locally.</p> <p>which is smart.</p> <p>until revocation enters the conversation.</p> <p>because JWT systems always hit the same wall again and again:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>validation is easy
but the revocation is hard
</code></pre></div></div> <p>example:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>device compromised at 10:01

token expires at 11:00

network still trusts it for 59 more minutes
unless revocation propagates everywhere instantly
</code></pre></div></div> <p>the draft barely discusses this.</p> <p>and that’s not a side issue.</p> <p>that’s central security architecture.</p> <p>and also the important thing to keep in mind:</p> <p>this is not one finished protocol. it’s an ecosystem proposal.</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>IPv8 Core
WHOIS8
OAuth8
WiFi8
Zone Server
Routing Specs
Firmware Distribution
</code></pre></div></div> <p>multiple companion drafts.</p> <p>multiple moving pieces.</p> <p>each needing independent IETF review.</p> <p>which means realistically:</p> <p>this is not near deployment. this is early-stage architectural provocation.</p> <h2 id="the-question-the-draft-doesnt-answer"><strong>the question the draft doesn’t answer</strong></h2> <p>IPv6 didn’t fail because of technical flaws.</p> <p>it failed because the internet found ways to postpone pain.</p> <p>NAT bought it more time to exist.</p> <p>CGNAT bought even more time.</p> <p>and once businesses can postpone infrastructure pain, they usually do.</p> <p>for decades.</p> <p>the draft argues IPv8 avoids this because IPv4 is embedded directly through the zero-prefix model.</p> <p>but economically, the transition problem still exists.</p> <p>someone still has to upgrade:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>routers
switch firmware
management systems
security tooling
observability stacks
</code></pre></div></div> <p>or some high level firmware or software update has to support this new version of IPv8 protocol and all the vendors have to abide by.</p> <p>and the draft’s main incentive mechanism seems to be:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>8to4 paths
→ slightly worse latency
→ pressure to upgrade
</code></pre></div></div> <p>but against trillion-dollar infrastructure inertia?</p> <p>that’s weak.</p> <p>networks don’t migrate because architecture diagrams look elegant.</p> <p>they migrate because staying old becomes unbearable.</p> <p>financially.<br/> operationally.<br/> politically.</p> <p>what is the forcing function for this protocol?</p> <p>the draft never fully answers that.</p> <p>however, IPv8 is worth reading.</p> <p>the routing table bound.<br/> the physics floor.<br/> the critique of management fragmentation.</p> <p>these are somehow the real problems.</p> <p>and some proposed ideas are genuinely thoughtful.</p> <p>but as a standards proposal, this is still very early.</p> <p>the compatibility claims seems to be overstated.<br/> the zone server failure modes are underexamined.<br/> the layering violations are asserted rather than justified.<br/> and the revocation model for JWTs are still blurry.</p> <p>and honestly, i’m just assuming things.</p> <p>read it more like a provocation than a roadmap.</p> <p>something asking:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>what if the internet stack itself
has accumulated too many patches
to keep pretending it's coherent?
</code></pre></div></div> <p>even if the answers aren’t fully there yet.</p> <p>that’s still more than most drafts do.</p>]]></content><author><name></name></author><category term="networking"/><category term="networking"/><category term="distributed-systems"/><summary type="html"><![CDATA[A reflective breakdown of the IPv8 draft proposal — compatibility claims, zero-prefix IPv4 embedding, ASN routing limits, physics-aware routing metrics, and the risks of collapsing internet infrastructure into a centralized zone server model]]></summary></entry><entry><title type="html">Semantic Map Explorer - Entropy Embedding Atlas</title><link href="https://karankessy.github.io/blog/2026/semantic-mapping-and-entropy/" rel="alternate" type="text/html" title="Semantic Map Explorer - Entropy Embedding Atlas"/><published>2026-04-18T00:00:00+00:00</published><updated>2026-04-18T00:00:00+00:00</updated><id>https://karankessy.github.io/blog/2026/semantic-mapping-and-entropy</id><content type="html" xml:base="https://karankessy.github.io/blog/2026/semantic-mapping-and-entropy/"><![CDATA[<h2 id="project-vector-atlas">Project: Vector Atlas</h2> <p><strong>Repository:</strong> <a href="https://github.com/karankessy/vectorAtlas">karankessy/vectorAtlas</a></p> <p>This article walks through the Vector Atlas project—an interactive tool for exploring text geometry through semantic embeddings and entropy analysis.</p> <h2 id="overview">Overview</h2> <p>This project is to understand &amp; read it as a small article about text geometry. We have it starting with raw sentences, turn them into vectors, connect nearby sentences into a graph, measure how mixed each neighborhood is, compresse the result into 2D, and then explore the whole thing in a browser.</p> <p>I am not trying to present this as a perfect or final answer to text analysis. It is more modest than that. The point is to build a clear mental model for how meaning can be represented, compared, and inspected without pretending the map is the territory.</p> <p>If you are new to embeddings, cosine similarity, KNN graphs, entropy, UMAP, or t-SNE as me, the goal here is to explain each part gently, then connect it back to the bigger picture, through what I’ve come to understand so far.</p> <h2 id="opening-intuition">Opening intuition</h2> <p>Text feels messy because it is not naturally numeric. Computers do not know that “the database connection pool is exhausted” is closer to “the server returned a 502 bad gateway error” than to “my cat knocked over the coffee mug again” unless we give them a way to compare meaning.</p> <p>This project is built around that gap.</p> <p>The basic idea is simple:</p> <ol> <li>Take a sentence.</li> <li>Turn it into a vector that captures semantic meaning.</li> <li>Find nearby sentences in that vector space.</li> <li>Ask how concentrated or mixed each local neighborhood is.</li> <li>Project the whole structure into 2D so a person can inspect it.</li> </ol> <p>That gives us a map, but not a geographical one. It is a semantic map: points that are close together tend to be similar in meaning, while points near the edges often show ambiguity, overlap, or cross-topic language.</p> <h2 id="project-overview">Project overview</h2> <p>The pipeline is:</p> <p>text dataset -&gt; embeddings -&gt; curated KNN graph -&gt; entropy -&gt; 2D reduction -&gt; visualization</p> <p>The dataset is a small mix of technical logs, security-related sentences, everyday conversation, ambiguous phrases, programming talk, payload-like strings, and philosophical statements. The variety is intentional. A map is more interesting when not everything belongs to the same semantic neighborhood.</p> <p>The next step is embedding: each sentence becomes a dense vector in 384 dimensions. Once text is in vector form, we can compare directions and distances instead of comparing raw strings.</p> <p>From there, the project builds a curated nearest-neighbor graph. That graph is the local structure of the map. It says which points are close enough to matter, and it is curated so that the graph is not overwhelmed by noisy or weak relationships.</p> <p>Entropy is then computed over each sentence’s neighborhood. Here, entropy is not used as a judgment of truth or quality. It is a structural measure: how concentrated is the neighborhood around this point? A sentence sitting cleanly inside one semantic cluster tends to have lower entropy; a sentence that lives near boundaries or mixes several ideas tends to have higher entropy.</p> <p>Finally, UMAP or t-SNE reduces the 384-dimensional space to 2D. That 2D layout is only for inspection. The real neighborhood logic still lives in the high-dimensional embedding space. The HTML output then turns the result into something you can pan, hover, and compare interactively.</p> <h2 id="repository-and-architecture-walkthrough">Repository and architecture walkthrough</h2> <p>The repository is organized so each file handles one part of the pipeline.</p> <ul> <li><a href="https://github.com/karankessy/vectorAtlas/blob/main/data.py">data.py</a> holds the sentence dataset and labels.</li> <li><a href="https://github.com/karankessy/vectorAtlas/blob/main/embeddings.py">embeddings.py</a> turns text into vectors using a sentence-transformer model.</li> <li><a href="https://github.com/karankessy/vectorAtlas/blob/main/similarity.py">similarity.py</a> builds the curated KNN graph.</li> <li><a href="https://github.com/karankessy/vectorAtlas/blob/main/entropy.py">entropy.py</a> computes neighborhood entropy and summary statistics.</li> <li><a href="https://github.com/karankessy/vectorAtlas/blob/main/reduce.py">reduce.py</a> performs UMAP or t-SNE projection into 2D.</li> <li><a href="https://github.com/karankessy/vectorAtlas/blob/main/visualize.py">visualize.py</a> builds the Plotly figures.</li> <li><a href="https://github.com/karankessy/vectorAtlas/blob/main/main.py">main.py</a> orchestrates the full pipeline.</li> <li><a href="https://github.com/karankessy/vectorAtlas/blob/main/run_easy.py">run_easy.py</a> wraps the pipeline in friendly presets.</li> </ul> <p>That split is not accidental. It keeps the engineering readable.</p> <p>The system has one clear top-to-bottom flow, but each stage answers a different question:</p> <ul> <li>What does the sentence mean numerically?</li> <li>Who is near it?</li> <li>How mixed is its local neighborhood?</li> <li>How do we show that geometry in a form a human can inspect?</li> </ul> <p>This is a useful pattern in small analytical projects: separate the semantic work, the graph work, the scoring work, and the visualization work. That way, each piece can be explained, tuned, or replaced without rewriting the whole project.</p> <h3 id="why-each-module-exists">Why each module exists</h3> <p><a href="https://github.com/karankessy/vectorAtlas/blob/main/data.py">data.py</a> exists so the example is stable and reproducible. The dataset is small on purpose, because the goal is to understand the geometry rather than chase a massive benchmark.</p> <p><a href="https://github.com/karankessy/vectorAtlas/blob/main/embeddings.py">embeddings.py</a> exists because raw text is not directly comparable in a useful geometric way. Embeddings are the bridge from language to vector space.</p> <p><a href="https://github.com/karankessy/vectorAtlas/blob/main/similarity.py">similarity.py</a> exists because raw nearest neighbors are often too noisy if used uncritically. The graph is curated so the local structure is more meaningful.</p> <p><a href="https://github.com/karankessy/vectorAtlas/blob/main/entropy.py">entropy.py</a> exists because not all neighborhoods are equally focused. Some points sit in clear semantic pockets; others sit on boundary regions. Entropy helps measure that difference.</p> <p><a href="https://github.com/karankessy/vectorAtlas/blob/main/reduce.py">reduce.py</a> exists because 384 dimensions are too many to draw directly. A projection gives us a readable layout.</p> <p><a href="https://github.com/karankessy/vectorAtlas/blob/main/visualize.py">visualize.py</a> exists because the end result should be inspectable, not just computed. The interactive map is where the project becomes useful to a person.</p> <p><a href="https://github.com/karankessy/vectorAtlas/blob/main/main.py">main.py</a> exists because the whole workflow should run from one command and be easy to tune.</p> <p><a href="https://github.com/karankessy/vectorAtlas/blob/main/run_easy.py">run_easy.py</a> exists because a starter should not have to remember every flag before seeing something meaningful.</p> <h2 id="dataset-section">Dataset section</h2> <p>The dataset mixes several kinds of sentences:</p> <ul> <li>technical logs and infrastructure messages,</li> <li>security alerts and payload-like strings,</li> <li>everyday conversational sentences,</li> <li>ambiguous phrases such as “this looks suspicious” or “request timed out”,</li> <li>programming and developer talk,</li> <li>and a small set of philosophical statements.</li> </ul> <p>The point of mixing them is not to make the data chaotic for its own sake. It is to make the map informative.</p> <p>If every sentence belonged to one style, the project would not tell us much. But when technical, ambiguous, benign, and security-related text all appear together, the map can show several useful things:</p> <ul> <li>where semantic clusters separate cleanly,</li> <li>where one category overlaps another,</li> <li>where ambiguous language sits near multiple groups,</li> <li>and how suspicious-looking strings behave compared with normal operational text.</li> </ul> <p>The categories in <a href="https://github.com/karankessy/vectorAtlas/blob/main/data.py">data.py</a> are intentionally mixed because the project is trying to study structure, not just labels.</p> <h2 id="embeddings-section">Embeddings section</h2> <p>Embeddings are the part that make the rest of the project possible.</p> <p>A sentence embedding is a numeric vector that tries to represent meaning. Instead of comparing raw text character by character, we compare vectors in a space where similar meanings should point in similar directions.</p> <p>This matters because raw text is a poor geometry object. Two sentences can be semantically close while looking very different on the surface. For example, “the server returned a 502 bad gateway error” and “the API rate limiter kicked in after 1000 requests” are both technical operational messages, even though the words are not interchangeable.</p> <p>The project uses <a href="https://github.com/karankessy/vectorAtlas/blob/main/embeddings.py">sentence-transformers</a> with the model <code class="language-plaintext highlighter-rouge">all-MiniLM-L6-v2</code>.</p> <p>That model produces 384-dimensional vectors, so each sentence becomes a point in:</p> \[E \in \mathbb{R}^{N \times 384}\] <p>For this dataset, $N = 170$, so the embedding matrix is:</p> \[E \in \mathbb{R}^{170 \times 384}\] <p>What does “384 dimensions” mean in practice?</p> <p>It means the sentence is represented by 384 numeric features that together describe semantic position in the model’s learned space. The dimensions themselves are not individually human-interpretable. You do not read them one by one like columns in a spreadsheet. Instead, you read the geometry they form together.</p> <p>The main intuition is this: sentences with similar meanings should end up near each other in embedding space, even if the wording differs. That is the foundation the rest of the pipeline uses.</p> <h2 id="similarity-and-graph-section">Similarity and graph section</h2> <p>Once sentences are vectors, we need a way to compare them.</p> <p>The project uses cosine similarity because it cares more about direction than raw magnitude. In text embeddings, that often works better than naïvely using Euclidean distance on the raw vector values.</p> <h3 id="cosine-similarity-in-plain-language">Cosine similarity in plain language</h3> <p>If two vectors point in nearly the same direction, they are likely semantically close. If they point in different directions, they are less similar.</p> <h3 id="cosine-similarity-formula">Cosine similarity formula</h3> <p>For vectors $u$ and $v$:</p> \[\text{sim}(u, v) = \frac{u \cdot v}{\|u\|\,\|v\|}\] \[d(u, v) = 1 - \text{sim}(u, v)\] <p>What the symbols mean:</p> <ul> <li>$u, v$ are sentence vectors.</li> <li>$u \cdot v$ is the dot product.</li> <li>$|u|$ and $|v|$ are vector lengths.</li> <li>$\text{sim}(u, v)$ is the cosine similarity.</li> <li>$d(u, v)$ is the cosine distance.</li> </ul> <p>Why this formula matters:</p> <ul> <li>the dot product grows when vectors point in similar directions,</li> <li>the lengths normalize away raw scale,</li> <li>and the result stays focused on angular closeness instead of absolute size.</li> </ul> <p>That is useful here because embeddings often care more about orientation in space than their raw magnitude.</p> <h3 id="nearest-neighbor-search-and-knn-graphs">Nearest-neighbor search and KNN graphs</h3> <p>Once we can measure similarity, we can ask a practical question: who is near whom?</p> <p>The project uses nearest-neighbor search to find the closest sentences to each sentence in the dataset. From that, it builds a KNN graph, where each node is a sentence and each edge connects it to a small set of nearby sentences.</p> <p>The graph is not the final answer. It is the local structure that the entropy calculation later inspects.</p> <h3 id="why-the-graph-is-curated">Why the graph is curated</h3> <p>Raw nearest-neighbor lists are often too literal. They can include weak or accidental matches. That is fine if you are doing recall-heavy retrieval, but not ideal if you want a readable semantic map.</p> <p>So <a href="https://github.com/karankessy/vectorAtlas/blob/main/similarity.py">similarity.py</a> curates the neighbors using three ideas:</p> <ul> <li>a similarity threshold,</li> <li>a mutual-neighbor requirement,</li> <li>and a reranking bonus based on shared neighborhood overlap.</li> </ul> <p>The reranking score is:</p> \[\text{score}(i, j) = \text{sim}(i, j) + \alpha \cdot \frac{|\mathcal{N}_i \cap \mathcal{N}_j|}{\text{shortlist}_k}\] <p>Here:</p> <ul> <li>$i$ is the current sentence,</li> <li>$j$ is a candidate neighbor,</li> <li>$\mathcal{N}_i$ is the candidate neighbor set for sentence $i$,</li> <li> <table> <tbody> <tr> <td>$</td> <td>\mathcal{N}_i \cap \mathcal{N}_j</td> <td>$ counts how many shortlist neighbors they share,</td> </tr> </tbody> </table> </li> <li>$\text{shortlist}_k$ is a normalization factor,</li> <li>and $\alpha$ controls how strongly shared context affects ranking.</li> </ul> <p>What each parameter changes in practice:</p> <ul> <li><code class="language-plaintext highlighter-rouge">--min-sim</code> makes the graph stricter when increased.</li> <li><code class="language-plaintext highlighter-rouge">--mutual-knn</code> removes one-way or less stable neighbor links when enabled.</li> <li><code class="language-plaintext highlighter-rouge">--shortlist-factor</code> controls how broad the candidate pool is before curation.</li> <li><code class="language-plaintext highlighter-rouge">--edge-neighbors</code> controls how many edges are drawn in the visualization.</li> </ul> <p>In plain terms, the graph curation step tries to keep edges that feel locally justified instead of simply numerically close.</p> <h2 id="entropy-section">Entropy section</h2> <p>Entropy is the part of the project that tells us how concentrated or spread out a neighborhood is.</p> <p>That may sound abstract at first, so it helps to think of a point in the graph as having a few neighbors that may matter more than others. If one neighbor is clearly dominant, the neighborhood is sharp and focused. If the neighbor influence is spread more evenly, the neighborhood is mixed.</p> <h3 id="from-distances-to-probabilities">From distances to probabilities</h3> <p>The project starts with the curated neighbor distances. It turns them into similarities:</p> \[s_j = 1 - d_j\] <p>Then it applies a temperature transform:</p> \[w_j = s_j^{1/T}\] <p>And finally normalizes the values into a probability distribution:</p> \[p_j = \frac{w_j}{\sum_{m=1}^{k} w_m}\] <p>What each symbol does:</p> <ul> <li>$d_j$ is the distance to the $j$-th neighbor.</li> <li>$s_j$ is the corresponding similarity-like value.</li> <li>$T$ is the temperature.</li> <li>$w_j$ is the temperature-adjusted weight.</li> <li>$p_j$ is the normalized probability assigned to that neighbor.</li> </ul> <h3 id="the-entropy-formula">The entropy formula</h3> <p>Once we have probabilities, entropy is computed as:</p> \[H = -\sum_{j=1}^{k} p_j \log(p_j)\] <p>The project then normalizes it:</p> \[H_{\text{norm}} = \frac{H}{\log(k)}\] <p>Why the normalization matters:</p> <ul> <li>it makes the score easier to compare across runs,</li> <li>and it keeps the values in a more interpretable range.</li> </ul> <h3 id="how-to-read-entropy-here">How to read entropy here</h3> <ul> <li>Low entropy usually means the sentence sits in a focused local neighborhood.</li> <li>High entropy usually means the sentence is near a boundary, mixed across categories, or semantically less settled.</li> </ul> <p>That is not a moral score. It does not mean a sentence is “good” or “bad,” only that its local structure is more or less concentrated.</p> <p>Temperature is the main knob here. Lower temperatures sharpen the neighbor weights and usually make the neighborhood look more decisive. Higher temperatures smooth the weights and can make the distribution flatter.</p> <h2 id="dimensionality-reduction-section">Dimensionality reduction section</h2> <p>The embedding space has 384 dimensions. Humans cannot inspect that directly, so the project projects the points down to 2D.</p> <p>This reduction is not the same thing as the semantic graph itself. The graph is built in the embedding space; the 2D projection is only the display layer.</p> <h3 id="umap">UMAP</h3> <p>UMAP is used as the default because it often preserves local topology well and gives a readable layout for this kind of data. It is a good choice when the main concern is neighborhood structure.</p> <h3 id="t-sne">t-SNE</h3> <p><code class="language-plaintext highlighter-rouge">t-SNE</code> is another projection method. It often makes clusters look more separated, which can be helpful for inspection. The tradeoff is that global geometry can be distorted more easily.</p> <h3 id="what-the-projection-means-and-what-it-does-not-mean">What the projection means and what it does not mean</h3> <p>The 2D map should be read as a visualization of neighborhood relationships, not as the source of truth itself.</p> <p>The important relationships were computed before projection. The projection simply gives them a form we can inspect.</p> <p>That means two points being near each other on the 2D map is informative, but it is not the same as saying the 2D map fully preserves all high-dimensional distances.</p> <h2 id="visualization-section">Visualization section</h2> <p><a href="https://github.com/karankessy/vectorAtlas/blob/main/visualize.py">visualize.py</a> builds two interactive maps with Plotly.</p> <h3 id="entropy-map">Entropy map</h3> <p>The entropy map colors points by entropy. It also draws faint edges for the curated KNN graph.</p> <p>What to look for:</p> <ul> <li>low-entropy points often sit inside more coherent clusters,</li> <li>high-entropy points often sit on boundaries or mix several themes,</li> <li>and the edges show which points were considered local neighbors.</li> </ul> <p>The colorbar represents entropy, so you can visually compare which regions are more focused and which are more mixed.</p> <h3 id="category-map">Category map</h3> <p>The category map uses the same 2D coordinates but colors points by label instead of entropy.</p> <p>This is useful because it shows whether the known categories separate cleanly or overlap in the embedding space.</p> <p>The hover labels reveal the sentence, the category, and the entropy value. That makes the map interactive in a way a static image is not. You can move across points and inspect local examples instead of guessing from a legend.</p> <p>Here are the two visualizations in action:</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/coloredByCategory-480.webp 480w,/assets/img/coloredByCategory-800.webp 800w,/assets/img/coloredByCategory-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/coloredByCategory.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <p>The category map shows how different semantic categories (tech, security, casual, ambiguous, etc.) distribute across the embedding space. Clean separation suggests the model is distinguishing them well; overlap can indicate genuine semantic similarity.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/entropyEmbeddingAtlas-480.webp 480w,/assets/img/entropyEmbeddingAtlas-800.webp 800w,/assets/img/entropyEmbeddingAtlas-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/entropyEmbeddingAtlas.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <p>The entropy map colors the same points by neighborhood entropy. Notice how lower-entropy regions (lighter colors) tend to cluster together, while higher-entropy points (darker colors) often sit at boundaries or transition zones between semantic clusters.</p> <h3 id="why-interactive-html-helps">Why interactive HTML helps</h3> <p>Interactive HTML is practical here because the interesting part of the project is local comparison.</p> <p>In a static image, you can see the whole field at once, but you cannot easily inspect the sentence text behind each point. With Plotly, you can hover, toggle categories, and inspect clusters and boundary points more carefully.</p> <h2 id="code-and-engineering-section">Code and engineering section</h2> <p>The main flow in <a href="https://github.com/karankessy/vectorAtlas/blob/main/main.py">main.py</a> is straightforward:</p> <ol> <li>Load sentences and labels.</li> <li>Optionally append a query sentence.</li> <li>Generate embeddings.</li> <li>Build the curated graph.</li> <li>Compute entropy.</li> <li>Reduce to 2D.</li> <li>Render the visualizations.</li> <li>Save the HTML files and print summaries.</li> </ol> <p>That order matters because each step depends on the previous one. The code is organized so the pipeline stays easy to inspect, and so each stage can be tuned separately.</p> <p>The curated KNN step improves readability and signal because it keeps the graph from being flooded with weak relationships. This is one of the main reasons the map stays interpretable instead of turning into a dense web of nearly everything connected to everything else.</p> <p>There is a tradeoff between strict curation and exploratory looseness:</p> <ul> <li>stricter settings give a cleaner graph with fewer noisy links,</li> <li>looser settings keep more possible relationships and can reveal broader structure,</li> <li>but too much looseness can make the map harder to interpret.</li> </ul> <p><a href="https://github.com/karankessy/vectorAtlas/blob/main/visualize.py">visualize.py</a> also pays attention to stability. The axes are fixed, the layout is kept readable, and the category map is set up so legend toggling does not cause the plot to jump around. That matters because a visualization should help you inspect the data, not distract you by reshuffling itself every time you click a category.</p> <h2 id="math-section">Math section</h2> <p>The main formulas are worth reading together, because they are connected.</p> <h3 id="1-cosine-similarity">1) Cosine similarity</h3> \[\text{sim}(u, v) = \frac{u \cdot v}{\|u\|\,\|v\|}\] <p>This measures directional similarity between two vectors.</p> <h3 id="2-cosine-distance">2) Cosine distance</h3> \[d(u, v) = 1 - \text{sim}(u, v)\] <p>This turns similarity into a distance-like value, where smaller means closer.</p> <h3 id="3-reranking-score">3) Reranking score</h3> \[\text{score}(i, j) = \text{sim}(i, j) + \alpha \cdot \frac{|\mathcal{N}_i \cap \mathcal{N}_j|}{\text{shortlist}_k}\] <p>This says a candidate neighbor is better when it is both directly similar and contextually similar.</p> <h3 id="4-temperature-adjusted-neighborhood-weight">4) Temperature-adjusted neighborhood weight</h3> \[s_j = 1 - d_j\] \[w_j = s_j^{1/T}\] <p>These convert distance into similarity and then sharpen or smooth the result.</p> <h3 id="5-probability-normalization">5) Probability normalization</h3> \[p_j = \frac{w_j}{\sum_{m=1}^{k} w_m}\] <p>This turns the neighbor weights into a probability distribution.</p> <h3 id="6-shannon-entropy">6) Shannon entropy</h3> \[H = -\sum_{j=1}^{k} p_j \log(p_j)\] <p>This measures how spread out the neighborhood probabilities are.</p> <h3 id="7-normalized-entropy">7) Normalized entropy</h3> \[H_{\text{norm}} = \frac{H}{\log(k)}\] <p>This rescales entropy so it is easier to compare across settings.</p> <h3 id="what-the-formulas-are-doing-conceptually">What the formulas are doing conceptually</h3> <p>The first pair of formulas tells us how close two vectors are. The reranking score asks whether two points belong to similar local surroundings. The entropy formulas then ask how concentrated each point’s neighborhood really is once we have the curated graph.</p> <p>That chain matters. It is easy to write a formula; it is harder to choose formulas that work together in a way that tells a coherent story about the data.</p> <h2 id="practical-tuning-section">Practical tuning section</h2> <p>The project has a few tuning knobs that change the feel of the output.</p> <h3 id="to-make-the-graph-stricter">To make the graph stricter</h3> <p>Increase <code class="language-plaintext highlighter-rouge">--min-sim</code>, keep mutual neighbors enabled, and reduce <code class="language-plaintext highlighter-rouge">--edge-neighbors</code>.</p> <p>This will usually make the map cleaner and reduce weak cross-links.</p> <h3 id="to-make-it-more-exploratory">To make it more exploratory</h3> <p>Lower <code class="language-plaintext highlighter-rouge">--min-sim</code>, allow more edges, and optionally disable mutual neighbors.</p> <p>This can reveal broader structure, but it can also introduce more noise.</p> <h3 id="what-the-main-knobs-do">What the main knobs do</h3> <ul> <li><code class="language-plaintext highlighter-rouge">--min-sim</code> filters out weak neighbors.</li> <li><code class="language-plaintext highlighter-rouge">--mutual-knn</code> keeps only more stable reciprocal relationships.</li> <li><code class="language-plaintext highlighter-rouge">--edge-neighbors</code> changes how many graph edges are drawn.</li> <li><code class="language-plaintext highlighter-rouge">--entropy-temp</code> controls how peaked or flat the neighbor distribution becomes.</li> </ul> <h3 id="example-recipes">Example recipes</h3> <p>Clean and stable:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python run_easy.py <span class="nt">--preset</span> strict <span class="nt">--method</span> umap
</code></pre></div></div> <p>Balanced default:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python run_easy.py <span class="nt">--preset</span> balanced <span class="nt">--method</span> umap
</code></pre></div></div> <p>More exploratory:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python run_easy.py <span class="nt">--preset</span> exploratory <span class="nt">--method</span> tsne
</code></pre></div></div> <h3 id="umap-versus-t-sne">UMAP versus t-SNE</h3> <p>Use UMAP when you want a good default layout that usually preserves local structure well.</p> <p>Use t-SNE when you want stronger cluster separation for inspection, and you are comfortable with the idea that the global layout may be less faithful.</p> <h2 id="how-to-run-it">How to run it</h2> <p>The default run is:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python main.py
</code></pre></div></div> <p>That uses UMAP, a moderate neighborhood size, curated graph settings, and entropy normalization.</p> <p>For starters, the easier entry point is:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python run_easy.py
</code></pre></div></div> <p>That script wraps the same pipeline in presets:</p> <ul> <li><code class="language-plaintext highlighter-rouge">balanced</code>: the default recommendation,</li> <li><code class="language-plaintext highlighter-rouge">strict</code>: cleaner and more selective,</li> <li><code class="language-plaintext highlighter-rouge">exploratory</code>: looser and more open to broader context.</li> </ul> <p>You can also pass a query sentence to see where a new example lands:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python run_easy.py <span class="nt">--query</span> <span class="s2">"possible SQL injection in login"</span>
</code></pre></div></div> <p>The project generates two HTML files:</p> <ul> <li><a href="entropy_map.html">entropy_map.html</a></li> <li><a href="category_map.html">category_map.html</a></li> </ul> <h2 id="output-interpretation-guide">Output interpretation guide</h2> <p>The console output gives you a compact numerical summary.</p> <p>Look for:</p> <ul> <li>the embedding shape,</li> <li>the number of graph edges,</li> <li>the entropy min, max, mean, and standard deviation,</li> <li>and the most extreme low-entropy and high-entropy examples.</li> </ul> <p>The entropy map is where the structure becomes visible.</p> <ul> <li>low-entropy regions often look more coherent,</li> <li>high-entropy points often sit near boundaries,</li> <li>and edge lines show local neighborhood relationships.</li> </ul> <p>The category map is useful for checking how the known labels distribute across the 2D layout.</p> <ul> <li>if categories form clean islands, the embedding geometry is separating them well,</li> <li>if they overlap, that can mean the language itself is overlapping or that the model is not strongly distinguishing them.</li> </ul> <p>High-entropy points should be read as structurally mixed, not wrong.</p> <p>Low-entropy points should be read as locally concentrated, not necessarily more important.</p> <p>Category overlap is not failure by itself. Sometimes it simply means the sentences are genuinely similar in meaning or style.</p> <h2 id="use-cases-and-limitations">Use cases and limitations</h2> <p>This project is useful when you want to explore semantic structure, compare categories, inspect ambiguous language, or get a visual sense of how embeddings behave on a small dataset.</p> <p>It is not a full predictive system.</p> <p>It does not replace supervised classification, anomaly detection with ground truth, or a retrieval system backed by a production index.</p> <p>That matters because exploratory geometry and prediction are related but not the same. This project is good at showing structure; it is not trying to make definitive decisions.</p> <p>There are also limits to keep in mind:</p> <ul> <li>embeddings can carry model bias,</li> <li>projection can distort global geometry,</li> <li>entropy depends on the quality of the neighborhood graph,</li> <li>and the map is only as informative as the dataset you give it.</li> </ul> <h2 id="closing-section">Closing section</h2> <p>The bigger lesson here is not that text becomes a perfect map. It is that language can be represented geometrically in a way that is useful for inspection.</p> <p>Once sentences become vectors, the rest of the pipeline becomes a series of carefully chosen questions:</p> <ul> <li>who is near whom,</li> <li>how sharp is the neighborhood,</li> <li>what does the 2D layout preserve,</li> <li>and what does the human reader notice when the result is finally visible?</li> </ul> <p>That is what this project is really doing. It is not just plotting points. It is turning language into a structure we can explore.</p> <p>If the article feels useful, the best outcome is not that it sounds impressive. The best outcome is that it makes the pipeline easier to understand, easier to tune, and easier to extend.</p> <h2 id="notes">Notes</h2> <ul> <li>The first run may download model weights from Hugging Face.</li> <li>You may see non-fatal warnings from UMAP or model loading.</li> <li>The HTML files are self-contained and can be opened directly in a browser.</li> </ul>]]></content><author><name></name></author><category term="ai"/><category term="ai"/><summary type="html"><![CDATA[Understanding text geometry through embeddings, KNN graphs, entropy, and interactive visualization]]></summary></entry><entry><title type="html">The Cost of Free Security: Wazuh and the Engineering Bill</title><link href="https://karankessy.github.io/blog/2026/cost-of-Engineering/" rel="alternate" type="text/html" title="The Cost of Free Security: Wazuh and the Engineering Bill"/><published>2026-04-10T10:00:00+00:00</published><updated>2026-04-10T10:00:00+00:00</updated><id>https://karankessy.github.io/blog/2026/cost-of-Engineering</id><content type="html" xml:base="https://karankessy.github.io/blog/2026/cost-of-Engineering/"><![CDATA[<figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/wazuh-crowdstrike-comparison-480.webp 480w,/assets/img/wazuh-crowdstrike-comparison-800.webp 800w,/assets/img/wazuh-crowdstrike-comparison-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/wazuh-crowdstrike-comparison.png" width="100%" height="auto" alt="Wazuh vs CrowdStrike: Bundled vs Unbundled Security" loading="eager" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <p>The question less of whether Wazuh has every capability out of the box. But more of whether your team does.</p> <hr/> <h2 id="starting-with-the-criticism-as-needful">Starting with the Criticism as needful</h2> <p>Spend any time in security forums or vendor comparison threads and you will hear the same takes. Wazuh does not have real machine learning. Its behavioral detection cannot match CrowdStrike Falcon. The response automation feels clunky. There is no native SOAR. You cannot get proper network telemetry without bolting on several other tools.</p> <p>And here is the thing. On the surface, none of that is wrong.</p> <p>The mistake is not in noticing the gaps. The mistake is in what you conclude from them. Because there is a meaningful difference between a platform that cannot do something and a platform that delegates it. That difference changes the entire conversation.</p> <hr/> <h2 id="what-wazuh-actually-is">What Wazuh Actually Is</h2> <p>No marketing language here.</p> <p>Wazuh is primarily a rule-based opensource SIEM and XD &amp; native R (response) promised to be coming up, platform built on OSSEC lineage. It is genuinely strong in log collection and correlation, file integrity monitoring, vulnerability detection, and compliance mapping. It ships with thousands of detection rules, agent-based endpoint monitoring across Linux, Windows, and macOS, and pre-built compliance modules covering PCI DSS, HIPAA, NIST 800-53, and GDPR. Dashboards included. That alone would cost real money on a commercial platform.</p> <p>What it does not do natively: it does not run ML models on endpoint telemetry, it does not perform kernel-level behavioral analysis, and it does not have a polished built-in SOAR. Its active response exists and works, but it is not the click-and-remediate workflow you get from something priced at twenty dollars per endpoint per month.</p> <p>That is the honest baseline. Being clear about it is the only way to have a useful conversation about what comes next.</p> <hr/> <h2 id="kernel-space-vs-user-space-and-why-it-matters">Kernel Space vs User Space, and Why It Matters</h2> <p>This part is important and gets skipped too often.</p> <p>On Windows, the Wazuh agent works mostly in user space. It collects telemetry from Windows Event Logs, Sysmon, security logs, and monitored file paths, then forwards that data to the Wazuh manager for rule matching and correlation. That model stays away from kernel interception by default, which makes it less intrusive and lower risk from a deployment standpoint.</p> <p>CrowdStrike Falcon is architecturally different. Its agent operates at the kernel level, giving it deep visibility into process behavior, memory operations, and system calls that a log-based agent simply cannot see. That visibility is what allows it to catch novel malware, fileless attacks, and living-off-the-land techniques that never produce a clean log entry.</p> <p>But kernel space is high privilege. When something goes wrong there, the impact is not contained.</p> <p>The July 2024 CrowdStrike incident is the clearest example of that tradeoff in production. A faulty content update pushed to the Falcon sensor caused BSODs on Windows machines globally, taking down critical systems across airlines, banks, hospitals, and more. The sensor’s kernel-level position meant a bad update did not just crash the agent, it crashed the OS. That is the blast radius you accept when a security component sits that deep in the stack.</p> <p>So this is a tradeoff, not a simple winner-loser comparison. User-space telemetry is less intrusive and carries lower deployment risk. Kernel-level telemetry is deeper and catches more, but a bad update can take the whole system down with it. Neither approach is careless. They just accept different kinds of risk.</p> <hr/> <h2 id="what-crowdstrike-does-better-out-of-the-box">What CrowdStrike Does Better Out of the Box</h2> <p>Credit where it is due. What CrowdStrike has built is genuinely impressive from an engineering standpoint.</p> <p>The behavioral AI running on Falcon is not a feature that was bolted on. It is an architectural decision baked into the product from the beginning, trained on massive telemetry from millions of endpoints over many years. On top of that, you get real-time response tooling that lets analysts interact directly with compromised endpoints from the console, managed threat intelligence from a dedicated research team, device control, identity protection modules, and vendor-backed SLAs.</p> <p>These are not just checklist items. They are the result of serious R&amp;D investment and a business model built around doing everything in one place. A vendor at that scale can build a vertically integrated product where every component talks to every other component natively. That is a real advantage.</p> <p>Open source was never trying to replicate that model. It is not competing on those terms.</p> <hr/> <h2 id="where-the-comparison-goes-wrong">Where the Comparison Goes Wrong</h2> <p>Most Wazuh vs CrowdStrike discussions frame it as a feature-for-feature shootout. Does Wazuh have ML? No. Does CrowdStrike? Yes. Score one. Does Wazuh have SOAR? Not natively. Point to CrowdStrike. By the end of the checklist, Wazuh looks incomplete and CrowdStrike looks like the obvious winner.</p> <p>But that framing assumes they are competing for the same buyer, solving the same problem, under the same philosophy. They are not.</p> <p>CrowdStrike is a commercial XDR platform built for organizations that want a fully integrated, vendor-managed security stack. You pay the per-endpoint fee, you get the whole thing, the vendor is responsible for making it work together.</p> <p>Wazuh is an open-source security foundation built for organizations that want to own their architecture, customize it to their environment, and integrate the tools they choose.</p> <p>Comparing them on a feature checklist is like comparing a custom-built rack server to a MacBook Pro. The MacBook is polished, works out of the box, and you pay a premium for that experience. The rack server is components. It assumes you know what you are building and have the hands to put it together.</p> <p>Neither is wrong. They just start from different assumptions about who is assembling the stack.</p> <hr/> <h2 id="what-you-can-actually-build-on-top-of-wazuh">What You Can Actually Build on Top of Wazuh</h2> <p>This is where it gets concrete.</p> <p><strong>Response and automation.</strong> Wazuh’s active response module can execute scripts on endpoints when specific alerts fire. Block an IP, kill a process, isolate a host. It is not a GUI-driven playbook builder, but it works. For more sophisticated orchestration, Shuffle is the most common open-source SOAR integration. It connects to Wazuh’s API and lets you build automated workflows: enrich an alert with threat intel, open a ticket, trigger a response action, notify the team. n8n works similarly for teams that prefer its model. For case management, TheHive sits on top and gives analysts a structured way to investigate alerts. Combined with Cortex for automated analysis, you get something close to an integrated response workflow. More setup than CrowdStrike. More maintenance. But it works.</p> <p><strong>Machine learning.</strong> Wazuh does not have a native ML engine, but the underlying data layer, OpenSearch, does. OpenSearch ships with an ML plugin that includes Random Cut Forest, an algorithm designed for streaming anomaly detection on time-series data. You can configure it to learn normal behavior baselines across your Wazuh indices and flag deviations like unusual login patterns, authentication spikes, or abnormal process creation rates. For teams that want more control, external Python pipelines can consume Wazuh data via API, run custom models, and feed results back in as custom alerts. A 2023 study in MDPI’s Electronics explored exactly this kind of integration and found the gap between Wazuh’s native capabilities and ML-augmented detection is bridgeable, not fundamental. The community is actively solving this, not ignoring it.</p> <p><strong>Threat intelligence.</strong> Wazuh integrates with VirusTotal for hash and URL reputation, MISP for structured threat intel feeds, URLHaus for malicious URL detection, and Maltiverse for multi-source indicator enrichment. These enrich alerts automatically or flag matches against known IOCs. Configuration is required. API keys, custom rules, occasionally wrapper scripts. The plumbing is there and the community documentation is solid.</p> <p><strong>Network visibility.</strong> Wazuh is primarily endpoint and log focused, but syslog ingestion is a core strength. Cisco ASA, Firepower, Meraki, ISE all speak syslog, and Wazuh can ingest, decode, and correlate those logs with custom decoders. For NetFlow data, you add a collector like Elastiflow feeding into the same OpenSearch backend. It is an additional component, not a native feature, but it sits cleanly within the same architecture.</p> <p><strong>Compliance.</strong> This is one area where Wazuh needs no apology. PCI DSS, HIPAA, NIST 800-53, GDPR, SOC 2. Rules are tagged to specific requirements, dashboards are pre-configured, and the mappings are built in. For organizations where compliance reporting is a primary driver, this is one of the strongest out-of-the-box arguments for the platform.</p> <hr/> <h2 id="the-honest-tradeoffs">The Honest Tradeoffs</h2> <p>Wazuh costs you engineering time. Every integration above requires someone to set it up, test it, tune it, and maintain it when things change. Shuffle does not configure itself. OpenSearch ML models need training and monitoring. Custom decoders for Cisco logs need updating when log formats shift. If your security team is two people also covering IT operations, the overhead of building and maintaining a full Wazuh ecosystem is significant.</p> <p>CrowdStrike costs you money and control. At roughly fifteen to twenty-five dollars per endpoint per month depending on modules and contract, a thousand-endpoint organization is looking at somewhere between one hundred eighty thousand and three hundred thousand dollars annually. You get a lot for that. But you are also locked into a vendor’s roadmap, their data retention policies, and their definition of what constitutes a threat. And as July 2024 showed, when a vendor with kernel-level access pushes a bad update, you are along for the ride whether you want to be or not.</p> <p>For resource-constrained organizations with competent technical staff, a well-built Wazuh stack with Shuffle, TheHive, OpenSearch ML, and threat intel feeds can cover eighty to ninety percent of what a commercial XDR provides, at a fraction of the licensing cost. The remaining gap is what you are paying CrowdStrike for. Whether you actually need it depends on your threat model.</p> <p>For enterprises that need a SOC-ready platform with vendor support, guaranteed SLAs, and minimal internal engineering overhead, CrowdStrike makes a legitimate case. There is no shame in buying a product that works out of the box when the alternative is building something your team does not have the capacity to maintain.</p> <hr/> <p>So to simply sum it up, Wazuh is not missing everything. It is just not bundled.</p> <p>The platform merely seems to be reflecting your team’s capability back at you. Put skilled engineers behind it, invest the time to integrate the right tools, and the ceiling is genuinely high. Install it expecting a commercial XDR experience with no additional effort and you will be disappointed, and you will write a forum post about how Wazuh lacks ML.</p> <p>That pattern is not new in technology. It is the same dynamic between Linux and Windows, between a hand-tuned Nginx setup and a managed CDN, between building your own data pipeline and paying for a managed one. The unbundled approach gives you flexibility, control, and lower licensing costs. The bundled approach gives you less friction, faster time-to-value, and someone to call when things break. Both are valid. Neither is universally better.</p> <p>The criticism of Wazuh is real but incomplete. The platform does not lack capabilities. It delegates them. Delegation only looks like a weakness when there is no one to delegate to.</p> <p>The honest question was never whether Wazuh is as good as CrowdStrike. It was always simpler than that: does your team have the skill and infrastructure to finish what Wazuh starts?</p> <p>If yes, you have something powerful. If no, the limitation is in the context, not the tool.</p> <ul> <li>Be a part of Wazuh Ambassadors Program, if you believe you’re a good fit: <a href="https://wazuh.com/ambassadors-program/?utm_source=ambassadors&amp;utm_medium=referral&amp;utm_campaign=ambassadors+program">https://wazuh.com/ambassadors-program/</a></li> </ul>]]></content><author><name></name></author><category term="security"/><category term="siem"/><category term="security"/><summary type="html"><![CDATA[CrowdStrike costs money and control. Wazuh costs engineering time. Understanding the fundamental tradeoff between a fully integrated commercial XDR platforms and an open-source security foundation you customize yourself.]]></summary></entry><entry><title type="html">Everything is Inter-Process Communication: HTTP, REST APIs, Serverless functions, what not</title><link href="https://karankessy.github.io/blog/2026/everything-is-IPC/" rel="alternate" type="text/html" title="Everything is Inter-Process Communication: HTTP, REST APIs, Serverless functions, what not"/><published>2026-03-20T10:00:00+00:00</published><updated>2026-03-20T10:00:00+00:00</updated><id>https://karankessy.github.io/blog/2026/everything-is-IPC</id><content type="html" xml:base="https://karankessy.github.io/blog/2026/everything-is-IPC/"><![CDATA[<p>Actual IPC, syscalls, or kernel code repository for this article: <a href="https://github.com/karankessy/benchmarkIPC">benchmarkIPC</a>.</p> <blockquote> <p>Disclaimer: the visualizer below is a client-side-only teaching aid. It does not run real IPC, syscalls, or kernel code. The behavior is mimicked entirely in JavaScript so the flow is easy to follow. Actual</p> </blockquote> <h3 id="client-side-visualizer">Client-Side Visualizer</h3> <iframe src="/assets/html/ipc_visualizer.html" title="IPC visualizer demo" loading="lazy" style="width: 100%; height: 760px; border: 1px solid var(--global-divider-color, #ded6c8); border-radius: 12px;"></iframe> <p>If the embedded demo does not load in your browser, open the standalone version at <a href="/assets/html/ipc_visualizer.html">/assets/html/ipc_visualizer.html</a>.</p> <p>Modern software looks intimidating from the outside. You hear terms like HTTP requests, REST APIs, microservices, containers, and serverless functions, and it can feel like there is an endless number of concepts to understand. But once you start pulling at the threads, something interesting happens. The complexity begins to collapse into one surprisingly simple idea.</p> <p>Processes talking to each other.</p> <p>That is really what is happening underneath all of it. When you type a URL into your browser and hit enter, your browser does not do everything by itself. The networking stack, the kernel, and the renderer are all separate processes, and they are constantly passing messages and data between themselves just to get that page on your screen. What looks like one action from the outside is actually a quiet conversation happening beneath the surface.</p> <p>The same is true on the backend. When a server handles an API call, it is not one monolithic thing doing all the work. The web server talks to worker processes. Those worker processes talk to database drivers. The database drivers talk to storage engines. They exchange data through pipes, sockets, shared memory, or queues managed by the kernel. Each one hands off responsibility to the next, like a relay race running faster than you can see.</p> <p>Even HTTP, which most people think of as network communication, is often just data traveling through a socket between two processes sitting on the same machine. The “network” label makes it sound distant, but physically, nothing left the box.</p> <p>IPC, inter-process communication, is not an obscure topic buried in operating systems textbooks. It is the quiet foundation that everything else rests on. Learn it, and the rest of the stack starts to make a lot more sense.</p> <h2 id="1-why-ipc-exists">1. Why IPC Exists</h2> <p>Modern software is rarely a single process doing one thing.</p> <p>Browsers isolate tabs for security. Databases split query workers from storage workers. API backends offload CPU-heavy jobs to background processes. The moment you divide work across processes, you need them to talk to each other.</p> <p>That’s the problem IPC solves.</p> <p>The OS deliberately isolates processes. Each one gets a private virtual address space. Process A cannot read or write Process B’s memory, the MMU and kernel enforce this boundary hard. Without it, one crash could corrupt the entire machine state.</p> <p>But isolation creates friction. Two isolated programs cannot simply exchange a pointer and say “read from here.” So IPC mechanisms become the bridge between isolation and collaboration.</p> <p>This document uses a real project as a systems microscope: four IPC mechanisms implemented in C, benchmarked head-to-head, and orchestrated through a Node.js layer. The four mechanisms are:</p> <ul> <li>POSIX shared memory</li> <li>Pipes</li> <li>Unix domain sockets</li> <li>TCP sockets</li> </ul> <p>The central question: if kernel mediation is safer and simpler, when is the overhead worth paying, and when should you bypass it entirely?</p> <hr/> <h2 id="2-how-process-isolation-works">2. How Process Isolation Works</h2> <h3 id="processes-vs-threads">Processes vs Threads</h3> <p>A <strong>process</strong> is a running program with:</p> <ul> <li>Its own virtual address space</li> <li>Its own file descriptor table</li> <li>Its own scheduling and accounting state in the kernel</li> </ul> <p>A <strong>thread</strong> is an execution stream inside a process. Threads share the process’s memory. Each thread has its own registers and stack. The scheduler treats threads and processes as the basic runnable units.</p> <p>The key fact: process memory is private by default.</p> <h3 id="virtual-address-spaces-and-the-mmu">Virtual Address Spaces and the MMU</h3> <p>A process sees virtual addresses, not physical RAM addresses. The <strong>MMU (Memory Management Unit)</strong> translates virtual addresses to physical addresses using page tables managed by the kernel.</p> <p>Why this matters for IPC: Process A and Process B may both have valid memory at virtual address <code class="language-plaintext highlighter-rouge">0x1000</code>. Those addresses can map to completely different physical pages. Pointers are process-local. They mean nothing outside their own address space.</p> <p>So any data exchange requires either:</p> <ul> <li>A shared mapping to the same physical pages (shared memory), or</li> <li>Kernel-mediated data transfer (pipe, socket, TCP)</li> </ul> <h3 id="why-just-share-memory-is-non-trivial">Why “Just Share Memory” Is Non-Trivial</h3> <p>Even if you map the same physical page into two processes, correctness is not automatic:</p> <ul> <li>Writer and reader may race</li> <li>CPU caches may delay visibility without proper ordering</li> <li>Struct layout mismatches can silently corrupt interpretation</li> </ul> <p>IPC is both an OS topic and a concurrency topic. You can’t fully separate them.</p> <hr/> <h2 id="3-foundations-syscalls-memory-and-kernel-mediation">3. Foundations: Syscalls, Memory, and Kernel Mediation</h2> <h3 id="user-space-vs-kernel-space">User Space vs Kernel Space</h3> <p>CPUs enforce privilege levels. User code runs unprivileged (ring 3 on x86, EL0 on ARM). Kernel code runs privileged (ring 0, EL1). User programs cannot directly reprogram page tables, manage hardware interrupts, or manipulate scheduler queues.</p> <p>A <strong>system call</strong> is a controlled privilege transition. It’s how user programs request kernel operations safely.</p> <p>Call chain for something like <code class="language-plaintext highlighter-rouge">send()</code>:</p> <ol> <li>Application calls libc wrapper</li> <li>libc places syscall number and args in registers</li> <li>CPU executes trap instruction (<code class="language-plaintext highlighter-rouge">syscall</code>, <code class="language-plaintext highlighter-rouge">svc</code>, etc.)</li> <li>Kernel validates pointers/permissions, runs syscall handler</li> <li>Kernel returns to user mode with result or <code class="language-plaintext highlighter-rouge">errno</code></li> </ol> <p>A mode transition is not free. It may flush pipeline stages, perturb branch prediction, and force synchronization between user and kernel control flow.</p> <h3 id="file-descriptor-model">File Descriptor Model</h3> <p>A <strong>file descriptor (FD)</strong> is a per-process integer that indexes that process’s descriptor table. Pipes, sockets, regular files, shared memory objects opened via <code class="language-plaintext highlighter-rouge">shm_open()</code> — all exposed through FDs.</p> <p>This “everything is a handle” model is why the API feels unified:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">read</span><span class="p">(</span><span class="n">fd</span><span class="p">,</span> <span class="p">...)</span>
<span class="n">write</span><span class="p">(</span><span class="n">fd</span><span class="p">,</span> <span class="p">...)</span>
<span class="n">close</span><span class="p">(</span><span class="n">fd</span><span class="p">)</span>
</code></pre></div></div> <p>Different FD types route to different kernel object implementations underneath.</p> <h3 id="context-switch">Context Switch</h3> <p>A <strong>context switch</strong> happens when the scheduler stops one execution context and resumes another. The kernel saves CPU state, program counter, stack pointer, general registers, SIMD state,switches memory mappings, and resumes another thread or process.</p> <p>Why it’s expensive:</p> <ul> <li>CPU caches and TLB entries become less useful for the next task</li> <li>Scheduler bookkeeping and run-queue operations add overhead</li> <li>Frequent switches inflate IPC latency for small messages</li> </ul> <p>IPC paths that block and wake frequently will accumulate this cost at high message rates.</p> <h3 id="virtual-memory-internals-page-tables-and-tlb">Virtual Memory Internals: Page Tables and TLB</h3> <p>Memory is divided into <strong>pages</strong> (usually 4 KiB). A process’s <strong>page tables</strong> map virtual page numbers to physical frames with permissions (read/write/execute, user/kernel).</p> <p>The <strong>TLB (Translation Lookaside Buffer)</strong> caches recent virtual-to-physical translations. TLB hits are fast. Misses require a page table walk, which costs cycles and memory bandwidth.</p> <p>How two processes share one physical page:</p> <ol> <li>Kernel allocates or references a physical frame</li> <li>Kernel installs page table entries in both processes pointing to the same physical frame (at different virtual addresses)</li> <li>CPU cache coherence ensures writes by one core become visible to readers on other cores,subject to synchronization rules</li> </ol> <h3 id="synchronization-and-memory-ordering">Synchronization and Memory Ordering</h3> <p><strong>Semaphore</strong>: Kernel-supported counter. <code class="language-plaintext highlighter-rouge">sem_wait()</code> decrements or blocks; <code class="language-plaintext highlighter-rouge">sem_post()</code> increments and may wake waiters.</p> <p><strong>Spin lock</strong>: Busy-wait loop that polls a flag. Avoids sleep/wakeup latency but burns CPU.</p> <p><strong>Atomic operation</strong>: Indivisible read-modify-write or load/store with ordering guarantees.</p> <p><strong>Acquire/Release ordering</strong>: A release store publishes prior writes. An acquire load that observes that store guarantees subsequent reads see published data. Weaker than full sequential consistency, but sufficient for single-producer/single-consumer handoff, and cheaper.</p> <p><strong>Cache line</strong>: Coherence unit, commonly 64 bytes on modern CPUs.</p> <p><strong>False sharing</strong>: Two unrelated variables land on the same cache line. One core modifying its variable invalidates the other core’s cached copy of an unrelated variable. Cross-core invalidations hurt performance badly even with no logical conflict.</p> <hr/> <h2 id="4-the-two-fundamental-models">4. The Two Fundamental Models</h2> <p>There are exactly two strategies for IPC:</p> <p><strong>Shared memory</strong>: The kernel maps the same physical page into both processes’ virtual address spaces. After the initial <code class="language-plaintext highlighter-rouge">shm_open</code> + <code class="language-plaintext highlighter-rouge">mmap</code> setup, reads and writes go directly to RAM,load and store instructions with no syscall involvement. The processes must synchronize access themselves.</p> <p><strong>Message passing</strong>: The kernel copies data between address spaces. One process writes into a kernel buffer; the other reads it out. Every message involves at least two context switches and two memory copies. The kernel handles synchronization implicitly, a blocking <code class="language-plaintext highlighter-rouge">read</code>/<code class="language-plaintext highlighter-rouge">recv</code> sleeps until data is available.</p> <p>The trade-off in one table:</p> <table> <thead> <tr> <th>Dimension</th> <th>Shared Memory</th> <th>Message Passing</th> </tr> </thead> <tbody> <tr> <td>Kernel per message</td> <td>No (after setup)</td> <td>Yes (every message)</td> </tr> <tr> <td>Copies per message</td> <td>0 (writer stores → reader loads)</td> <td>2 (user→kernel→user)</td> </tr> <tr> <td>Context switches</td> <td>0</td> <td>2 per message</td> </tr> <tr> <td>Synchronization</td> <td>Manual (your problem)</td> <td>Implicit (kernel blocks reader)</td> </tr> <tr> <td>Cross-host</td> <td>Never</td> <td>Yes (TCP sockets)</td> </tr> <tr> <td>Complexity</td> <td>High</td> <td>Low-to-moderate</td> </tr> </tbody> </table> <p>Shared memory is faster because it’s just reading and writing RAM after setup. No system calls. No copying. Direct memory access.</p> <p>But you pay for that speed with complexity. Synchronization is your problem. Data layout is your problem. Cache coherence is your problem.</p> <p>Message passing is slower because the kernel sits in the middle of every transfer. But the kernel solves coordination for you, and serialization handles layout differences and version mismatches.</p> <hr/> <h2 id="5-shared-memory-fast-and-unforgiving">5. Shared Memory: Fast and Unforgiving</h2> <h3 id="how-it-works">How It Works</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>+-------------+        +-------------+
| Process A   |        | Process B   |
|-------------|        |-------------|
| Virtual Mem |        | Virtual Mem |
|   0x5000    |        |   0x8000    |
|      |      |        |      |      |
|      v      |        |      v      |
| Shared Page +--------+ Shared Page |
+-------------+        +-------------+
            |
            v
    Physical Memory
    (Single Page)
</code></pre></div></div> <p>Process A sees the shared region at <code class="language-plaintext highlighter-rouge">0x5000</code>. Process B sees it at <code class="language-plaintext highlighter-rouge">0x8000</code>. Different virtual addresses. Same physical bytes.</p> <p>After <code class="language-plaintext highlighter-rouge">mmap</code>, every read and write is just a CPU load or store instruction. No syscall. No kernel involvement.</p> <h3 id="syscall-sequence">Syscall Sequence</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Writer:                           Reader:
  shm_open(O_CREAT|O_RDWR)         shm_open(O_RDWR)
  ftruncate(sizeof(SharedRegion))
  mmap(MAP_SHARED)                  mmap(MAP_SHARED)
  sem_open(O_CREAT) × 2             sem_open() × 2
  close(shm_fd)                     close(shm_fd)
  ──── setup complete ────           ──── setup complete ────
  sem_wait(sem_reader)               sem_wait(sem_writer)
  memcpy to region-&gt;msg              memcpy from region-&gt;msg
  sem_post(sem_writer)               sem_post(sem_reader)
  ... repeat ...                     ... until msg.done == 1 ...
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">shm_open()</code> creates or finds the object in kernel namespace. <code class="language-plaintext highlighter-rouge">ftruncate()</code> sets its byte size,skip this and your mapping is zero-length. <code class="language-plaintext highlighter-rouge">mmap(MAP_SHARED)</code> installs the virtual mapping. First access triggers a page fault; kernel materializes the physical frame, updates the PTE, and resumes the faulting instruction.</p> <p><code class="language-plaintext highlighter-rouge">shm_unlink()</code> and <code class="language-plaintext highlighter-rouge">sem_unlink()</code> must be called at startup to clear stale state from previous crashed runs. POSIX shared memory and named semaphores persist beyond process lifetime until explicitly deleted. This will catch you eventually.</p> <p>Zero-copy nuance: it’s zero-copy with respect to kernel-mediated transfer. The CPU still moves bytes through caches when you <code class="language-plaintext highlighter-rouge">memcpy</code> or do load/store instructions. That’s not the same as free.</p> <h3 id="cache-line-aware-data-layout">Cache-Line-Aware Data Layout</h3> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">struct</span> <span class="n">SharedMessage</span> <span class="p">{</span>
    <span class="kt">int</span>      <span class="n">sequence</span><span class="p">;</span>    <span class="c1">// 4 bytes</span>
    <span class="kt">int</span>      <span class="n">type</span><span class="p">;</span>        <span class="c1">// 4 bytes</span>
    <span class="kt">double</span>   <span class="n">value</span><span class="p">;</span>       <span class="c1">// 8 bytes</span>
    <span class="kt">char</span>     <span class="n">text</span><span class="p">[</span><span class="mi">40</span><span class="p">];</span>    <span class="c1">// 40 bytes</span>
    <span class="kt">int</span>      <span class="n">done</span><span class="p">;</span>        <span class="c1">// 4 bytes (termination flag)</span>
    <span class="kt">char</span>     <span class="n">_pad</span><span class="p">[</span><span class="mi">4</span><span class="p">];</span>     <span class="c1">// padding → total: 64 bytes</span>
<span class="p">};</span>
</code></pre></div></div> <p>The struct is padded to exactly 64 bytes, a typical CPU cache line. This is deliberate. If the struct straddled two cache lines, every access would require fetching both. Worse: on multi-core systems, two unrelated variables on the same cache line cause false sharing. Cores keep invalidating each other’s cached copies on every write, even when they’re writing to different fields. You’ll see this in a profiler as inexplicable slowdown in code that looks correct.</p> <h3 id="the-data-layout-problem">The Data Layout Problem</h3> <p>Both processes are staring at raw bytes. If they don’t agree on what those bytes mean, nothing works and nothing crashes.</p> <p>Say the writer compiles with one alignment setting and the reader compiles with another. The writer expects <code class="language-plaintext highlighter-rouge">double value</code> at offset 8; the reader expects it at offset 12. The read succeeds. The value is garbage. No error. No exception. Just wrong data flowing silently through your system.</p> <p>Endianness compounds this. x86 stores <code class="language-plaintext highlighter-rouge">0x12345678</code> as <code class="language-plaintext highlighter-rouge">78 56 34 12</code>. A big-endian ARM stores it as <code class="language-plaintext highlighter-rouge">12 34 56 78</code>. Share memory between them and every multi-byte value is scrambled.</p> <p>Worst case: writer updates the struct definition, reader doesn’t get recompiled. They’re reading different data structures from the same memory. That bug won’t announce itself immediately. It will corrupt something downstream, and you’ll spend days blaming the wrong component.</p> <p>This is why shared memory almost always requires both processes to be compiled from the same source, with the same compiler, on the same architecture.</p> <h3 id="synchronization">Synchronization</h3> <p>No kernel in the middle means no coordination either.</p> <p>If A writes while B reads, you get a race condition. B might see half-old, half-new data,not the old state, not the new state, something in between that makes no logical sense. Imagine A writes half a value and B reads in between. That bug won’t crash immediately. It’ll corrupt something silently. You’ll find it three weeks later, in a component that had nothing to do with the original write.</p> <p>The two-semaphore handshake solves this:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Writer                           Reader
───────                          ──────
sem_wait(sem_reader)   ←──────┐
write data to region          │
sem_post(sem_writer)   ──┐    │
                         │    │   sem_wait(sem_writer) ←──┐
                         └────┼─→ read data from region   │
                              │   sem_post(sem_reader) ───┘
                              └─→ (writer unblocks)
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">sem_reader</code> starts at 1 (slot is free). <code class="language-plaintext highlighter-rouge">sem_writer</code> starts at 0 (no data ready). One message in flight at a time. Clean. Correct.</p> <p>But correct and fast are different things. Each <code class="language-plaintext highlighter-rouge">sem_wait</code>/<code class="language-plaintext highlighter-rouge">sem_post</code> is a syscall. At 100K messages, that’s 200K syscalls, precisely the overhead shared memory is supposed to avoid. Semaphores are the right tool for teaching demos and coarse-grained coordination. For high-frequency hot paths, you need something else.</p> <p>Even proper mutex implementations have edges. On multi-core systems, A might write to shared memory and that write sits in A’s CPU cache. B reads and gets the old value from its own cache. The mutex didn’t help. You also need <strong>memory barriers</strong> — instructions that force CPUs to flush and reload from main memory. This is why mutex and atomic implementations use special instructions that include implicit barriers.</p> <p>And this is how tech giants utilize this IPC mechanism for optimization &amp; efficiency.</p> <p>Case 1: PostgreSQL</p> <h3 id="postgresql--the-shared-buffer-pool">PostgreSQL — The Shared Buffer Pool</h3> <p>Every backend process in Postgres (one per client connection) reads and writes through a single shared memory region called <code class="language-plaintext highlighter-rouge">shared_buffers</code>. Instead of each process hitting disk independently, they all access the same in-memory page cache. The kernel requirement is explicit: Postgres requires OS-level shared memory and semaphores to function at all.</p> <p>The sizing decision matters more than most config tuning. A reasonable starting value is 25% of system memory — allocating more than 40% is unlikely to help because PostgreSQL also relies on the OS page cache as a second layer underneath.</p> <ul> <li>📖 <a href="https://www.postgresql.org/docs/current/kernel-resources.html">PostgreSQL Docs: Managing Kernel Resources</a> — why Postgres needs OS-level IPC to function at all.</li> </ul> <p>Case 2: Nginx</p> <h3 id="nginx--shared-cache-between-workers">NGINX — Shared Cache Between Workers</h3> <p>NGINX workers are separate OS processes, not threads. Each runs its own event loop. For state that must be visible across all workers — cache metadata, rate-limit counters, session data — shared memory is the deliberate exception carved out for cross-worker coordination. Workers are otherwise fully isolated and do not share memory by default.</p> <ul> <li>📖 <a href="https://blog.nginx.org/blog/inside-nginx-how-we-designed-for-performance-scale">Inside NGINX: How We Designed for Performance &amp; Scale</a> — NGINX’s own engineering post on the event-loop worker model.</li> </ul> <p>–</p> <h2 id="6-message-passing-slower-but-the-kernel-does-the-hard-part">6. Message Passing: Slower, But the Kernel Does the Hard Part</h2> <h3 id="the-data-path">The Data Path</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Process A
   |
   | send()
   v
+-------------------+
|   Kernel Space    |
|   IPC Buffer      |
+-------------------+
   |
   | receive()
   v
Process B
</code></pre></div></div> <p>What actually happens for each message:</p> <ol> <li>A calls <code class="language-plaintext highlighter-rouge">send()</code> with a buffer</li> <li>System call traps into kernel mode</li> <li>Kernel validates user pointer, copies data from A’s address space to kernel space</li> <li>B calls <code class="language-plaintext highlighter-rouge">receive()</code></li> <li>Another system call</li> <li>Kernel copies data from kernel space to B’s buffer</li> <li>B resumes with the message</li> </ol> <p>Two copies. Two context switches. Every single message.</p> <p>It’s slower than shared memory. That’s just true.</p> <p>But the kernel now coordinates everything. If B calls <code class="language-plaintext highlighter-rouge">receive()</code> before a message arrives, it sleeps. When a message comes in, the kernel wakes it up. No mutex. No polling loop. No race condition hiding in your timing assumptions.</p> <p>And when data crosses the kernel boundary, you can serialize it. Convert your struct to JSON, Protobuf, or whatever format both sides understand. The receiver deserializes into its own layout. Endianness gets handled. Struct version mismatches can be detected. The layout problem that makes shared memory fragile mostly disappears.</p> <h3 id="blocking-vs-non-blocking">Blocking vs Non-Blocking</h3> <p><strong>Blocking</strong> <code class="language-plaintext highlighter-rouge">receive()</code> puts the process to sleep until a message arrives. Simple, but watch for deadlocks. If A waits for B and B waits for A, neither wakes up ever.</p> <p><strong>Non-blocking</strong> <code class="language-plaintext highlighter-rouge">receive()</code> returns immediately with an error if nothing’s available. The process can do other work and check again. More complexity in your code, but no blocking hazards.</p> <p>Some kernels support <strong>zero-copy</strong>: instead of copying the message twice, the kernel temporarily maps the sender’s buffer into the receiver’s address space. The data never moves. This only works on the same host and not all IPC mechanisms support it, but it narrows the performance gap with shared memory.</p> <h3 id="pipes">Pipes</h3> <p>A kernel-managed byte stream between file descriptors.</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">pipe</span><span class="p">(</span><span class="n">pipefd</span><span class="p">)</span> <span class="err">→</span> <span class="n">fork</span><span class="p">()</span> <span class="err">→</span>
  <span class="n">Parent</span><span class="o">:</span> <span class="n">close</span><span class="p">(</span><span class="n">pipefd</span><span class="p">[</span><span class="mi">0</span><span class="p">]),</span> <span class="n">write</span><span class="p">(</span><span class="n">pipefd</span><span class="p">[</span><span class="mi">1</span><span class="p">],</span> <span class="o">&amp;</span><span class="n">msg</span><span class="p">,</span> <span class="k">sizeof</span><span class="p">(</span><span class="n">msg</span><span class="p">))</span>
  <span class="n">Child</span><span class="o">:</span>  <span class="n">close</span><span class="p">(</span><span class="n">pipefd</span><span class="p">[</span><span class="mi">1</span><span class="p">]),</span> <span class="n">read</span><span class="p">(</span><span class="n">pipefd</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span> <span class="o">&amp;</span><span class="n">buf</span><span class="p">,</span> <span class="k">sizeof</span><span class="p">(</span><span class="n">buf</span><span class="p">))</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">pipe()</code> allocates a kernel pipe object with a circular buffer plus read/write file descriptors. <code class="language-plaintext highlighter-rouge">write()</code> copies user bytes into the pipe’s kernel buffer. <code class="language-plaintext highlighter-rouge">read()</code> copies from the kernel buffer to user memory. Blocked readers wake when new data arrives; blocked writers wake when space frees. EOF signals when all writers close and the buffer drains.</p> <p>Pipes are <strong>anonymous</strong> (created via <code class="language-plaintext highlighter-rouge">pipe()</code>, usually inherited across <code class="language-plaintext highlighter-rouge">fork()</code>) or <strong>named</strong> (filesystem node via <code class="language-plaintext highlighter-rouge">mkfifo</code>, visible to unrelated processes). Unidirectional. Simple. Limited.</p> <p>Raw struct transfer works here because both sides are typically the same binary,same compiler, same struct layout, no serialization needed. If the struct changes, both sides must be recompiled together.</p> <p>Case 1: Unix</p> <h3 id="unix-shell-pipelines--the-original-use-case">Unix Shell Pipelines — The Original Use Case</h3> <p>Every time you run <code class="language-plaintext highlighter-rouge">cat access.log | grep ERROR | wc -l</code>, you’re creating two anonymous pipes in the kernel. The shell connects stdout of one process directly to stdin of the next through kernel-managed byte stream buffers. No intermediate files. No disk.</p> <p>The design insight was that processes don’t need to know they’re using pipes. Standard input and output get redirected at the file descriptor level. Programs written for files work through pipes unchanged. Douglas McIlroy championed the concept at Bell Labs; Ken Thompson implemented it in one evening in 1973 by modifying both the kernel and the shell.</p> <ul> <li>📖 <a href="https://en.wikipedia.org/wiki/Pipeline_(Unix)">Pipeline (Unix) — Wikipedia</a> — full history of the pipe operator and how the kernel implements it.</li> <li>📖 <a href="https://toroid.org/unix-pipe-implementation">How Unix Pipes Are Implemented</a> — traces the <code class="language-plaintext highlighter-rouge">pipe()</code> syscall through modern Linux kernel source.</li> </ul> <hr/> <p>Case 2: Chrome</p> <h3 id="google-chrome--browser--renderer-ipc">Google Chrome — Browser ↔ Renderer IPC</h3> <p>Chrome runs each renderer in a sandboxed process isolated from the browser process and from other tabs. The browser and renderers communicate using Mojo (or Chromium’s legacy IPC system) over an asynchronous named pipe created per renderer process.</p> <p>The isolation is the point. If a renderer crashes, the browser process keeps running. The pipe is the boundary that makes crash containment real — not just an architectural diagram.</p> <ul> <li>📖 <a href="https://www.chromium.org/developers/design-documents/multi-process-architecture/">Inside Chromium: Multi-Process Architecture</a> — Chromium’s own design documentation on process isolation and IPC.</li> <li>📖 <a href="https://developer.chrome.com/blog/inside-browser-part1">Inside a Modern Browser — Chrome Dev Blog</a> — accessible walkthrough of the multi-process model and why IPC is necessary for it.</li> </ul> <hr/> <h3 id="unix-domain-sockets">Unix Domain Sockets</h3> <p>Like pipes but richer: full-duplex, connection-oriented, works between unrelated processes.</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">Server:</span> <span class="n">socket</span><span class="p">(</span><span class="n">AF_UNIX</span><span class="p">,</span> <span class="n">SOCK_STREAM</span><span class="p">)</span> <span class="err">→</span> <span class="n">bind</span><span class="p">(</span><span class="s">"/tmp/ipc.sock"</span><span class="p">)</span>
        <span class="err">→</span> <span class="n">listen</span><span class="p">()</span> <span class="err">→</span> <span class="n">accept</span><span class="p">()</span> <span class="err">→</span> <span class="n">send</span><span class="p">()</span><span class="o">/</span><span class="n">recv</span><span class="p">()</span>
<span class="n">Client</span><span class="o">:</span> <span class="n">socket</span><span class="p">(</span><span class="n">AF_UNIX</span><span class="p">,</span> <span class="n">SOCK_STREAM</span><span class="p">)</span> <span class="err">→</span> <span class="n">connect</span><span class="p">(</span><span class="s">"/tmp/ipc.sock"</span><span class="p">)</span>
        <span class="err">→</span> <span class="n">recv</span><span class="p">()</span><span class="o">/</span><span class="n">send</span><span class="p">()</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">socket()</code> allocates an endpoint. <code class="language-plaintext highlighter-rouge">bind(path)</code> inserts it in the filesystem namespace. <code class="language-plaintext highlighter-rouge">listen()</code> marks the socket passive with an accept queue. <code class="language-plaintext highlighter-rouge">accept()</code> dequeues a pending connection and returns a connected FD. <code class="language-plaintext highlighter-rouge">send()</code>/<code class="language-plaintext highlighter-rouge">recv()</code> copy through kernel socket buffers.</p> <p>Faster than TCP on the same host because it skips IP routing and most network-layer machinery while keeping full socket semantics. Supports optional metadata capabilities like credential passing and fd passing (<code class="language-plaintext highlighter-rouge">SCM_RIGHTS</code>).</p> <p>Unix socket files persist on the filesystem after a crash. If the server dies without <code class="language-plaintext highlighter-rouge">unlink()</code>, the next run fails on <code class="language-plaintext highlighter-rouge">bind()</code>. Both sides should unlink the path at startup before binding.</p> <p>Case 1: Redis</p> <h3 id="redis--dropping-tcp-overhead-for-local-connections">Redis — Dropping TCP Overhead for Local Connections</h3> <p>When Redis and the application run on the same machine, every TCP connection still goes through the full protocol stack: sequence numbers, checksums, flow control, all of it. Switching to a Unix domain socket skips that entirely. Benchmarks consistently show a 15–25% reduction in latency compared to TCP loopback, and roughly 40% higher throughput on simple GET/SET operations.</p> <p>The rule is simple: if both sides are on the same box, use Unix socket. The moment one side moves to a different host, TCP is the only option.</p> <ul> <li>📖 <a href="https://www.revsys.com/12days/unix-sockets/">Unix Sockets — 12 Days of Performance (REVSYS)</a> — clean benchmark comparison with production context.</li> <li>📖 <a href="https://redis.io/docs/management/optimization/benchmarks/">Redis Benchmark Documentation</a> — Redis’s own numbers on socket vs TCP performance.</li> </ul> <hr/> <p>Case 2: Nginx</p> <h3 id="nginx--talking-to-php-fpm">NGINX — Talking to PHP-FPM</h3> <p>In a typical PHP stack, NGINX receives HTTP requests and proxies them to PHP-FPM workers. When both run on the same server, this connection is commonly a Unix domain socket rather than <code class="language-plaintext highlighter-rouge">127.0.0.1:9000</code>. IP routing, checksum computation, and TCP state tracking are all skipped.</p> <p>The performance difference is measurable and the configuration change is one line. It’s one of the cheapest wins in a standard web stack.</p> <ul> <li>📖 <a href="http://nginx.org/en/docs/ngx_core_module.html">NGINX Core Module Documentation</a> — covers Unix socket configuration and inter-worker locking mechanisms.</li> </ul> <h3 id="tcp-sockets">TCP Sockets</h3> <p>Same socket API but routable across hosts.</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nl">Server:</span> <span class="n">socket</span><span class="p">(</span><span class="n">AF_INET</span><span class="p">)</span> <span class="err">→</span> <span class="n">bind</span><span class="p">(</span><span class="n">IP</span><span class="o">:</span><span class="n">port</span><span class="p">)</span> <span class="err">→</span> <span class="n">listen</span><span class="p">()</span> <span class="err">→</span> <span class="n">accept</span><span class="p">()</span> <span class="err">→</span> <span class="n">send</span><span class="p">()</span><span class="o">/</span><span class="n">recv</span><span class="p">()</span>
<span class="n">Client</span><span class="o">:</span> <span class="n">socket</span><span class="p">(</span><span class="n">AF_INET</span><span class="p">)</span> <span class="err">→</span> <span class="n">connect</span><span class="p">(</span><span class="n">IP</span><span class="o">:</span><span class="n">port</span><span class="p">)</span> <span class="err">→</span> <span class="n">send</span><span class="p">()</span><span class="o">/</span><span class="n">recv</span><span class="p">()</span>
</code></pre></div></div> <p>TCP adds overhead that doesn’t exist in Unix sockets:</p> <ul> <li><strong>Segmentation</strong>: byte stream split into segments</li> <li><strong>Sequence numbers</strong>: byte-order tracking and loss recovery</li> <li><strong>Checksums</strong>: integrity verification</li> <li><strong>Flow control</strong>: receiver-advertised window limits in-flight bytes</li> <li><strong>Congestion control</strong>: sender rate adaptation</li> </ul> <p>On localhost, packets bypass physical NIC hardware. But the TCP state machine, buffering, segmentation, ACK handling, and checksum logic still execute in full. This is exactly why TCP is reliable and network-capable, and exactly why it’s slower than shared memory for tiny local messages.</p> <p><code class="language-plaintext highlighter-rouge">SO_REUSEADDR</code> is necessary on the server to allow restart during TCP’s <code class="language-plaintext highlighter-rouge">TIME_WAIT</code> state, avoiding <code class="language-plaintext highlighter-rouge">EADDRINUSE</code> when the previous instance just closed. Use <code class="language-plaintext highlighter-rouge">port 0</code> to let the OS assign an available ephemeral port and avoid collisions entirely.</p> <p>TCP is a byte stream, not a message stream. Two <code class="language-plaintext highlighter-rouge">send()</code> calls may arrive as a single <code class="language-plaintext highlighter-rouge">recv()</code>. Use delimited protocols,newline-delimited JSON works well,to recover message boundaries. Fixed-size messages with known lengths also work if you don’t need schema evolution.</p> <p>Case 1: Microservices</p> <h3 id="every-microservice-architecture--the-forced-choice">Every Microservice Architecture — The Forced Choice</h3> <p>The entire move to microservices is a forced adoption of TCP-based message passing. Services must be deployable independently, scalable independently, and movable between hosts. TCP is the only IPC mechanism that crosses host boundaries. Whether it’s gRPC, HTTP/2, REST, or a message queue, the transport underneath is TCP.</p> <p>The overhead is real. TCP adds protocol cost that shared memory and Unix sockets don’t. But independent deployment across machines leaves no alternative.</p> <ul> <li>📖 <a href="https://blog.cloudflare.com/the-sad-state-of-linux-socket-balancing/">The Sad State of Linux Socket Balancing — Cloudflare</a> — a production war story about TCP socket architecture at scale, covering <code class="language-plaintext highlighter-rouge">SO_REUSEPORT</code>, NGINX worker balancing, and kernel-level accept queue behavior.</li> </ul> <hr/> <h2 id="7-full-data-path-application-to-hardware">7. Full Data Path: Application to Hardware</h2> <h3 id="message-passing-path-sendrecv">Message-Passing Path (send/recv)</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Application code builds payload
  → libc wrapper marshals syscall
  → CPU trap to kernel mode
  → Kernel validates user pointer, copies payload to kernel buffer
  → Socket/pipe subsystem queues data
  → Scheduler wakes receiver (or network softirq for TCP)
  → Transport/network layers process protocol state (TCP)
  → Memory subsystem and caches move data; coherence maintains visibility
  → Receiver syscall copies kernel buffer to user buffer
  → Return to user mode; application resumes
</code></pre></div></div> <h3 id="shared-memory-hot-path">Shared Memory Hot Path</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>One-time setup: shm_open + mmap (syscalls, happens once)
  → Per-message: writer stores payload
  → Release-store to atomic flag (ready = 1)
  → Reader acquire-loads flag
  → Reader loads payload directly
  → No per-message copy through kernel buffers
</code></pre></div></div> <p>The difference is stark. Message passing pays kernel transition cost on every single transfer. Shared memory pays it once at setup, then runs entirely in user space.</p> <hr/> <h2 id="8-benchmark-quantifying-the-trade-off">8. Benchmark: Quantifying the Trade-Off</h2> <h3 id="methodology">Methodology</h3> <p>100,000 messages × 64 bytes each, through all four mechanisms, parent/child process pair, timing via <code class="language-plaintext highlighter-rouge">CLOCK_MONOTONIC</code> with nanosecond precision. Each mechanism gets forked independently; parent measures wall time from first write to <code class="language-plaintext highlighter-rouge">wait(NULL)</code> return.</p> <h3 id="expected-results">Expected Results</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Shared Memory:   ~7.6M msgs/sec   (baseline)
Pipe:            ~1.7M msgs/sec   (~4.5x slower)
Unix Socket:     ~1.3M msgs/sec   (~5.8x slower)
TCP (localhost): ~1.1M msgs/sec   (~6.6x slower)
</code></pre></div></div> <h3 id="why-semaphore-based-shared-memory-initially-lost">Why Semaphore-Based Shared Memory Initially Lost</h3> <p>The first benchmark version used POSIX semaphores for shared memory coordination. Shared memory came out slower than pipes.</p> <p>The root cause: <code class="language-plaintext highlighter-rouge">sem_wait()</code> and <code class="language-plaintext highlighter-rouge">sem_post()</code> are syscalls. At 100K messages, semaphore-based shared memory adds 200K kernel transitions,precisely the overhead shared memory is supposed to eliminate. Synchronization overhead consumed the transport advantage entirely.</p> <p>This is the most important lesson in the benchmark. The mechanism you choose for synchronization can completely negate the mechanism you choose for transport.</p> <h3 id="why-spin--atomics-won">Why Spin + Atomics Won</h3> <p>Replacing semaphores with an atomic spin lock removed all syscalls from the hot path:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">struct</span> <span class="n">SpinSlot</span> <span class="p">{</span>
    <span class="k">volatile</span> <span class="kt">int</span> <span class="n">ready</span><span class="p">;</span>     <span class="c1">// 0 = writer can write, 1 = reader can read</span>
    <span class="kt">char</span> <span class="n">_pad1</span><span class="p">[</span><span class="mi">60</span><span class="p">];</span>         <span class="c1">// push data to next cache line</span>
    <span class="kt">char</span> <span class="n">data</span><span class="p">[</span><span class="n">MSG_SIZE</span><span class="p">];</span>    <span class="c1">// 64 bytes on its own cache line</span>
<span class="p">};</span>
</code></pre></div></div> <p>Writer:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">while</span> <span class="p">(</span><span class="n">__atomic_load_n</span><span class="p">(</span><span class="o">&amp;</span><span class="n">slot</span><span class="o">-&gt;</span><span class="n">ready</span><span class="p">,</span> <span class="n">__ATOMIC_ACQUIRE</span><span class="p">)</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">)</span> <span class="p">;</span>  <span class="c1">// spin</span>
<span class="n">memcpy</span><span class="p">(</span><span class="n">slot</span><span class="o">-&gt;</span><span class="n">data</span><span class="p">,</span> <span class="n">data</span><span class="p">,</span> <span class="n">MSG_SIZE</span><span class="p">);</span>
<span class="n">__atomic_store_n</span><span class="p">(</span><span class="o">&amp;</span><span class="n">slot</span><span class="o">-&gt;</span><span class="n">ready</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">__ATOMIC_RELEASE</span><span class="p">);</span>
</code></pre></div></div> <p>Reader:</p> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">while</span> <span class="p">(</span><span class="n">__atomic_load_n</span><span class="p">(</span><span class="o">&amp;</span><span class="n">slot</span><span class="o">-&gt;</span><span class="n">ready</span><span class="p">,</span> <span class="n">__ATOMIC_ACQUIRE</span><span class="p">)</span> <span class="o">==</span> <span class="mi">0</span><span class="p">)</span> <span class="p">;</span>  <span class="c1">// spin</span>
<span class="n">memcpy</span><span class="p">(</span><span class="n">buf</span><span class="p">,</span> <span class="n">slot</span><span class="o">-&gt;</span><span class="n">data</span><span class="p">,</span> <span class="n">MSG_SIZE</span><span class="p">);</span>
<span class="n">__atomic_store_n</span><span class="p">(</span><span class="o">&amp;</span><span class="n">slot</span><span class="o">-&gt;</span><span class="n">ready</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="n">__ATOMIC_RELEASE</span><span class="p">);</span>
</code></pre></div></div> <p>No syscalls anywhere in the hot path. The spin loops execute entirely in user space.</p> <p><code class="language-plaintext highlighter-rouge">__ATOMIC_ACQUIRE</code> / <code class="language-plaintext highlighter-rouge">__ATOMIC_RELEASE</code> ordering is sufficient here. A release store publishes all prior writes. An acquire load that observes the release guarantees subsequent reads see published data. This is weaker than <code class="language-plaintext highlighter-rouge">__ATOMIC_SEQ_CST</code> (full sequential consistency) but correct for a single-producer/single-consumer slot, and significantly cheaper because it doesn’t require a full memory barrier.</p> <p>The 60-byte padding between <code class="language-plaintext highlighter-rouge">ready</code> and <code class="language-plaintext highlighter-rouge">data</code> puts them on separate cache lines. Without this, the writer modifying <code class="language-plaintext highlighter-rouge">data</code> would invalidate the cache line holding <code class="language-plaintext highlighter-rouge">ready</code> on the reader’s core, forcing a cache-line transfer even when the reader is only polling the flag. False sharing would destroy the throughput gain from using atomics in the first place.</p> <p>Spin locks burn CPU cycles while waiting,100% core utilization during spin. In this benchmark, where producer and consumer run tightly coupled on separate cores, that’s acceptable. In production with variable message rates, it’s expensive. Know the trade-off before reaching for it.</p> <hr/> <h2 id="9-serialization-binary-vs-schema-driven">9. Serialization: Binary vs Schema-Driven</h2> <h3 id="raw-struct-transfer-pipe-shared-memory">Raw Struct Transfer (Pipe, Shared Memory)</h3> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">write</span><span class="p">(</span><span class="n">pipefd</span><span class="p">[</span><span class="mi">1</span><span class="p">],</span> <span class="o">&amp;</span><span class="n">msg</span><span class="p">,</span> <span class="k">sizeof</span><span class="p">(</span><span class="n">msg</span><span class="p">));</span>  <span class="c1">// pipe</span>
<span class="n">region</span><span class="o">-&gt;</span><span class="n">msg</span> <span class="o">=</span> <span class="n">data</span><span class="p">;</span>                    <span class="c1">// shared memory</span>
</code></pre></div></div> <p>Zero serialization overhead. The bytes in memory are the bytes transferred.</p> <p>Risks:</p> <ul> <li>Struct layout must match exactly (same types, order, padding, alignment)</li> <li>Compiler version, flags (<code class="language-plaintext highlighter-rouge">-fpack-struct</code>, alignment pragmas) can silently change layout</li> <li>Adding or removing fields requires recompiling both sides simultaneously</li> </ul> <h3 id="json-serialization-socket-demos">JSON Serialization (Socket Demos)</h3> <div class="language-c highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">snprintf</span><span class="p">(</span><span class="n">msg</span><span class="p">,</span> <span class="k">sizeof</span><span class="p">(</span><span class="n">msg</span><span class="p">),</span>
    <span class="s">"{</span><span class="se">\"</span><span class="s">seq</span><span class="se">\"</span><span class="s">:%d,</span><span class="se">\"</span><span class="s">type</span><span class="se">\"</span><span class="s">:%d,</span><span class="se">\"</span><span class="s">value</span><span class="se">\"</span><span class="s">:%.4f,</span><span class="se">\"</span><span class="s">text</span><span class="se">\"</span><span class="s">:</span><span class="se">\"</span><span class="s">msg #%d</span><span class="se">\"</span><span class="s">}"</span><span class="p">,</span>
    <span class="n">i</span> <span class="o">+</span> <span class="mi">1</span><span class="p">,</span> <span class="n">i</span> <span class="o">%</span> <span class="mi">3</span><span class="p">,</span> <span class="p">(</span><span class="n">i</span> <span class="o">+</span> <span class="mi">1</span><span class="p">)</span> <span class="o">*</span> <span class="mi">2</span><span class="p">.</span><span class="mi">236</span><span class="p">,</span> <span class="n">i</span> <span class="o">+</span> <span class="mi">1</span><span class="p">);</span>
</code></pre></div></div> <p>Self-describing, tolerant of schema evolution, no struct layout dependency. New fields don’t break old readers.</p> <p>Cost: CPU time for formatting and parsing, larger payload, string escaping edge cases.</p> <p>This mirrors real-world practice cleanly. Shared memory systems,database buffer pools, HFT engines,use binary formats. Distributed systems, REST APIs, microservices, use serialized formats. The choice is architecture, not just performance.</p> <hr/> <h2 id="10-the-cross-host-boundary">10. The Cross-Host Boundary</h2> <p>Shared memory requires two processes to map the same physical RAM. That’s only possible on one machine. There is no workaround.</p> <p>Message passing doesn’t care where the processes live.</p> <p>On the same host, the kernel copies between address spaces. Fast enough for most things.</p> <p>Across hosts:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Process A (Host 1)
      |
      v
  Kernel (Host 1)
      |
   TCP/IP Stack
      |
   Network
      |
   TCP/IP Stack
      |
  Kernel (Host 2)
      |
      v
Process B (Host 2)
</code></pre></div></div> <p>Network round-trips add latency measured in milliseconds. Local IPC runs in microseconds. That’s a real difference.</p> <p>But if your processes run on different machines, you don’t get to choose. Shared memory doesn’t cross machine boundaries. TCP does.</p> <hr/> <h2 id="11-nodejs-high-level-language-low-level-mechanics">11. Node.js: High-Level Language, Low-Level Mechanics</h2> <p>Node.js looks high level. It’s not.</p> <h3 id="child_processfork-and-ipc">child_process.fork() and IPC</h3> <p><code class="language-plaintext highlighter-rouge">fork()</code> in Node creates a new Node process and establishes an IPC channel to the parent. That channel is implemented with OS primitives, typically pipes or domain sockets depending on platform.</p> <p>When JavaScript calls <code class="language-plaintext highlighter-rouge">process.send(obj)</code>:</p> <ol> <li>V8 serializes the object to a transportable binary representation</li> <li>Node writes framed bytes to the IPC channel</li> <li>Kernel copies bytes through pipe/socket buffers</li> <li>Child event loop reads and deserializes to JS object</li> </ol> <p>No JS heap pointer sharing occurs across processes. Isolation stays intact. The serialization is what makes cross-process messaging safe in a language that doesn’t expose raw memory.</p> <h3 id="libuv-and-the-event-loop">libuv and the Event Loop</h3> <p>Node’s non-blocking I/O is driven by libuv:</p> <ol> <li>Register interest in socket readability/writability</li> <li>Kernel notifies readiness via event demultiplexer (<code class="language-plaintext highlighter-rouge">kqueue</code> on macOS, <code class="language-plaintext highlighter-rouge">epoll</code> on Linux)</li> <li>libuv schedules JS callbacks when I/O can proceed</li> </ol> <p>An Express route that handles a socket request maps to:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>JavaScript handler
  → C++ Node bindings
  → libuv
  → syscalls (socket, bind, listen, accept, send, recv)
  → kernel networking stack
</code></pre></div></div> <p>“High-level runtime” means “layered systems interface,” not “no kernel.”</p> <h3 id="express-route-to-syscall-chain">Express Route to Syscall Chain</h3> <p>A route like <code class="language-plaintext highlighter-rouge">/demos/tcp-socket</code> in this project:</p> <ol> <li><code class="language-plaintext highlighter-rouge">spawn(server)</code> — background, capture stdout</li> <li><code class="language-plaintext highlighter-rouge">setTimeout(500ms)</code> — wait for server to bind and listen</li> <li><code class="language-plaintext highlighter-rouge">execSync(client)</code> — run client to completion, capture output</li> <li><code class="language-plaintext highlighter-rouge">server.kill(SIGTERM)</code> — cleanup</li> </ol> <p>That 500ms delay is empirical. Enough for <code class="language-plaintext highlighter-rouge">bind + listen</code> on localhost. The C server must be listening before the client connects. If you shorten it on a slow CI machine, the client gets <code class="language-plaintext highlighter-rouge">ECONNREFUSED</code> and the test fails in ways that look flaky.</p> <h3 id="newline-delimited-json">Newline-Delimited JSON</h3> <p>Both Unix socket and TCP Node.js demos use newline-delimited JSON: each message is a JSON object followed by <code class="language-plaintext highlighter-rouge">\n</code>. The receiver splits on newlines to recover individual messages.</p> <p>This solves TCP stream coalescing. TCP is a byte stream. Multiple <code class="language-plaintext highlighter-rouge">write()</code> calls may arrive in one <code class="language-plaintext highlighter-rouge">data</code> event. Without delimiters, you can’t tell where one message ends and the next begins. Newlines are simple and sufficient for most cases. Length-prefixed binary framing is faster but more code.</p> <hr/> <h2 id="12-pitfalls-and-failure-modes">12. Pitfalls and Failure Modes</h2> <p><strong>Stale shared memory and semaphores</strong>: POSIX shared memory objects and named semaphores persist after process exit. A crashed previous run leaves stale objects with wrong state. Always call <code class="language-plaintext highlighter-rouge">shm_unlink</code> and <code class="language-plaintext highlighter-rouge">sem_unlink</code> at startup — treat it as a reset.</p> <p><strong>Struct layout mismatch</strong>: Different compilers, flags, or architectures can change struct alignment silently. The reader doesn’t segfault. It reads the wrong bytes at the wrong offsets and produces wrong values. No warning. This is why both sides must compile from the same source under the same conditions.</p> <p><strong>TCP message coalescing</strong>: Two <code class="language-plaintext highlighter-rouge">send()</code> calls can arrive as one <code class="language-plaintext highlighter-rouge">recv()</code>. Always use delimited protocols or fixed-size messages. Never assume one send = one receive.</p> <p><strong>Port conflicts (EADDRINUSE)</strong>: Use <code class="language-plaintext highlighter-rouge">SO_REUSEADDR</code> on the C server to handle <code class="language-plaintext highlighter-rouge">TIME_WAIT</code>. Use <code class="language-plaintext highlighter-rouge">port 0</code> in Node.js to let the OS assign an available port and avoid collisions.</p> <p><strong>Unix socket file cleanup</strong>: The socket file persists on the filesystem after a crash. Next run fails on <code class="language-plaintext highlighter-rouge">bind()</code>. Unlink the path at startup before binding.</p> <p><strong>False sharing</strong>: If the spin flag and payload data share a cache line, the writer modifying data invalidates the reader’s cached flag on every write — even though the reader only cares about the flag. Pad them to separate cache lines. 60 bytes of padding between a 4-byte flag and a 64-byte payload is not waste; it’s the difference between 7.6M msgs/sec and a fraction of that.</p> <p><strong>Semaphores masking shared memory performance</strong>: If your benchmark shows shared memory losing to pipes, check your synchronization. Per-message semaphore calls reintroduce the kernel transitions you were trying to avoid. The mechanism and its synchronization strategy must be evaluated together.</p> <hr/> <h2 id="13-design-decisions">13. Design Decisions</h2> <table> <thead> <tr> <th>Decision</th> <th>Rationale</th> </tr> </thead> <tbody> <tr> <td>Semaphores for demos, spin locks for benchmark</td> <td>Semaphores are pedagogically clear; spin locks prove the performance hypothesis</td> </tr> <tr> <td>JSON for socket demos, raw structs for pipe/shm</td> <td>Mirrors real-world: local binary vs network serialized</td> </tr> <tr> <td>64-byte struct padding</td> <td>Matches typical x86/ARM cache line; prevents false sharing</td> </tr> <tr> <td>Named semaphores (not unnamed)</td> <td>Required for coordination between independent processes (not just threads)</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">shm_unlink</code> at writer startup</td> <td>Clears stale state from previous crashed runs</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">port 0</code> for Node.js TCP</td> <td>OS assigns available ephemeral port; avoids EADDRINUSE</td> </tr> <tr> <td>Newline-delimited JSON for Node.js socket demos</td> <td>Solves TCP stream coalescing</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">SO_REUSEADDR</code> on C TCP server</td> <td>Allows restart during TIME_WAIT</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">CLOCK_MONOTONIC</code> for timing</td> <td>Not affected by NTP adjustments or wall-clock changes</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">__ATOMIC_ACQUIRE</code>/<code class="language-plaintext highlighter-rouge">__ATOMIC_RELEASE</code> (not <code class="language-plaintext highlighter-rouge">SEQ_CST</code>)</td> <td>Sufficient for single-producer/single-consumer; no full memory barrier needed</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">INADDR_LOOPBACK</code> not <code class="language-plaintext highlighter-rouge">INADDR_ANY</code></td> <td>Security: demo/benchmark should not accept external connections</td> </tr> </tbody> </table> <hr/> <h2 id="14-when-to-use-each">14. When to Use Each</h2> <p><strong>Shared memory</strong>: Both processes on the same machine, you control both sides, you need every microsecond, and you’re willing to own synchronization, layout, and cache behavior. High-frequency trading engines and database buffer pools live here.</p> <p><strong>Pipes</strong>: Simple unidirectional data flow between related processes, usually parent/child. Correct by default. Not much ceremony. Good starting point before you need more.</p> <p><strong>Unix domain sockets</strong>: Local multi-process service decomposition where you want full-duplex communication, connection semantics, or unrelated processes. The sweet spot between pipes and TCP for same-host work.</p> <p><strong>TCP sockets</strong>: Any time processes might run on different hosts — or might move there. The only option for distributed systems. You pay for the protocol overhead, but you buy reliability, routing, and reach that no other mechanism provides.</p> <hr/> <h2 id="15-what-the-benchmark-actually-teaches">15. What the Benchmark Actually Teaches</h2> <p>The headline result is “shared memory is fastest.” That’s true but incomplete.</p> <p>The deeper result is to show that the shared memory with semaphores lost to pipes. Adding spin atomics made it win again. The synchronization strategy dominated the transport mechanism choice.</p> <p>The benchmark is only valid because the synchronization and measurement methodology match the question being asked. A hidden syscall in your sync path can invalidate your entire conclusion. When someone shows you a benchmark, the first question is: what exactly is being measured?</p> <p>Benchmarks must measure the mechanism you think you’re measuring.</p> <p>Operating systems are a set of explicit trade-offs among safety, portability, debuggability, and raw speed. IPC mechanisms are where those trade-offs become numbers. That’s what makes IPC worth understanding at this depth — not to memorize the syscall sequence for <code class="language-plaintext highlighter-rouge">shm_open</code>, but to build the intuition for what each layer costs, why it costs that, and when the cost is worth paying.</p> <p>High-level runtimes don’t remove OS fundamentals. They package them. The kernel is always there.</p> <p>Repository for this article: <a href="https://github.com/karankessy/benchmarkIPC">benchmarkIPC</a>.</p>]]></content><author><name></name></author><category term="systems"/><category term="systems"/><category term="networking"/><summary type="html"><![CDATA[Modern software is rarely a single process doing one thing. Browsers isolate tabs for security. Databases split query workers from storage workers. API backends offload CPU-heavy jobs to background processes. The moment you divide work across processes, you need them to talk to each other.]]></summary></entry><entry><title type="html">Why Your API Works in Postman but Fails in the Browser</title><link href="https://karankessy.github.io/blog/2026/understanding-cors/" rel="alternate" type="text/html" title="Why Your API Works in Postman but Fails in the Browser"/><published>2026-03-15T10:00:00+00:00</published><updated>2026-03-15T10:00:00+00:00</updated><id>https://karankessy.github.io/blog/2026/understanding-cors</id><content type="html" xml:base="https://karankessy.github.io/blog/2026/understanding-cors/"><![CDATA[<p>If you’ve ever built a modern web application, you’ve probably stared at a CORS error in your browser console wondering why your perfectly fine API call works in Postman but dies in the browser. This article breaks down exactly what’s happening under the hood - from the Same-Origin Policy’s origins in 1995, through preflight mechanics, credentials, common misconfigurations, and real-world deployment patterns.</p> <p>I’ve also put together a hands-on demo project - <a href="https://github.com/karankessy/demoCORS"><strong>demoCORS</strong></a> - that you can clone and run locally to see every concept in action.</p> <hr/> <h2 id="1-the-same-origin-policy-sop">1. The Same-Origin Policy (SOP)</h2> <h3 id="why-it-exists">Why it exists</h3> <p>The year is 1995. Netscape Navigator 2.0 introduces JavaScript into the browser. For the first time, code running in one browser tab can manipulate the DOM, read form data, and make HTTP requests. Immediately, a problem emerges: if a user has two tabs open - their bank at <code class="language-plaintext highlighter-rouge">bank.com</code> and a malicious site at <code class="language-plaintext highlighter-rouge">evil.com</code> - the malicious site’s JavaScript could read data from the bank tab.</p> <p>The <strong>Same-Origin Policy</strong> was introduced to prevent this. It is the foundational security boundary of the web platform: <strong>code from one origin cannot read data from a different origin.</strong></p> <p>It’s important to understand what SOP does NOT do: it does not prevent requests from being <em>sent</em>. It prevents responses from being <em>read</em>. A page on <code class="language-plaintext highlighter-rouge">evil.com</code> can trigger a GET request to <code class="language-plaintext highlighter-rouge">bank.com</code>, and that request will arrive at the bank’s server (potentially with cookies). What SOP prevents is <code class="language-plaintext highlighter-rouge">evil.com</code>’s JavaScript from reading the bank’s response.</p> <h3 id="what-is-an-origin">What is an “origin”?</h3> <p>An origin is defined by three components:</p> <blockquote> <p><strong>Origin = scheme + host + port</strong></p> </blockquote> <p>Let’s examine concrete examples:</p> <table> <thead> <tr> <th>URL</th> <th>Scheme</th> <th>Host</th> <th>Port</th> <th>Origin</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">https://example.com</code></td> <td><code class="language-plaintext highlighter-rouge">https</code></td> <td><code class="language-plaintext highlighter-rouge">example.com</code></td> <td><code class="language-plaintext highlighter-rouge">443</code> (implicit)</td> <td><code class="language-plaintext highlighter-rouge">https://example.com</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">https://api.example.com</code></td> <td><code class="language-plaintext highlighter-rouge">https</code></td> <td><code class="language-plaintext highlighter-rouge">api.example.com</code></td> <td><code class="language-plaintext highlighter-rouge">443</code></td> <td><code class="language-plaintext highlighter-rouge">https://api.example.com</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">http://example.com</code></td> <td><code class="language-plaintext highlighter-rouge">http</code></td> <td><code class="language-plaintext highlighter-rouge">example.com</code></td> <td><code class="language-plaintext highlighter-rouge">80</code></td> <td><code class="language-plaintext highlighter-rouge">http://example.com</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">https://example.com:8080</code></td> <td><code class="language-plaintext highlighter-rouge">https</code></td> <td><code class="language-plaintext highlighter-rouge">example.com</code></td> <td><code class="language-plaintext highlighter-rouge">8080</code></td> <td><code class="language-plaintext highlighter-rouge">https://example.com:8080</code></td> </tr> </tbody> </table> <p>Every single one of these is a <strong>different origin</strong>:</p> <ul> <li>Row 1 vs Row 2: different <strong>host</strong> (<code class="language-plaintext highlighter-rouge">example.com</code> vs <code class="language-plaintext highlighter-rouge">api.example.com</code> - subdomains are different hosts)</li> <li>Row 1 vs Row 3: different <strong>scheme</strong> (<code class="language-plaintext highlighter-rouge">https</code> vs <code class="language-plaintext highlighter-rouge">http</code>)</li> <li>Row 1 vs Row 4: different <strong>port</strong> (<code class="language-plaintext highlighter-rouge">443</code> vs <code class="language-plaintext highlighter-rouge">8080</code>)</li> </ul> <p>This is exactly the principle at work in the <a href="https://github.com/karankessy/demoCORS">demoCORS</a> project: <code class="language-plaintext highlighter-rouge">http://localhost:3000</code> and <code class="language-plaintext highlighter-rouge">http://localhost:4000</code> differ in port, making them cross-origin.</p> <h3 id="what-sop-blocks">What SOP blocks</h3> <p>SOP restricts JavaScript’s ability to read cross-origin responses from:</p> <ul> <li><code class="language-plaintext highlighter-rouge">fetch()</code> and <code class="language-plaintext highlighter-rouge">XMLHttpRequest</code></li> <li>Reading <code class="language-plaintext highlighter-rouge">&lt;canvas&gt;</code> pixels painted from cross-origin images</li> <li>Accessing <code class="language-plaintext highlighter-rouge">contentDocument</code> of cross-origin <code class="language-plaintext highlighter-rouge">&lt;iframe&gt;</code>s</li> <li>Reading responses from cross-origin <code class="language-plaintext highlighter-rouge">EventSource</code> (SSE)</li> </ul> <p>SOP does <strong>NOT</strong> block:</p> <ul> <li><code class="language-plaintext highlighter-rouge">&lt;img src="..."&gt;</code> - images load cross-origin (but you can’t read their pixels)</li> <li><code class="language-plaintext highlighter-rouge">&lt;script src="..."&gt;</code> - scripts load and execute cross-origin (the origin of JSONP and supply-chain attacks)</li> <li><code class="language-plaintext highlighter-rouge">&lt;link rel="stylesheet" href="..."&gt;</code> - stylesheets load cross-origin</li> <li><code class="language-plaintext highlighter-rouge">&lt;form action="..."&gt;</code> - forms can submit cross-origin (the origin of CSRF attacks)</li> <li><code class="language-plaintext highlighter-rouge">&lt;iframe src="..."&gt;</code> - iframes load cross-origin (but parent can’t read content)</li> </ul> <hr/> <h2 id="2-why-cors-exists">2. Why CORS Exists</h2> <p>SOP was designed for a 1990s web where pages were self-contained documents. The modern web is fundamentally different:</p> <ul> <li><strong>Single-Page Applications (SPAs)</strong> serve a static frontend from <code class="language-plaintext highlighter-rouge">app.example.com</code> and call APIs at <code class="language-plaintext highlighter-rouge">api.example.com</code></li> <li><strong>Microservices</strong> expose multiple APIs on different domains or ports</li> <li><strong>Third-party integrations</strong> require calling payment processors, analytics services, CDNs</li> <li><strong>Development environments</strong> run frontends and backends on different ports (exactly like <a href="https://github.com/karankessy/demoCORS">demoCORS</a>: port 3000 and port 4000)</li> </ul> <p>Without any mechanism to relax SOP, none of these architectures would work. You’d be forced to same-origin-proxy every API call through your frontend server.</p> <p><strong>CORS (Cross-Origin Resource Sharing)</strong> is that mechanism. It is a <strong>protocol</strong> - a set of HTTP headers - that allows a server to declare: “I trust requests from these specific origins.”</p> <p>The key insight is: <strong>CORS is not a way to add security. It’s a controlled way to remove a security restriction.</strong> SOP is the default. CORS is the opt-out.</p> <hr/> <h2 id="3-cors-request-types">3. CORS Request Types</h2> <p>The CORS specification divides cross-origin requests into two categories based on their potential to cause side effects.</p> <h3 id="simple-requests">Simple Requests</h3> <p>A request is “simple” (formally a request that doesn’t trigger a preflight) when ALL of these conditions are met:</p> <p><strong>Method</strong> is one of:</p> <ul> <li><code class="language-plaintext highlighter-rouge">GET</code></li> <li><code class="language-plaintext highlighter-rouge">HEAD</code></li> <li><code class="language-plaintext highlighter-rouge">POST</code></li> </ul> <p><strong>Headers</strong> are limited to the CORS-safelisted set:</p> <ul> <li><code class="language-plaintext highlighter-rouge">Accept</code></li> <li><code class="language-plaintext highlighter-rouge">Accept-Language</code></li> <li><code class="language-plaintext highlighter-rouge">Content-Language</code></li> <li><code class="language-plaintext highlighter-rouge">Content-Type</code> (with restrictions, see below)</li> <li><code class="language-plaintext highlighter-rouge">Range</code> (with restrictions)</li> </ul> <p><strong>Content-Type</strong> (if present) is one of:</p> <ul> <li><code class="language-plaintext highlighter-rouge">application/x-www-form-urlencoded</code></li> <li><code class="language-plaintext highlighter-rouge">multipart/form-data</code></li> <li><code class="language-plaintext highlighter-rouge">text/plain</code></li> </ul> <p><strong>No</strong> <code class="language-plaintext highlighter-rouge">ReadableStream</code> body is used. <strong>No</strong> event listeners are registered on the <code class="language-plaintext highlighter-rouge">XMLHttpRequest.upload</code> object.</p> <p><strong>Why these conditions?</strong> Because a simple request is no more dangerous than what an HTML <code class="language-plaintext highlighter-rouge">&lt;form&gt;</code> can already do. Forms can POST with <code class="language-plaintext highlighter-rouge">application/x-www-form-urlencoded</code> or <code class="language-plaintext highlighter-rouge">multipart/form-data</code> to any URL. SOP never blocked form submissions. So a “simple” CORS request adds no new attack surface - the browser just needs to check whether the server wants the <em>response</em> shared.</p> <p>In the <a href="https://github.com/karankessy/demoCORS">demoCORS</a> project, the <code class="language-plaintext highlighter-rouge">fetch()</code> calls are simple GET requests with no custom headers:</p> <div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">await</span> <span class="nf">fetch</span><span class="p">(</span><span class="dl">"</span><span class="s2">http://localhost:4000/api/data</span><span class="dl">"</span><span class="p">);</span>
</code></pre></div></div> <p>This meets all simple request criteria, so <strong>no preflight occurs</strong>.</p> <h3 id="preflighted-requests">Preflighted Requests</h3> <p>Any request that doesn’t meet the “simple” criteria triggers a <strong>preflight</strong>. Common triggers include:</p> <ul> <li>Methods: <code class="language-plaintext highlighter-rouge">PUT</code>, <code class="language-plaintext highlighter-rouge">DELETE</code>, <code class="language-plaintext highlighter-rouge">PATCH</code></li> <li>Headers: <code class="language-plaintext highlighter-rouge">Authorization</code>, <code class="language-plaintext highlighter-rouge">X-Custom-Header</code>, any custom header</li> <li>Content-Type: <code class="language-plaintext highlighter-rouge">application/json</code></li> </ul> <p>Before sending the actual request, the browser sends an <code class="language-plaintext highlighter-rouge">OPTIONS</code> request asking the server: “Will you accept this kind of request?”</p> <p>The reason for preflight is safety: methods like <code class="language-plaintext highlighter-rouge">DELETE</code> or headers like <code class="language-plaintext highlighter-rouge">Authorization</code> can cause <strong>side effects</strong> on the server. The browser needs to verify the server actually expects these cross-origin requests <em>before</em> sending them, because unlike simple requests, these operations couldn’t be triggered by a plain HTML <code class="language-plaintext highlighter-rouge">&lt;form&gt;</code>.</p> <h3 id="full-http-flow---preflighted-request">Full HTTP Flow - Preflighted Request</h3> <p><strong>Scenario:</strong> A frontend at <code class="language-plaintext highlighter-rouge">https://app.example.com</code> sends a JSON POST to <code class="language-plaintext highlighter-rouge">https://api.example.com</code>.</p> <h4 id="step-1-preflight-request-browser--server">Step 1: Preflight Request (Browser → Server)</h4> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>OPTIONS /api/users HTTP/1.1
Host: api.example.com
Origin: https://app.example.com
Access-Control-Request-Method: POST
Access-Control-Request-Headers: content-type, authorization
</code></pre></div></div> <h4 id="step-2-preflight-response-server--browser">Step 2: Preflight Response (Server → Browser)</h4> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>HTTP/1.1 204 No Content
Access-Control-Allow-Origin: https://app.example.com
Access-Control-Allow-Methods: GET, POST, PUT, DELETE
Access-Control-Allow-Headers: content-type, authorization
Access-Control-Max-Age: 86400
</code></pre></div></div> <h4 id="step-3-actual-request-browser--server">Step 3: Actual Request (Browser → Server)</h4> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>POST /api/users HTTP/1.1
Host: api.example.com
Origin: https://app.example.com
Content-Type: application/json
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9...

{"name": "Karan", "role": "Developer"}
</code></pre></div></div> <h4 id="step-4-actual-response-server--browser">Step 4: Actual Response (Server → Browser)</h4> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>HTTP/1.1 201 Created
Access-Control-Allow-Origin: https://app.example.com
Content-Type: application/json

{"id": 42, "name": "Karan", "role": "Developer"}
</code></pre></div></div> <p>If the preflight response at Step 2 were missing <code class="language-plaintext highlighter-rouge">authorization</code> in <code class="language-plaintext highlighter-rouge">Access-Control-Allow-Headers</code>, the browser would <strong>never send Step 3</strong>. The actual request is completely blocked before it leaves the browser.</p> <hr/> <h2 id="4-cors-request-flow---step-by-step">4. CORS Request Flow - Step by Step</h2> <p>Here’s the complete decision tree the browser follows for every <code class="language-plaintext highlighter-rouge">fetch()</code> call:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>JavaScript calls fetch(url, options)
         │
         ▼
┌─────────────────────────┐
│ Is the target origin the │
│ same as the page origin? │
└───────┬─────────┬────────┘
        │ Yes     │ No
        ▼         ▼
  [Normal HTTP  ┌──────────────────────┐
   request,     │ Is this a "simple"   │
   no CORS]     │ request?             │
                └──────┬────────┬──────┘
                       │ Yes    │ No
                       ▼        ▼
               [Send request  [Send OPTIONS
                directly with  preflight first]
                Origin header]       │
                       │             ▼
                       │      ┌───────────────────┐
                       │      │ Does preflight     │
                       │      │ response approve?  │
                       │      └──┬──────────┬──────┘
                       │         │ Yes      │ No
                       │         ▼          ▼
                       │    [Send actual  [Block. TypeError
                       │     request]      in JavaScript]
                       │         │
                       ▼         ▼
               ┌───────────────────────┐
               │ Does response contain │
               │ valid CORS headers?   │
               └──────┬─────────┬──────┘
                      │ Yes     │ No
                      ▼         ▼
              [Expose response  [Block response.
               to JavaScript]   TypeError in
                                JavaScript]
</code></pre></div></div> <h3 id="the-critical-misconception">The critical misconception</h3> <blockquote> <p><strong>CORS does not prevent the request from reaching the server. It only controls whether the browser exposes the response to JavaScript.</strong></p> </blockquote> <p>For simple requests, the server always receives and processes the request. The <code class="language-plaintext highlighter-rouge">Origin</code> header is present, but the server responds normally. Only then does the browser decide whether to hand the response to JavaScript.</p> <p>This means <strong>CORS is not a substitute for server-side authorization</strong>. If your server processes a <code class="language-plaintext highlighter-rouge">DELETE /api/users/42</code> and only then sends back a response missing CORS headers, the record is already deleted - the browser just won’t show the confirmation to the attacker’s JavaScript.</p> <p>For preflighted requests, the protection is stronger: the browser’s OPTIONS preflight happens first, and if it fails, the actual <code class="language-plaintext highlighter-rouge">DELETE</code> is never sent. But you should <strong>never</strong> rely on preflight as your security boundary.</p> <hr/> <h2 id="5-preflight-mechanism---deep-dive">5. Preflight Mechanism - Deep Dive</h2> <h3 id="the-options-request">The OPTIONS request</h3> <p>When a browser determines that a request requires preflight, it constructs an <code class="language-plaintext highlighter-rouge">OPTIONS</code> request automatically. JavaScript code has <strong>no control</strong> over this - no ability to modify, suppress, or intercept the preflight.</p> <p><strong>Example preflight request:</strong></p> <div class="language-http highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">OPTIONS</span> <span class="nn">/api/data</span> <span class="k">HTTP</span><span class="o">/</span><span class="m">1.1</span>
<span class="na">Host</span><span class="p">:</span> <span class="s">api.example.com</span>
<span class="na">Origin</span><span class="p">:</span> <span class="s">https://app.example.com</span>
<span class="na">Access-Control-Request-Method</span><span class="p">:</span> <span class="s">POST</span>
<span class="na">Access-Control-Request-Headers</span><span class="p">:</span> <span class="s">authorization, content-type</span>
</code></pre></div></div> <p>Key headers:</p> <ul> <li><code class="language-plaintext highlighter-rouge">Origin</code>: The requesting page’s origin (always present)</li> <li><code class="language-plaintext highlighter-rouge">Access-Control-Request-Method</code>: The HTTP method the <em>actual</em> request will use</li> <li><code class="language-plaintext highlighter-rouge">Access-Control-Request-Headers</code>: The non-safelisted headers the <em>actual</em> request will include</li> </ul> <h3 id="expected-server-response">Expected server response</h3> <p>The server must respond to the OPTIONS request with:</p> <div class="language-http highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">HTTP</span><span class="o">/</span><span class="m">1.1</span> <span class="m">204</span> <span class="ne">No Content</span>
<span class="na">Access-Control-Allow-Origin</span><span class="p">:</span> <span class="s">https://app.example.com</span>
<span class="na">Access-Control-Allow-Methods</span><span class="p">:</span> <span class="s">GET, POST, PUT, DELETE</span>
<span class="na">Access-Control-Allow-Headers</span><span class="p">:</span> <span class="s">authorization, content-type</span>
<span class="na">Access-Control-Max-Age</span><span class="p">:</span> <span class="s">86400</span>
</code></pre></div></div> <h3 id="what-happens-when-headers-are-missing">What happens when headers are missing</h3> <table> <thead> <tr> <th>Missing header</th> <th>Result</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">Access-Control-Allow-Origin</code></td> <td>Preflight fails. Actual request never sent.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">Access-Control-Allow-Methods</code> (doesn’t include requested method)</td> <td>Preflight fails.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">Access-Control-Allow-Headers</code> (doesn’t include requested header)</td> <td>Preflight fails.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">Access-Control-Max-Age</code></td> <td>Preflight succeeds but browser repeats OPTIONS for every request.</td> </tr> </tbody> </table> <h3 id="preflight-caching">Preflight caching</h3> <p><code class="language-plaintext highlighter-rouge">Access-Control-Max-Age</code> tells the browser how many seconds to cache the preflight result. During that window, subsequent requests to the same URL with compatible methods and headers skip the OPTIONS check entirely.</p> <p><strong>Without caching, every single API call generates two HTTP requests</strong> - a significant performance tax. In the <a href="https://github.com/karankessy/demoCORS">demoCORS</a> project, <code class="language-plaintext highlighter-rouge">Max-Age</code> is not configured, meaning every preflight is re-sent. In production, values of <code class="language-plaintext highlighter-rouge">86400</code> (24 hours) or <code class="language-plaintext highlighter-rouge">3600</code> (1 hour) are typical.</p> <p>Note: Browsers impose their own maximum. Chrome caps at 7200 seconds (2 hours), regardless of what the server sends.</p> <hr/> <h2 id="6-cors-headers-explained---in-depth">6. CORS Headers Explained - In Depth</h2> <h3 id="access-control-allow-origin"><code class="language-plaintext highlighter-rouge">Access-Control-Allow-Origin</code></h3> <p><strong>Purpose:</strong> The single most important CORS header. Declares which origin(s) may read the response.</p> <p><strong>Valid values:</strong></p> <ul> <li><code class="language-plaintext highlighter-rouge">*</code> - Any origin (but cannot be used with credentials)</li> <li><code class="language-plaintext highlighter-rouge">https://specific-origin.com</code> - Exactly one origin</li> <li>(There is <strong>no</strong> native support for multiple origins in a single header value)</li> </ul> <p><strong>The multiple-origin problem:</strong> You cannot write:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Access-Control-Allow-Origin: https://a.com, https://b.com   ❌ INVALID
</code></pre></div></div> <p>The spec only allows a single origin or <code class="language-plaintext highlighter-rouge">*</code>. To support multiple origins, the server must:</p> <ol> <li>Read the <code class="language-plaintext highlighter-rouge">Origin</code> request header</li> <li>Check it against an allowlist</li> <li>If approved, reflect that specific origin in the response</li> <li>Set <code class="language-plaintext highlighter-rouge">Vary: Origin</code> to prevent cache poisoning</li> </ol> <p>This is exactly what the <code class="language-plaintext highlighter-rouge">cors</code> npm middleware does when given an array or function for the <code class="language-plaintext highlighter-rouge">origin</code> option.</p> <p><strong>Edge case:</strong> If the server sets <code class="language-plaintext highlighter-rouge">Access-Control-Allow-Origin: null</code>, the browser will match it against the <code class="language-plaintext highlighter-rouge">null</code> origin (used by sandboxed iframes, <code class="language-plaintext highlighter-rouge">file://</code> URLs, and redirected requests). This is dangerous because an attacker can craft a sandboxed iframe with <code class="language-plaintext highlighter-rouge">Origin: null</code> and exploit this match.</p> <h3 id="access-control-allow-methods"><code class="language-plaintext highlighter-rouge">Access-Control-Allow-Methods</code></h3> <p><strong>Purpose:</strong> Sent only in preflight responses. Lists the HTTP methods the server accepts.</p> <p><strong>Note:</strong> For simple requests (GET, HEAD, POST), this header is not checked - the browser already knows those methods are inherently acceptable at the CORS level.</p> <p><strong>Example:</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Access-Control-Allow-Methods: GET, POST, PUT, DELETE, PATCH
</code></pre></div></div> <h3 id="access-control-allow-headers"><code class="language-plaintext highlighter-rouge">Access-Control-Allow-Headers</code></h3> <p><strong>Purpose:</strong> Sent only in preflight responses. Lists the request headers the server accepts beyond the CORS-safelisted set.</p> <p><strong>Example:</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Access-Control-Allow-Headers: Authorization, Content-Type, X-Request-ID
</code></pre></div></div> <p><strong>Important:</strong> As of the 2023 Fetch specification, <code class="language-plaintext highlighter-rouge">*</code> is a valid value for this header (and <code class="language-plaintext highlighter-rouge">Access-Control-Allow-Methods</code>), meaning “all headers/methods are allowed.” However, this wildcard <strong>does not apply</strong> when <code class="language-plaintext highlighter-rouge">credentials: true</code> - in that case, every header must be explicitly listed.</p> <h3 id="access-control-allow-credentials"><code class="language-plaintext highlighter-rouge">Access-Control-Allow-Credentials</code></h3> <p><strong>Purpose:</strong> Tells the browser to include cookies, <code class="language-plaintext highlighter-rouge">Authorization</code> headers, and TLS client certificates in cross-origin requests.</p> <p><strong>The cardinal rule:</strong></p> <blockquote> <p>When <code class="language-plaintext highlighter-rouge">Access-Control-Allow-Credentials: true</code> is set, <code class="language-plaintext highlighter-rouge">Access-Control-Allow-Origin</code> <strong>MUST NOT</strong> be <code class="language-plaintext highlighter-rouge">*</code>.</p> </blockquote> <p>Browsers enforce this strictly. If a server sends both <code class="language-plaintext highlighter-rouge">Access-Control-Allow-Origin: *</code> and <code class="language-plaintext highlighter-rouge">Access-Control-Allow-Credentials: true</code>, the browser rejects the response. This prevents a server from accidentally exposing authenticated data to every site on the internet.</p> <p><strong>Client side:</strong> The <code class="language-plaintext highlighter-rouge">fetch()</code> call must also opt in:</p> <div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">fetch</span><span class="p">(</span><span class="nx">url</span><span class="p">,</span> <span class="p">{</span> <span class="na">credentials</span><span class="p">:</span> <span class="dl">"</span><span class="s2">include</span><span class="dl">"</span> <span class="p">});</span>
</code></pre></div></div> <p>Without <code class="language-plaintext highlighter-rouge">credentials: 'include'</code>, the browser won’t send cookies even if the server allows them.</p> <h3 id="access-control-max-age"><code class="language-plaintext highlighter-rouge">Access-Control-Max-Age</code></h3> <p><strong>Purpose:</strong> Duration (in seconds) the browser may cache the preflight response.</p> <p><strong>Example:</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Access-Control-Max-Age: 86400
</code></pre></div></div> <p><strong>Browser limits:</strong></p> <ul> <li>Chrome: max 7200 (2 hours)</li> <li>Firefox: max 86400 (24 hours)</li> <li>Safari: max 604800 (7 days)</li> </ul> <h3 id="access-control-expose-headers"><code class="language-plaintext highlighter-rouge">Access-Control-Expose-Headers</code></h3> <p><strong>Purpose:</strong> By default, JavaScript can only access these response headers: <code class="language-plaintext highlighter-rouge">Cache-Control</code>, <code class="language-plaintext highlighter-rouge">Content-Language</code>, <code class="language-plaintext highlighter-rouge">Content-Length</code>, <code class="language-plaintext highlighter-rouge">Content-Type</code>, <code class="language-plaintext highlighter-rouge">Expires</code>, <code class="language-plaintext highlighter-rouge">Pragma</code>. All other headers are hidden.</p> <p>To expose custom response headers (e.g., <code class="language-plaintext highlighter-rouge">X-Request-ID</code>, <code class="language-plaintext highlighter-rouge">X-RateLimit-Remaining</code>), the server must include:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Access-Control-Expose-Headers: X-Request-ID, X-RateLimit-Remaining
</code></pre></div></div> <hr/> <h2 id="7-credentials-and-cookies">7. Credentials and Cookies</h2> <h3 id="the-problem">The problem</h3> <p>By default, <code class="language-plaintext highlighter-rouge">fetch()</code> does not send cookies or auth headers cross-origin. To include them:</p> <p><strong>Client:</strong></p> <div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">fetch</span><span class="p">(</span><span class="dl">"</span><span class="s2">https://api.example.com/me</span><span class="dl">"</span><span class="p">,</span> <span class="p">{</span>
  <span class="na">credentials</span><span class="p">:</span> <span class="dl">"</span><span class="s2">include</span><span class="dl">"</span><span class="p">,</span> <span class="c1">// Send cookies cross-origin</span>
<span class="p">});</span>
</code></pre></div></div> <p><strong>Server must respond with:</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Access-Control-Allow-Origin: https://app.example.com  (NOT *)
Access-Control-Allow-Credentials: true
</code></pre></div></div> <h3 id="why--cannot-work-with-credentials">Why <code class="language-plaintext highlighter-rouge">*</code> cannot work with credentials</h3> <p>If <code class="language-plaintext highlighter-rouge">Access-Control-Allow-Origin: *</code> were allowed with credentials, any website could:</p> <ol> <li>Make a credentialed request to your API</li> <li>The browser would send the user’s cookies</li> <li>The <code class="language-plaintext highlighter-rouge">*</code> would allow any origin to read the response</li> <li>The attacker reads authenticated data</li> </ol> <p>This is why browsers enforce the rule: <strong>wildcard + credentials = rejected</strong>.</p> <h3 id="how-samesite-cookies-interact">How <code class="language-plaintext highlighter-rouge">SameSite</code> cookies interact</h3> <p>Modern browsers also enforce <code class="language-plaintext highlighter-rouge">SameSite</code> cookie attributes:</p> <ul> <li><code class="language-plaintext highlighter-rouge">SameSite=Strict</code>: Cookie is <strong>never</strong> sent cross-site</li> <li><code class="language-plaintext highlighter-rouge">SameSite=Lax</code>: Cookie sent only for top-level navigations (GET), not for <code class="language-plaintext highlighter-rouge">fetch()</code></li> <li><code class="language-plaintext highlighter-rouge">SameSite=None; Secure</code>: Cookie is sent cross-site (required for CORS with credentials)</li> </ul> <p>Even if CORS is correctly configured, a cookie with <code class="language-plaintext highlighter-rouge">SameSite=Lax</code> won’t be included in a cross-origin <code class="language-plaintext highlighter-rouge">fetch()</code>. The cookie must be <code class="language-plaintext highlighter-rouge">SameSite=None; Secure</code>.</p> <hr/> <h2 id="8-common-cors-misconfigurations">8. Common CORS Misconfigurations</h2> <h3 id="1-origin-reflection-without-validation">1. Origin Reflection Without Validation</h3> <div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">app</span><span class="p">.</span><span class="nf">use</span><span class="p">((</span><span class="nx">req</span><span class="p">,</span> <span class="nx">res</span><span class="p">,</span> <span class="nx">next</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="nx">res</span><span class="p">.</span><span class="nf">setHeader</span><span class="p">(</span><span class="dl">"</span><span class="s2">Access-Control-Allow-Origin</span><span class="dl">"</span><span class="p">,</span> <span class="nx">req</span><span class="p">.</span><span class="nx">headers</span><span class="p">.</span><span class="nx">origin</span><span class="p">);</span>
  <span class="nx">res</span><span class="p">.</span><span class="nf">setHeader</span><span class="p">(</span><span class="dl">"</span><span class="s2">Access-Control-Allow-Credentials</span><span class="dl">"</span><span class="p">,</span> <span class="dl">"</span><span class="s2">true</span><span class="dl">"</span><span class="p">);</span>
  <span class="nf">next</span><span class="p">();</span>
<span class="p">});</span>
</code></pre></div></div> <p>This reflects whatever origin the request sends. Combined with credentials, an attacker at <code class="language-plaintext highlighter-rouge">evil.com</code> can:</p> <ul> <li>Make a credentialed request to your API</li> <li>Your server reflects <code class="language-plaintext highlighter-rouge">evil.com</code> as the allowed origin</li> <li>The browser sends cookies and allows <code class="language-plaintext highlighter-rouge">evil.com</code> to read the response</li> <li><strong>Result: full data exfiltration</strong></li> </ul> <p><strong>Severity: Critical</strong></p> <h3 id="2-null-origin-allowance">2. Null Origin Allowance</h3> <div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">cors</span><span class="p">({</span> <span class="na">origin</span><span class="p">:</span> <span class="dl">"</span><span class="s2">null</span><span class="dl">"</span> <span class="p">});</span> <span class="c1">// or checking req.headers.origin === 'null'</span>
</code></pre></div></div> <p>The <code class="language-plaintext highlighter-rouge">null</code> origin can be crafted via:</p> <ul> <li>Sandboxed iframes: <code class="language-plaintext highlighter-rouge">&lt;iframe sandbox="allow-scripts" src="data:text/html,..."&gt;</code></li> <li><code class="language-plaintext highlighter-rouge">file://</code> URLs</li> <li>Cross-origin redirects</li> </ul> <p>An attacker embeds their exploit in a sandboxed iframe, which sends <code class="language-plaintext highlighter-rouge">Origin: null</code>, and the server approves it.</p> <h3 id="3-regex-mistakes">3. Regex Mistakes</h3> <div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">cors</span><span class="p">({</span> <span class="na">origin</span><span class="p">:</span> <span class="sr">/example</span><span class="se">\.</span><span class="sr">com/</span> <span class="p">});</span>
</code></pre></div></div> <p>This matches <code class="language-plaintext highlighter-rouge">malicious-example.com</code>, <code class="language-plaintext highlighter-rouge">example.com.evil.com</code>, etc. The regex is not anchored.</p> <p><strong>Correct:</strong></p> <div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">cors</span><span class="p">({</span> <span class="na">origin</span><span class="p">:</span> <span class="sr">/^https:</span><span class="se">\/\/([</span><span class="sr">a-z</span><span class="se">]</span><span class="sr">+</span><span class="se">\.)?</span><span class="sr">example</span><span class="se">\.</span><span class="sr">com$/</span> <span class="p">});</span>
</code></pre></div></div> <h3 id="4-pre-domain-matching">4. Pre-domain Matching</h3> <div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">allowed</span> <span class="o">=</span> <span class="p">[</span><span class="dl">'</span><span class="s1">https://app.example.com</span><span class="dl">'</span><span class="p">];</span>
<span class="kd">const</span> <span class="nx">origin</span> <span class="o">=</span> <span class="nx">req</span><span class="p">.</span><span class="nx">headers</span><span class="p">.</span><span class="nx">origin</span><span class="p">;</span>
<span class="k">if </span><span class="p">(</span><span class="nx">allowed</span><span class="p">.</span><span class="nf">some</span><span class="p">(</span><span class="nx">a</span> <span class="o">=&gt;</span> <span class="nx">origin</span><span class="p">.</span><span class="nf">includes</span><span class="p">(</span><span class="nx">a</span><span class="p">)))</span> <span class="p">{</span> <span class="p">...</span> <span class="p">}</span>
</code></pre></div></div> <p>An attacker registers <code class="language-plaintext highlighter-rouge">https://app.example.com.evil.com</code> - the <code class="language-plaintext highlighter-rouge">includes()</code> check passes.</p> <p><strong>Correct approach:</strong> Use exact string matching or properly anchored regexes.</p> <h3 id="5-wildcard-subdomain-trust">5. Wildcard Subdomain Trust</h3> <div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nf">cors</span><span class="p">({</span> <span class="na">origin</span><span class="p">:</span> <span class="sr">/^https:</span><span class="se">\/\/</span><span class="sr">.*</span><span class="se">\.</span><span class="sr">example</span><span class="se">\.</span><span class="sr">com$/</span> <span class="p">});</span>
</code></pre></div></div> <p>This trusts every subdomain. If any subdomain has an XSS vulnerability (say, <code class="language-plaintext highlighter-rouge">user-content.example.com</code>), the attacker can leverage that XSS to make credentialed requests from a trusted origin.</p> <hr/> <h2 id="9-cors-vs-other-browser-protections">9. CORS vs Other Browser Protections</h2> <h3 id="cors-vs-csrf">CORS vs CSRF</h3> <table> <thead> <tr> <th> </th> <th>CORS</th> <th>CSRF</th> </tr> </thead> <tbody> <tr> <td><strong>What it protects</strong></td> <td>Reading responses</td> <td>Performing unauthorized actions</td> </tr> <tr> <td><strong>Mechanism</strong></td> <td>Response headers</td> <td>Tokens, <code class="language-plaintext highlighter-rouge">SameSite</code> cookies</td> </tr> <tr> <td><strong>Who enforces</strong></td> <td>Browser (blocks response reading)</td> <td>Server (validates tokens)</td> </tr> <tr> <td><strong>Attack vector</strong></td> <td>Cross-origin data theft</td> <td>Cross-origin action execution</td> </tr> </tbody> </table> <p>CORS does not prevent CSRF. A form submission or a simple POST from an attacker’s site will reach your server regardless of CORS settings. You need CSRF tokens or <code class="language-plaintext highlighter-rouge">SameSite</code> cookies separately.</p> <h3 id="cors-vs-corb-cross-origin-read-blocking">CORS vs CORB (Cross-Origin Read Blocking)</h3> <p>CORB is a <strong>browser-side</strong> defense (introduced in Chrome 68) that blocks certain cross-origin responses from even entering the renderer process - before CORS checks. It protects against Spectre-style side-channel attacks.</p> <p>If a <code class="language-plaintext highlighter-rouge">&lt;script&gt;</code> tag requests an HTML or JSON resource cross-origin, CORB strips the body entirely, preventing even a Spectre attack from reading it from memory.</p> <p>CORB is transparent and automatic. It does not replace CORS - it adds defense-in-depth.</p> <h3 id="cors-vs-csp-content-security-policy">CORS vs CSP (Content Security Policy)</h3> <table> <thead> <tr> <th> </th> <th>CORS</th> <th>CSP</th> </tr> </thead> <tbody> <tr> <td><strong>Direction</strong></td> <td>Controls who can <strong>read from</strong> me</td> <td>Controls what my page can <strong>load</strong></td> </tr> <tr> <td><strong>Configured by</strong></td> <td>The server being called</td> <td>The server serving the page</td> </tr> <tr> <td><strong>Mechanism</strong></td> <td><code class="language-plaintext highlighter-rouge">Access-Control-*</code> response headers</td> <td><code class="language-plaintext highlighter-rouge">Content-Security-Policy</code> header</td> </tr> <tr> <td><strong>Example</strong></td> <td>“Only <code class="language-plaintext highlighter-rouge">app.example.com</code> can read my API”</td> <td>“My page can only load scripts from <code class="language-plaintext highlighter-rouge">cdn.example.com</code>”</td> </tr> </tbody> </table> <p>They protect from opposite directions. CSP says “I restrict what my page can do.” CORS says “I restrict who can read my responses.”</p> <h3 id="interaction">Interaction</h3> <p>In a well-configured system:</p> <ul> <li><strong>CSP</strong> prevents XSS by restricting script sources</li> <li><strong>CORS</strong> prevents data theft by restricting response access</li> <li><strong>SameSite cookies</strong> prevent CSRF by restricting cookie sending</li> <li><strong>CORB</strong> prevents side-channel data leaks</li> </ul> <p>These are layers of a defense-in-depth strategy, not alternatives to each other.</p> <hr/> <h2 id="10-practical-debugging">10. Practical Debugging</h2> <h3 id="browser-devtools---the-cors-debugging-workflow">Browser DevTools - The CORS Debugging Workflow</h3> <p><strong>Step 1: Open Network Tab</strong> Open DevTools (F12) → Network tab → reproduce the request.</p> <p><strong>Step 2: Look for the OPTIONS request</strong> If preflight is involved, you’ll see two requests:</p> <ol> <li><code class="language-plaintext highlighter-rouge">OPTIONS /api/data</code> - the preflight</li> <li><code class="language-plaintext highlighter-rouge">GET /api/data</code> (or POST, etc.) - the actual request</li> </ol> <p>If only the OPTIONS appears and the actual request is missing, the preflight failed.</p> <p><strong>Step 3: Inspect the preflight response headers</strong> Click the OPTIONS request and check Response Headers:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Access-Control-Allow-Origin: https://app.example.com   ← present?
Access-Control-Allow-Methods: GET, POST                 ← includes your method?
Access-Control-Allow-Headers: Authorization              ← includes your headers?
</code></pre></div></div> <p><strong>Step 4: Check the Console</strong> Chrome provides detailed CORS error messages:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Access to fetch at 'http://localhost:4000/api/data' from origin
'http://localhost:3000' has been blocked by CORS policy: No
'Access-Control-Allow-Origin' header is present on the requested resource.
</code></pre></div></div> <p><strong>Step 5: Common mistakes to check</strong></p> <ul> <li>Is the <code class="language-plaintext highlighter-rouge">Origin</code> being sent? (Check request headers)</li> <li>Is the server responding with the correct <code class="language-plaintext highlighter-rouge">Access-Control-Allow-Origin</code>?</li> <li>Is there a redirect? (Redirects during CORS can fail)</li> <li>Are credentials being sent? (Check if <code class="language-plaintext highlighter-rouge">Access-Control-Allow-Credentials: true</code> is present)</li> <li>Is the status code 200/204 for the preflight? (Some servers return 403 for OPTIONS)</li> </ul> <p><strong>Step 6: <code class="language-plaintext highlighter-rouge">curl</code> to isolate client vs server</strong> Test from the command line to determine if it’s a server configuration issue:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-v</span> <span class="nt">-X</span> OPTIONS <span class="se">\</span>
  <span class="nt">-H</span> <span class="s2">"Origin: http://localhost:3000"</span> <span class="se">\</span>
  <span class="nt">-H</span> <span class="s2">"Access-Control-Request-Method: GET"</span> <span class="se">\</span>
  http://localhost:4000/api/data
</code></pre></div></div> <p>If the response includes correct CORS headers, the server is fine - the issue is in the browser-side code. If not, the server needs fixing.</p> <hr/> <h2 id="11-real-world-deployment-patterns">11. Real-World Deployment Patterns</h2> <h3 id="nodejs--express">Node.js / Express</h3> <p>The <a href="https://github.com/karankessy/demoCORS">demoCORS</a> project uses the <code class="language-plaintext highlighter-rouge">cors</code> npm package, which is the standard for Express:</p> <div class="language-js highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">cors</span> <span class="o">=</span> <span class="nf">require</span><span class="p">(</span><span class="dl">"</span><span class="s2">cors</span><span class="dl">"</span><span class="p">);</span>

<span class="c1">// Production configuration</span>
<span class="nx">app</span><span class="p">.</span><span class="nf">use</span><span class="p">(</span>
  <span class="nf">cors</span><span class="p">({</span>
    <span class="na">origin</span><span class="p">:</span> <span class="p">[</span><span class="dl">"</span><span class="s2">https://app.example.com</span><span class="dl">"</span><span class="p">,</span> <span class="dl">"</span><span class="s2">https://staging.example.com</span><span class="dl">"</span><span class="p">],</span>
    <span class="na">methods</span><span class="p">:</span> <span class="p">[</span><span class="dl">"</span><span class="s2">GET</span><span class="dl">"</span><span class="p">,</span> <span class="dl">"</span><span class="s2">POST</span><span class="dl">"</span><span class="p">,</span> <span class="dl">"</span><span class="s2">PUT</span><span class="dl">"</span><span class="p">,</span> <span class="dl">"</span><span class="s2">DELETE</span><span class="dl">"</span><span class="p">],</span>
    <span class="na">allowedHeaders</span><span class="p">:</span> <span class="p">[</span><span class="dl">"</span><span class="s2">Content-Type</span><span class="dl">"</span><span class="p">,</span> <span class="dl">"</span><span class="s2">Authorization</span><span class="dl">"</span><span class="p">],</span>
    <span class="na">credentials</span><span class="p">:</span> <span class="kc">true</span><span class="p">,</span>
    <span class="na">maxAge</span><span class="p">:</span> <span class="mi">86400</span><span class="p">,</span>
  <span class="p">})</span>
<span class="p">);</span>
</code></pre></div></div> <p><strong>Common mistake:</strong> Using <code class="language-plaintext highlighter-rouge">cors()</code> with no options (allows <code class="language-plaintext highlighter-rouge">*</code>), then wondering why credentials don’t work.</p> <h3 id="nginx">Nginx</h3> <p>CORS is frequently handled at the reverse proxy level:</p> <div class="language-nginx highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">server</span> <span class="p">{</span>
    <span class="kn">location</span> <span class="n">/api/</span> <span class="p">{</span>
        <span class="kn">if</span> <span class="s">(</span><span class="nv">$request_method</span> <span class="p">=</span> <span class="s">'OPTIONS')</span> <span class="p">{</span>
            <span class="kn">add_header</span> <span class="s">'Access-Control-Allow-Origin'</span> <span class="nv">$http_origin</span><span class="p">;</span>
            <span class="kn">add_header</span> <span class="s">'Access-Control-Allow-Methods'</span> <span class="s">'GET,</span> <span class="s">POST,</span> <span class="s">PUT,</span> <span class="s">DELETE'</span><span class="p">;</span>
            <span class="kn">add_header</span> <span class="s">'Access-Control-Allow-Headers'</span> <span class="s">'Authorization,</span> <span class="s">Content-Type'</span><span class="p">;</span>
            <span class="kn">add_header</span> <span class="s">'Access-Control-Allow-Credentials'</span> <span class="s">'true'</span><span class="p">;</span>
            <span class="kn">add_header</span> <span class="s">'Access-Control-Max-Age'</span> <span class="mi">86400</span><span class="p">;</span>
            <span class="kn">return</span> <span class="mi">204</span><span class="p">;</span>
        <span class="p">}</span>

        <span class="kn">add_header</span> <span class="s">'Access-Control-Allow-Origin'</span> <span class="nv">$http_origin</span> <span class="s">always</span><span class="p">;</span>
        <span class="kn">add_header</span> <span class="s">'Access-Control-Allow-Credentials'</span> <span class="s">'true'</span> <span class="s">always</span><span class="p">;</span>

        <span class="kn">proxy_pass</span> <span class="s">http://backend</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p><strong>Common mistakes:</strong></p> <ul> <li>Forgetting the <code class="language-plaintext highlighter-rouge">always</code> directive (headers are not sent on error responses like 500)</li> <li>Using <code class="language-plaintext highlighter-rouge">$http_origin</code> without validation (origin reflection vulnerability)</li> <li>Not handling OPTIONS separately (the proxy forwards it to the backend, which may return 404)</li> </ul> <h3 id="cloud-apis-aws-api-gateway">Cloud APIs (AWS API Gateway)</h3> <p>AWS API Gateway has built-in CORS support:</p> <ul> <li>Configured per endpoint via console or CloudFormation/SAM</li> <li>Automatically generates OPTIONS responses</li> <li>Easy to misconfigure by enabling “allow all origins” during development and forgetting to restrict in production</li> </ul> <p><strong>Common mistake:</strong> Setting <code class="language-plaintext highlighter-rouge">Access-Control-Allow-Origin: *</code> on API Gateway and adding <code class="language-plaintext highlighter-rouge">Authorization</code> headers. This works without credentials but breaks when <code class="language-plaintext highlighter-rouge">credentials: 'include'</code> is added.</p> <h3 id="api-gateways-kong-envoy-traefik">API Gateways (Kong, Envoy, Traefik)</h3> <p>Most API gateways provide CORS as a plugin or middleware:</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Kong CORS plugin</span>
<span class="na">plugins</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">cors</span>
    <span class="na">config</span><span class="pi">:</span>
      <span class="na">origins</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="s">https://app.example.com</span>
      <span class="na">methods</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="s">GET</span>
        <span class="pi">-</span> <span class="s">POST</span>
      <span class="na">headers</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="s">Authorization</span>
        <span class="pi">-</span> <span class="s">Content-Type</span>
      <span class="na">credentials</span><span class="pi">:</span> <span class="kc">true</span>
      <span class="na">max_age</span><span class="pi">:</span> <span class="m">86400</span>
</code></pre></div></div> <p><strong>Advantage:</strong> CORS is handled at the edge, and individual microservices don’t need to implement it.</p> <p><strong>Risk:</strong> If the gateway handles CORS but a microservice also adds CORS headers, you get <strong>duplicate headers</strong> - and browsers may reject responses with multiple <code class="language-plaintext highlighter-rouge">Access-Control-Allow-Origin</code> values.</p> <h3 id="summary-of-common-deployment-mistakes">Summary of Common Deployment Mistakes</h3> <table> <thead> <tr> <th>Mistake</th> <th>Consequence</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">Access-Control-Allow-Origin: *</code> with auth APIs</td> <td>Credentials cannot be used</td> </tr> <tr> <td>Reflecting origin without validation</td> <td>Data theft via any malicious site</td> </tr> <tr> <td>CORS on application but not on error responses</td> <td>CORS errors mask 500 errors, making debugging impossible</td> </tr> <tr> <td>Duplicate CORS headers (gateway + app)</td> <td>Browser sees two <code class="language-plaintext highlighter-rouge">Allow-Origin</code> values and rejects</td> </tr> <tr> <td>Not handling OPTIONS at the proxy</td> <td>Preflight returns 404, all non-simple requests fail</td> </tr> <tr> <td>Allowing <code class="language-plaintext highlighter-rouge">null</code> origin</td> <td>Exploitable via sandboxed iframes</td> </tr> <tr> <td>Missing <code class="language-plaintext highlighter-rouge">Vary: Origin</code></td> <td>CDN/cache serves wrong origin header to wrong client</td> </tr> </tbody> </table> <hr/> <h2 id="conclusion-a-mental-model-for-cors">Conclusion: A Mental Model for CORS</h2> <p>CORS is often perceived as an obstacle - an annoying error that appears in the console during development. But understanding it deeply reveals that it is an elegantly simple protocol:</p> <ol> <li><strong>The browser is the enforcer.</strong> Not the server, not JavaScript - the browser’s security engine.</li> <li><strong>The server is the policy declarer.</strong> It communicates trust via response headers.</li> <li><strong>JavaScript is the subject.</strong> It can initiate requests but has no power to override CORS.</li> <li><strong>CORS protects the response, not the request.</strong> The server always receives the request (except when preflight fails).</li> <li><strong>Preflight is an optimization for safety.</strong> It prevents dangerous requests from being sent, not just their responses from being read.</li> </ol> <p>When you encounter a CORS error, the debugging approach is mechanical:</p> <ol> <li>What is my origin?</li> <li>What origin does the server allow?</li> <li>Do they match?</li> <li>Am I triggering preflight? Does the server handle OPTIONS?</li> <li>Am I sending credentials? Is the server configured for credentials?</li> </ol> <p>If you can answer these five questions, you can debug any CORS issue.</p> <hr/> <h2 id="try-it-yourself">Try It Yourself</h2> <p>Clone the <a href="https://github.com/karankessy/demoCORS"><strong>demoCORS</strong></a> project to see these concepts in action:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/karankessy/demoCORS.git
<span class="nb">cd </span>demoCORS
npm <span class="nb">install
</span>npm start
</code></pre></div></div> <p>Open <code class="language-plaintext highlighter-rouge">http://localhost:3000</code> and watch the <strong>Network</strong> tab in DevTools to see CORS headers and preflight requests in real time.</p> <hr/> <h2 id="further-reading">Further Reading</h2> <ul> <li><a href="https://developer.mozilla.org/en-US/docs/Web/HTTP/CORS">MDN - Cross-Origin Resource Sharing (CORS)</a></li> <li><a href="https://fetch.spec.whatwg.org/#http-cors-protocol">Fetch Standard - CORS Protocol</a></li> <li><a href="https://web.dev/articles/cross-origin-resource-sharing">web.dev - Cross-Origin Resource Sharing</a></li> <li><a href="https://portswigger.net/web-security/cors">PortSwigger - CORS Misconfigurations</a></li> <li><a href="https://cheatsheetseries.owasp.org/cheatsheets/CORS_Cheat_Sheet.html">OWASP - CORS Cheat Sheet</a></li> </ul>]]></content><author><name></name></author><category term="networking"/><category term="http"/><category term="security"/><category term="networking"/><summary type="html"><![CDATA[An in-depth exploration of Cross-Origin Resource Sharing (CORS), preflight requests, and common pitfalls, accompanied by a hands-on demo.]]></summary></entry><entry><title type="html">F5 BIG-IP + Wazuh: Getting Logs Working After 18 Months</title><link href="https://karankessy.github.io/blog/2025/f5-bigip-wazuh-logging/" rel="alternate" type="text/html" title="F5 BIG-IP + Wazuh: Getting Logs Working After 18 Months"/><published>2025-11-08T15:46:20+00:00</published><updated>2025-11-08T15:46:20+00:00</updated><id>https://karankessy.github.io/blog/2025/f5-bigip-wazuh-logging</id><content type="html" xml:base="https://karankessy.github.io/blog/2025/f5-bigip-wazuh-logging/"><![CDATA[<p>When hope was almost gone, I decided to give the F5–Wazuh integration one more try.<br/> Its already been eighteen months since I first had the failure trying integrate it, tried more than 50 times, had nearly convinced me these two were never going to get along.</p> <p>F5 BIG-IP is a good of a solutions when it comes to Web Application Firewall and Load Balancing tasks. I’ve been working with it for over half and a year now — it does its job quite well, protecting applications through real time traffic inspection and respective response enforcement. But it comes with a very real limit when it comes to local log storage at least in this series of F5 WAF which is claimed to be fixed in newer product series. On virtual editions, it caps at 3 million records or 2 GB of database space; on physical systems, it goes to 5 GB.</p> <p>I often get surprised when I find people getting disappointed over F5 not storing logs for much longer time. It is a limit but not really a flaw — it is so by design. WAF’s main job is to process live traffic and respond to attacks instantly &amp; accordingly. Storing, indexing, and searching logs is whole another world — slow, storage-hungry, and heavy on CPU. If F5 did all that itself, it would steal power from what it’s best at: stopping attacks as they happen. That’s why offloading logs &amp; events to a SIEM is all that makes sense &amp; that is why it is made for.</p> <p>Expecting a single product to be both a WAF and a SIEM is like asking a Formula 1 car to tow a truck—technically possible, but you’ll burn the engine before the truck moves.</p> <p><strong>And that is where <a href="https://wazuh.com/?utm_source=ambassadors&amp;utm_medium=referral&amp;utm_campaign=ambassadors+program">Wazuh</a> comes in.</strong><br/> F5 does it all to guard the gate, Wazuh keeps the records. It collects logs, parses them into useful fields, stores them for months or years depends how rich you are, and even alerts you when something suspicious happens. It understands CEF, and so does F5 if you set it to ArcSight mode. On paper, this pairing looked simple.</p> <hr/> <h3 id="the-eighteen-month-standoff">The Eighteen-Month Standoff</h3> <p>The documentation looked simple enough:</p> <ul> <li>Both systems support syslog.</li> <li>Both understand CEF.</li> <li>A few settings on each side, and done.</li> </ul> <p>But the reality wasn’t so generous. Find my issue here: <a href="https://github.com/wazuh/wazuh/issues/27392">#27392</a></p> <p>I tried buffer tweaks, IP changes, subnet shuffles, protocol switches, even manual rsyslog in the middle, even version downgrades. Sometimes some part of logs came through fine but just chunked in half, sometimes as hex garbage, sometimes not at all. One day a config would work, the next it was dead for no reason I could find.</p> <hr/> <h3 id="and-the-breakthrough">And the Breakthrough</h3> <p>And then, one day, no more drama — it just worked.<br/> Maybe Wazuh’s updates fixed something. Maybe my setup finally clicked. Or maybe, after all that time, the stars just lined up. I’m really unknown what is that miracle that made it work this time. It was the most basic configuration I did, which I had repeated maybe dozens of times 18 months before.</p> <p>Anyways, moving onwards with the newly-wed couples.</p> <p><strong>Step 1 – Create the F5 Logging Profile</strong><br/> In F5:</p> <ul> <li>Go to <strong>Security → Event Logs → Logging Profiles</strong></li> <li>Give it a name</li> <li>Select the category to log (for me: <code class="language-plaintext highlighter-rouge">Application Security</code>)</li> <li>Check <strong>Storage Destination → Remote Storage</strong></li> <li>Use <strong>TCP</strong> protocol for bigger payloads</li> <li>Add your Wazuh receiver’s IP and listening port</li> </ul> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/f5logp-480.webp 480w,/assets/img/f5logp-800.webp 800w,/assets/img/f5logp-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/f5logp.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <p><strong>Step 2 – Attach to Virtual Server</strong><br/> In <strong>Local Traffic → Virtual Servers</strong>, pick your target server.<br/> Attach the <code class="language-plaintext highlighter-rouge">LOG-WAZUH</code> profile under <strong>Security → Policies</strong>.<br/> That’s what makes the logs actually leave F5.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/f5secp-480.webp 480w,/assets/img/f5secp-800.webp 800w,/assets/img/f5secp-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/f5secp.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <p><strong>Step 3 – Configure Wazuh</strong><br/> In <code class="language-plaintext highlighter-rouge">ossec.conf</code>, allow the IP that F5 uses to send logs.<br/> That IP is the <strong>external self IP</strong> on F5.</p> <p>To find it:</p> <ul> <li>Go to <strong>Network → Self-IPs → External Self IPs</strong></li> <li>Or in the F5 CLI:</li> </ul> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ip route get 192.168.180.242 <span class="c"># (Wazuh receiver IP)</span>
</code></pre></div></div> <p>The output shows the sending IP — use that in Wazuh’s config.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/wazuhconf-480.webp 480w,/assets/img/wazuhconf-800.webp 800w,/assets/img/wazuhconf-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/wazuhconf.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <p>Restart Wazuh, and it’s ready to receive logs.</p> <hr/> <h3 id="seeing-the-results">Seeing the results</h3> <p>I opened the Wazuh dashboard — and there they were. Clean, parsed CEF logs from F5, showing (fields are yet to have their naming ceremony, never mind):</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/f5wlog-480.webp 480w,/assets/img/f5wlog-800.webp 800w,/assets/img/f5wlog-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/f5wlog.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <p>After eighteen months of silence, they were finally speaking.</p> <p>Then I hit my DVWA with some test payloads.</p> <h3 id="xss-it-a-bit">XSS it a bit</h3> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/f5xsslog-480.webp 480w,/assets/img/f5xsslog-800.webp 800w,/assets/img/f5xsslog-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/f5xsslog.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <h3 id="command-injection">Command Injection</h3> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/f5ci-480.webp 480w,/assets/img/f5ci-800.webp 800w,/assets/img/f5ci-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/f5ci.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/f5celog-480.webp 480w,/assets/img/f5celog-800.webp 800w,/assets/img/f5celog-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/f5celog.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <hr/> <h3 id="an-alternative-path">An alternative path</h3> <p>For high-volume production environments, F5 offers <strong>High Speed Logging (HSL)</strong> which is faster and more flexible than standard syslog. That’s a bigger story for another time; detailed guidance lives here: <a href="https://nishalrai.com.np/posts/remote-hsl-f5bigip/">Remote HSL for F5 BIG-IP</a>.</p> <hr/> <h3 id="and-the-lessons-from-the-journey">And the Lessons from the Journey</h3> <p>Just grace actually, but to know a thing is; each tool sticks to what it does best:</p> <ul> <li><strong>F5</strong> – live traffic analysis and blocking</li> <li><strong>Wazuh</strong> – storing, indexing, and digging through logs later</li> </ul> <p>Now, logs flow from F5 to Wazuh in real time. I can check months of history without worrying about F5’s storage limits.</p> <p>As I write this, logs keep coming in, each one like a little postcard from F5 saying, <em>“I caught something. Thought you should know.”</em></p> <p>It took eighteen months for them to talk. But now they do.</p> <p>Maybe it was an update, maybe a coincidence, at least my ego is not letting I was mistaken as I’ve almost given 1 month trying all the ways I could — or maybe machines just have their moods too.</p> <p>Nothing mystical about it, but after enough retries, even logic seems to respond better when you stop arguing with it.<br/> This time, it simply worked. And that was enough.</p> <hr/> <ul> <li>Be a part of Wazuh Ambassadors Program, if you believe you’re a good fit: <a href="https://wazuh.com/ambassadors-program/?utm_source=ambassadors&amp;utm_medium=referral&amp;utm_campaign=ambassadors+program">https://wazuh.com/ambassadors-program/</a></li> </ul>]]></content><author><name></name></author><category term="security"/><category term="f5-bigip"/><category term="siem"/><category term="security"/><category term="networking"/><summary type="html"><![CDATA[How I finally got F5 BIG-IP and Wazuh to exchange logs — configuration steps, troubleshooting, and lessons learned from an 18-month integration.]]></summary></entry><entry><title type="html">TOON - The New JSON</title><link href="https://karankessy.github.io/blog/2025/toon-the-new-json/" rel="alternate" type="text/html" title="TOON - The New JSON"/><published>2025-11-07T15:46:20+00:00</published><updated>2025-11-07T15:46:20+00:00</updated><id>https://karankessy.github.io/blog/2025/toon-the-new-json</id><content type="html" xml:base="https://karankessy.github.io/blog/2025/toon-the-new-json/"><![CDATA[<p>JSON is everywhere. APIs use it. So do backends and frontends. It is the basic and the default way most of the services talk.</p> <p>But JSON wasn’t made for large language models. It carries a lot of unnecessary baggage: curly braces, commas, extra spaces. All those were required for programmatic purposes, proper formatted so that it could be indexed properly, parsed properly, read properly &amp; play with all the kinds of logic we want. It help human eyes and programs, but for models they just take up too much tokens.</p> <p>And the problem is when you send JSON to an LLM, every character is a token. Each bracket be in starting-ending, each newline, each comma adds to your token count. That means higher cost, higher processing time, higher CO2 consumption and what not.</p> <p>Taking this as an example:</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"user_id"</span><span class="p">:</span><span class="w"> </span><span class="mi">123</span><span class="p">,</span><span class="w">
  </span><span class="nl">"email"</span><span class="p">:</span><span class="w"> </span><span class="s2">"heisenberg@gmail.com"</span><span class="p">,</span><span class="w">
  </span><span class="nl">"active"</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p>The data’s clear, but the format is heavy, redundant characters are too much. For models, all the extras waste tokens.</p> <p>And now what TOON (Token-Oriented Object Notation) does is, it just strips out what large language models don’t need, unnecessary curly braces, commas, extra spaces. It just keeps the data, drops the noise.</p> <p>Like this:</p> <p><code class="language-plaintext highlighter-rouge">user_id:123,email:heisenberg@gmail.com,active:true</code></p> <p>With this you allegedly reduce 40-60% of the tokens. Things run faster. It costs less to process, keeps your pocket warm &amp; also CO2 control, much needed one.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/toon-480.webp 480w,/assets/img/toon-800.webp 800w,/assets/img/toon-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/toon.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <p>But the problem is, TOON works best on flat JSON data. If your JSON is nested—one object inside another—flatten it first. If you don’t, TOON can make things worse. You’ll use more tokens; likely 20-30% more, not fewer.</p> <p>So, we have turn this (Nested JSON):</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
    </span><span class="s2">"user"</span><span class="w">
        </span><span class="p">{</span><span class="w">
            </span><span class="nl">"id"</span><span class="p">:</span><span class="mi">123</span><span class="p">,</span><span class="w">
            </span><span class="nl">"email"</span><span class="p">:</span><span class="s2">"heisenberg@gmail.com"</span><span class="w">
        </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p>To this (Flat Normalized JSON):</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"user.id"</span><span class="p">:</span><span class="w"> </span><span class="mi">123</span><span class="p">,</span><span class="w">
  </span><span class="nl">"user.email"</span><span class="p">:</span><span class="w"> </span><span class="s2">"heisenberg@gmail.com"</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p>You can make use of JSON normalizer packages if available for the respective programming languages. Then only, the TOON gives you the benefit.</p> <p>Thus, we have to make use of TOON when it fits:</p> <ul> <li>JSON is flat or you can flatten it, either manually or via normalizers.</li> <li>Token costs matter.</li> <li>You’re optimizing for models—not humans.</li> </ul> <p>And hence, If you are in need of human readability, stick with JSON. If your JSON structure is deeply nested and you can’t flatten it due to some complexities, TOON may not help.</p> <p>Which is why it asks us to measure the difference and then only test on our own data or make use of it.</p> <p>The point is simple. Better performance, lower cost, less waste. No drama. No buzzwords.</p> <p>And that’s how we tune and optimize systems to run and not just compile go, not knowing if it is the function or the disease running it.</p> <p>Know more in detail: <a href="https://github.com/toon-format/toon">TOON Github</a></p>]]></content><author><name></name></author><category term="ai"/><category term="data-formats"/><category term="ai"/><summary type="html"><![CDATA[TOON is about to take a wild ride in LLMs. Its going to save you money, time & power. Go through it.]]></summary></entry><entry><title type="html">Three Essential HTTP Routing Patterns</title><link href="https://karankessy.github.io/blog/2025/http-routing-patterns/" rel="alternate" type="text/html" title="Three Essential HTTP Routing Patterns"/><published>2025-04-18T12:13:30+00:00</published><updated>2025-04-18T12:13:30+00:00</updated><id>https://karankessy.github.io/blog/2025/http-routing-patterns</id><content type="html" xml:base="https://karankessy.github.io/blog/2025/http-routing-patterns/"><![CDATA[<p>Every reverse proxy, ingress controller, and load balancer is answering the same question: given this request, which backend gets it. There are only really three places to look for the answer, and which one you pick has consequences beyond routing.</p> <hr/> <h2 id="1-host-based">1. Host-based</h2> <p>Route on the <code class="language-plaintext highlighter-rouge">Host</code> header. Several domains resolve to the same address, and the proxy separates them by the name the client asked for.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Host: api.example.com      ──►  api service
Host: app.example.com      ──►  frontend
Host: admin.example.com    ──►  admin service
</code></pre></div></div> <p>This is the oldest of the three, and it is what virtual hosting has meant since HTTP/1.1 made the <code class="language-plaintext highlighter-rouge">Host</code> header mandatory. Before that, a server could not tell which site a request was for and you needed an IP address per site.</p> <p>The thing that matters in practice is TLS. Each hostname needs a certificate that covers it, so you are managing a SAN certificate, a wildcard, or per-host certificates. Host-based routing pushes work into certificate management that path-based routing does not.</p> <p>What it buys you is clean separation. Different domains can have genuinely different security postures, different certificates, and can be moved to different infrastructure later without any client-visible URL change.</p> <hr/> <h2 id="2-path-based">2. Path-based</h2> <p>Route on the URI path.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>http://api.example.com/getprofile/v1/123456
                      └──────────────────┘
                              path
</code></pre></div></div> <p>One hostname, one certificate, requests split by prefix. <code class="language-plaintext highlighter-rouge">/api</code> to one service, <code class="language-plaintext highlighter-rouge">/static</code> to another, <code class="language-plaintext highlighter-rouge">/v2</code> to a newer deployment.</p> <p>This is the dominant pattern in container orchestration, and the reason is that it composes well with ingress controllers. Adding a service means adding a path rule, not provisioning a hostname and a certificate.</p> <p>The catch is that the path is part of your public API. A service mounted at <code class="language-plaintext highlighter-rouge">/api/v1/users</code> has that prefix baked into every client, every bookmark, and every integration. Moving it later is a breaking change or a permanent redirect you maintain forever. Hostnames are comparatively easy to repoint via DNS; paths are not.</p> <p>Path rewriting helps, and introduces its own confusion when the path the backend sees differs from the path the client sent. That mismatch shows up in logs, in generated URLs, and in redirects, and it is a recurring source of bugs.</p> <hr/> <h2 id="3-header-based">3. Header-based</h2> <p>Route on any other header, or on a cookie.</p> <p>This is the flexible one and the one to use most sparingly.</p> <p>It covers cookie-based session persistence, sending a client back to the backend that holds its session. It covers A/B testing and canary releases, where a header or cookie decides whether a request reaches the new version. It covers feature flags and internal-only routing on a custom header.</p> <p>The <code class="language-plaintext highlighter-rouge">Host</code> header is excluded from this category by convention, because host-based routing is its own well-understood thing with its own certificate implications.</p> <p>Two cautions. Routing on a header that a client controls is a trust decision: if a custom header sends requests to an internal or preview backend, anyone can set that header. Strip or validate it at the edge. And header-based rules are invisible in the URL, which makes them the hardest of the three to debug, because the request that failed looks identical to the one that worked.</p> <hr/> <h2 id="choosing">Choosing</h2> <p>These are not alternatives so much as layers, and real deployments use all three at once: host to pick the environment, path to pick the service, header to pick the version.</p> <p>The useful instinct is to route on the most stable attribute that distinguishes the traffic. Hostnames change rarely. Paths change more often and are harder to change. Headers change constantly and are invisible.</p> <p>Push routing decisions toward the stable end where you can, and reserve header-based rules for the things that are genuinely dynamic, like rollouts and session affinity, rather than for structure that ought to be visible in the URL.</p>]]></content><author><name></name></author><category term="networking"/><category term="http"/><category term="networking"/><summary type="html"><![CDATA[Host-based, path-based, and header-based routing. What each one reads from the request, what that costs you, and why the Host header sits in its own category.]]></summary></entry><entry><title type="html">WebAssembly: Will this replace Docker?</title><link href="https://karankessy.github.io/blog/2024/webassembly-vs-docker/" rel="alternate" type="text/html" title="WebAssembly: Will this replace Docker?"/><published>2024-12-27T15:46:20+00:00</published><updated>2024-12-27T15:46:20+00:00</updated><id>https://karankessy.github.io/blog/2024/webassembly-vs-docker</id><content type="html" xml:base="https://karankessy.github.io/blog/2024/webassembly-vs-docker/"><![CDATA[<p>Docker shipped a technical preview integrating WebAssembly, and the question that followed was whether WASM eventually replaces containers.</p> <p>I think the question is framed wrong, and the reason is that the two technologies are solving different problems that happen to look similar from a distance. Worth working through what each one actually does before comparing them.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/wasm-docker-hero-480.webp 480w,/assets/img/wasm-docker-hero-800.webp 800w,/assets/img/wasm-docker-hero-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/wasm-docker-hero.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="WebAssembly and Docker side by side" data-zoomable="" loading="eager" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> </div> </div> <hr/> <h2 id="webassembly">WebAssembly</h2> <p>WASM is a binary instruction format. You compile C, C++, or Rust into it, and a runtime executes it.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/wasm2-480.webp 480w,/assets/img/wasm2-800.webp 800w,/assets/img/wasm2-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/wasm2.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <p>It started in the browser, alongside HTML, CSS, and JavaScript, so that heavy applications could run on a web page at close to native speed. Tools like Emscripten compile existing C codebases into it.</p> <p>The part that made it interesting outside the browser is that the format does not assume a browser. Any system with a WASM runtime can execute the binary, in the way that the JVM runs bytecode or CPython runs its own. The binary is platform-neutral: same module, different operating systems and processor architectures, no recompilation.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/wasm-runtime-diagram-480.webp 480w,/assets/img/wasm-runtime-diagram-800.webp 800w,/assets/img/wasm-runtime-diagram-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/wasm-runtime-diagram.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" alt="WASM runtime executing modules outside the browser" data-zoomable="" loading="eager" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> </div> </div> <p>On its own a WASM module can do nothing but compute. It has no file access, no network, no clock. WASI is the interface that grants those capabilities explicitly, and that is what makes server-side WASM possible at all.</p> <p>That detail is also the security story. A container starts with broad access to a namespaced view of a system and you restrict from there. A WASM module starts with nothing and you grant from there. Deny-by-default is a meaningfully different starting position.</p> <hr/> <h2 id="docker">Docker</h2> <p>Docker packages an application together with its dependencies into an image, and runs that image as a container.</p> <p>The problem it solves is environment drift. Running a C program on someone else’s machine means the right compiler, the right libraries, the right paths. The image carries all of it, so the program runs the same on a colleague’s laptop as on a server.</p> <p>Underneath, a container is not a virtual machine. It is a process on the host kernel, isolated with namespaces and cgroups. That is why containers are cheap compared to VMs, and it is also why an image built for Linux on x86 does not run on a different kernel or architecture.</p> <hr/> <h2 id="comparing-them">Comparing them</h2> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/wasm3-480.webp 480w,/assets/img/wasm3-800.webp 800w,/assets/img/wasm3-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/wasm3.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" data-zoomable="" loading="eager" onerror="this.onerror=null; document.querySelectorAll('.responsive-img-srcset').forEach(function (n) { n.remove(); });"/> </picture> </figure> <p><strong>What gets packaged.</strong> Docker ships a filesystem: your binary plus the libraries and userland it needs. WASM ships a compiled module and nothing else, with WASI supplying system access at runtime.</p> <p><strong>Portability.</strong> A Docker image is tied to an OS and architecture. A WASM module is not. This is the clearest advantage WASM has.</p> <p><strong>Size and startup.</strong> Container images run tens to hundreds of megabytes and start in seconds. WASM modules are typically a few megabytes and start in milliseconds. That startup difference is what makes WASM attractive for serverless and edge workloads, where cold start is the dominant cost.</p> <p><strong>Isolation model.</strong> Containers share the host kernel and are isolated by namespaces. WASM runs in a sandbox with no capabilities until granted. Different mechanisms, and the WASM model is the more restrictive by default.</p> <hr/> <h2 id="so-replacement">So, replacement?</h2> <p>No, and the size and startup numbers are a hint as to why.</p> <p>A WASM module is small because it does not carry a userland. That is a benefit when your workload is a self-contained computation and a hard limitation when your workload is PostgreSQL, or anything that expects a filesystem, a process model, threads, and a full libc. Containers carry all of that because plenty of software genuinely needs it.</p> <p>Docker’s own integration reflects this. It runs WASM containers alongside Linux and Windows containers under the same tooling, rather than as a migration path off them. You get one workflow, one registry, one orchestrator, with the right runtime per workload.</p> <p>The likely shape is boring and reasonable: WASM for short-lived, compute-shaped, cold-start-sensitive work at the edge and in plugin systems, containers for long-running services with real system dependencies, both under the same management layer.</p> <p>The interesting thing about WASM is not that it might displace containers. It is that a portable, deny-by-default, millisecond-start execution format is a genuinely new primitive, and the useful question is what that enables rather than what it replaces.</p>]]></content><author><name></name></author><category term="systems"/><category term="containers"/><category term="systems"/><summary type="html"><![CDATA[WebAssembly vs Docker. WASM, a technology that revamped the way we thought about containerization.]]></summary></entry></feed>